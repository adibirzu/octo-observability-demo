fastapi==0.138.1
uvicorn[standard]==0.49.0
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
psycopg2-binary==2.9.12
oracledb==4.0.1
httpx==0.28.1
python-multipart==0.0.32
jinja2==3.1.6
aiofiles==25.1.0

# OpenTelemetry
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
opentelemetry-instrumentation-fastapi==0.64b0
opentelemetry-instrumentation-sqlalchemy==0.64b0
opentelemetry-instrumentation-httpx==0.64b0
opentelemetry-instrumentation-logging==0.64b0
opentelemetry-exporter-prometheus==0.64b0
prometheus-client==0.25.0

# OCI SDK (for logging)
oci==2.180.0

# Security (bcrypt for password hashing, PyJWT for IDCS SSO ID-token verification)
bcrypt==5.0.0
PyJWT[crypto]==2.13.0

psutil>=7.2.2

# Transitive — opentelemetry-instrumentation imports `packaging` but
# does not declare it as a runtime dep on some platforms. Pin here so
# container builds are reproducible.
packaging>=26.2

# Redis client — rate limit + cache + async order stream
redis[hiredis]>=7.4.0,<8.0

# Alembic — schema migrations against ATP / local Postgres
alembic>=1.18.4,<2.0

# Local editable packages (octo-cache, octo-async-worker) — install via
# requirements-dev.txt or Dockerfile COPY + pip install -e. Not pinned here
# so `pip install -r requirements.txt` works in CI without the monorepo tree.
