---
milestone: v1.1
milestone_name: scaling-demo
shipped: 2026-05-19
requirements_added_in_milestone: [SCALE-01, SCALE-02, SCALE-03, SCALE-04]
all_satisfied: true
audit: ../v1.1-MILESTONE-AUDIT.md
---

# Milestone v1.1 — Requirements (Archived)

All four SCALE-* requirements added for v1.1 are **satisfied** with evidence per phase verifier and the milestone audit.

## Requirements added in v1.1

### Scale and Elasticity

- [x] **SCALE-01** — HPA expansion covers shop, crm, and java-apm services with CPU + memory targets; the D-05 RPS metric is gated behind a nested Helm flag (`autoscaling.rps.enabled`) that defaults to `false`, so the legacy `helm template` output is bit-identical until an operator opts in. CRM HPA remains untouched (D-01).
  - **Evidence:** `deploy/k8s/oke/shop/deployment.yaml`, `deploy/k8s/oke/apm-java-demo/deployment.yaml`, `deploy/helm/octo-apm-demo/templates/{shop-hpa,java-gateway-hpa}.yaml`, `deploy/helm/octo-apm-demo/values.yaml`
  - **Tests:** `tests/test_stress_demo_surface.py` 14/14 green; `helm template` with default values produces 0 External-metric blocks (parity check)
  - **Verified by:** Plan 07-01

- [x] **SCALE-02** — The OKE Cluster Autoscaler add-on is configurable through an idempotent, dry-run-by-default operator script (`deploy/oke/configure-cluster-autoscaler.sh`); a sibling `prometheus-adapter` Helm values file publishes `shop_request_rate` and `java_request_rate` as External Metrics for the optional custom-metric HPA path (default off).
  - **Evidence:** `deploy/oke/configure-cluster-autoscaler.sh`, `deploy/oke/cluster-autoscaler-config.json`, `deploy/helm/octo-apm-demo/charts/prometheus-adapter-values.yaml`
  - **Tests:** `tests/test_stress_demo_surface.py` adds 8 assertions covering CA script + adapter values; offline contract tests verify dry-run default + no inline OCIDs
  - **Verified by:** Plan 07-02
  - **Operator-deferred:** Live `oci ce cluster install-addon cluster-autoscaler` + Prometheus-adapter `helm install` gated behind approved rollout window (STATE.md Blockers/Concerns)

- [x] **SCALE-03** — An admin-only `/admin/stress-test` UI on `admin.${DNS_DOMAIN}` exposes a FastAPI router with Pydantic-validated apply/clear/state endpoints, a three-channel MELTS audit (OTel span + Log Analytics `push_log` + OCI Monitoring `octo_apm_demo/stress_run_count` point) keyed by `run_id`, host-bound + OCTO scope enforcement, and a k6-based load-generator pod authenticated by `X-Internal-Service-Key` cross-service auth.
  - **Evidence:** `crm/server/modules/{stress_test,_admin_host}.py`, `crm/server/templates/stress_test_admin.html`, `crm/server/main.py`, `tools/stress-runner/`, `deploy/k8s/oke/stress-runner/`
  - **Tests:** `tests/test_stress_demo_surface.py` 17 new endpoint/audit assertions green; `tests/test_admin_host_helper.py` 10/10; `crm/tests/test_admin_coordinator.py` 7/7 (Phase 5 admin-host regression intact)
  - **Verified by:** Plans 07-03, 07-04, 07-05, 07-06

- [x] **SCALE-04** — APM saved queries, OCI Monitoring custom metrics in the `octo_apm_demo` namespace + alarm definitions, Log Analytics OKE Monitoring saved searches, and a published autoscaling dashboard are captured via existing apply tooling. No live OCI calls run in unit tests; only offline contract tests per VALIDATION.md Dimension 8.
  - **Evidence:** `tools/apm-saved-queries/` (4 JSON specs + apply.sh), `tools/monitoring-alarms/` (2 alarm JSONs + apply.sh), `tools/la-saved-searches/oke-autoscaling-*.json` (4 searches + dashboard), `site/workshop/lab-11-oke-autoscaling.md`, `site/operations/stress-demo-lb-routing.md`
  - **Tests:** 8 new pytest assertions + 4 LA saved-search assertions; `mkdocs build --strict` green
  - **Verified by:** Plans 07-07, 07-08, 07-09, 07-10
  - **Operator-deferred:** Live `apply.sh APPLY=true` runs (APM saved queries + Monitoring alarms + LA saved searches) executed by operator at demo-deploy time with required env (`COMPARTMENT_ID`, `APM_DOMAIN_ID`, `NOTIFICATION_TOPIC_OCID`)

## Outcome

| Requirement | Status | Evidence | Notes |
|---|---|---|---|
| SCALE-01 | ✓ satisfied | Plan 07-01 | Default-off backward compat verified |
| SCALE-02 | ✓ satisfied | Plan 07-02 | Live add-on apply operator-gated |
| SCALE-03 | ✓ satisfied | Plans 07-03/04/05/06 | Three-channel MELTS audit wired |
| SCALE-04 | ✓ satisfied | Plans 07-07/08/09/10 | Lab 11 walkthrough operator-gated |

**4/4 satisfied. No adjustments, no drops, no orphans.**

## Future scope identified during v1.1

Promoted to future milestones (not v1.1 blockers):
- Phoenix-native build + registry migration (Phase 8 candidate, `.planning/PHASE-8-BRIEF.md`)
- Cluster ops hardening — boot volume resize, daily image GC, Stack Monitoring metric-server clean-reinstall (Phase 9 candidate)
- Per-service Prometheus exposition + mgmt-agent scrape config (separate metric-instrumentation phase)
- `git filter-repo` repo recreate to scrub orphan SHAs (executed 2026-05-19 outside the gsd flow)
