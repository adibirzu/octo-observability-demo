---
phase: 08-phoenix-native-build-registry-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/oke/ocir-login.sh
  - site/operations/phoenix-auth-token-setup.md
autonomous: false
requirements: [BUILD-02]
tags: [phoenix, ocir, auth-token, docker-login, secret-hygiene]
must_haves:
  truths:
    - "deploy/oke/ocir-login.sh reads the OCIR auth token strictly from `~/.ocir-token` (mode 0600), pipes it via stdin to `docker login --password-stdin`, and refuses to run if the token file is missing or has wider mode than 0600."
    - "The script never echoes, cats, or assigns the token to a shell variable that could be inadvertently printed. It uses shell redirection: `docker login ... < \"${HOME}/.ocir-token\"`."
    - "Script ends with the line 'Done. OCIR auth token was not printed.' (matching analog at deploy/oke/bootstrap-demo-secrets.sh:275)."
    - "site/operations/phoenix-auth-token-setup.md documents the Auth Token generation procedure (OCI Console → Identity → Users → Auth Tokens), the `~/.ocir-token` file convention (mode 0600), and the operator bash-history audit step (`unset HISTFILE` or `env HISTFILE=/dev/null bash`)."
  artifacts:
    - path: "deploy/oke/ocir-login.sh"
      provides: "Operator-gated docker login to ${OCIR_REGION}.ocir.io using --password-stdin from a 0600 token file. No token value ever appears on the command line or in stdout."
    - path: "site/operations/phoenix-auth-token-setup.md"
      provides: "Runbook for generating + storing the OCIR auth token on the jumphost, with placeholder-only env-var table and bash-history hygiene checklist."
  key_links:
    - from: "~/.ocir-token (mode 0600)"
      to: "docker login phx.ocir.io"
      via: "docker login --username \"${OCIR_USERNAME}\" --password-stdin \"${OCIR_REGION}.ocir.io\" < \"${HOME}/.ocir-token\""
    - from: "site/operations/phoenix-auth-token-setup.md"
      to: "deploy/oke/ocir-login.sh"
      via: "Step-by-step: generate token → scp to jumphost ~/.ocir-token mode 0600 → run ocir-login.sh"
---

<objective>

Create the operator-gated docker login wrapper `deploy/oke/ocir-login.sh` that authenticates the Phoenix jumphost to `${OCIR_REGION}.ocir.io` using an Auth Token read from `~/.ocir-token` (mode 0600), piped to docker via `--password-stdin`. Pair it with a runbook documenting the Auth Token generation procedure and bash-history hygiene.

This plan addresses **BUILD-02** (auth via Auth Token, never API key; token piped via stdin to keep it out of shell history) and is the last prerequisite before the build pipeline pilot in Plan 08-04. Per `<security_rules>` and PATTERNS shared pattern D, the token MUST NEVER appear as a `--password <val>` argument or be assigned to a shell variable that could be echoed. The runbook MUST use placeholder tokens only (`<OCIR_USERNAME>`, `${OCIR_TENANCY}`) per `.planning/SECURITY.md`.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/SECURITY.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-PATTERNS.md
@deploy/oke/bootstrap-demo-secrets.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy/oke/ocir-login.sh with stdin-only token flow + mode-0600 enforcement</name>
  <files>deploy/oke/ocir-login.sh</files>
  <action>
    Create the script following the header/usage pattern from `deploy/oke/bootstrap-demo-secrets.sh:1-31` (PATTERNS analog 08-03). Use `set -euo pipefail`. Defaults: `: "${OCIR_REGION:=us-phoenix-1}"`, `: "${OCIR_USERNAME:?OCIR_USERNAME is required — e.g. \\${OCIR_TENANCY}/<user-name>}"`, `: "${OCIR_TOKEN_FILE:=${HOME}/.ocir-token}"`, `: "${BUILDER:=docker}"`. Implement `require_mode()` taking `(file, want_octal)` — uses `stat -c '%a' "$f" 2>/dev/null || stat -f '%Lp' "$f"` (Linux/macOS portable) and `[[ "$got" == "$want" ]] || exit 1` (analog provided inline in PATTERNS Plan 08-03 section, lines 122-131). Implement `main()`: assert `OCIR_TOKEN_FILE` exists, run `require_mode "$OCIR_TOKEN_FILE" 600`, then run the docker login via shell redirection (analog `docker login --username "${OCIR_USERNAME}" --password-stdin "${OCIR_REGION}.ocir.io" < "${OCIR_TOKEN_FILE}"`). If `BUILDER=podman` is selected, use `podman login --username "${OCIR_USERNAME}" --password-stdin "${OCIR_REGION}.ocir.io" < "${OCIR_TOKEN_FILE}"` instead. After login succeeds, run `docker info 2>/dev/null | grep -A1 'Registry:'` (or `podman info`) to confirm the registry endpoint matches `${OCIR_REGION}.ocir.io` — fail loudly if not. The token MUST NEVER be assigned to a variable: NO `TOKEN=$(cat ~/.ocir-token)`, NO `docker login -p "$TOKEN"`, NO `echo "$TOKEN"` — verifier greps for these anti-patterns. Final line: `echo "Done. OCIR auth token was not printed."` (analog `deploy/oke/bootstrap-demo-secrets.sh:275`).
  </action>
  <verify>
    <automated>bash -n deploy/oke/ocir-login.sh && grep -q '\-\-password-stdin' deploy/oke/ocir-login.sh && grep -q "require_mode.*600" deploy/oke/ocir-login.sh && grep -q 'OCIR auth token was not printed' deploy/oke/ocir-login.sh && ! grep -Eq '(--password\s+["$]|TOKEN=|cat[[:space:]]+"?\$\{?HOME\}?/\.ocir-token|echo[[:space:]]+"?\$\{?TOKEN)' deploy/oke/ocir-login.sh && ! grep -E 'ocid1\.[a-z]+\.oc1\.[a-z0-9-]+\.[a-z0-9]{20,}' deploy/oke/ocir-login.sh</automated>
  </verify>
  <done>Script uses --password-stdin via shell redirection only, enforces mode 0600 on the token file, contains zero variable assignments of the token, ends with the no-secrets-printed line.</done>
</task>

<task type="auto">
  <name>Task 2: Write site/operations/phoenix-auth-token-setup.md runbook (placeholders only)</name>
  <files>site/operations/phoenix-auth-token-setup.md</files>
  <action>
    Create the runbook following the header pattern from `site/operations/stress-demo-lb-routing.md:1-13` (PATTERNS Plan 08-08 analog). Sections: "When to apply" (cutover prep for Phase 8 build pipeline), "Prerequisites" (placeholder-only env-var table per PATTERNS shared pattern F — rows for `OCIR_REGION`, `OCIR_TENANCY`, `OCIR_USERNAME`, `JUMPHOST_PUBLIC_IP`), "Steps" (numbered: (1) Generate Auth Token in OCI Console under Identity → Users → Auth Tokens → Generate Token, store name as "octo-apm-demo-jumphost-build"; (2) Copy token to clipboard ONCE — it cannot be retrieved later; (3) On operator workstation, run `env HISTFILE=/dev/null bash` to start a no-history shell; (4) `scp` or `rsync` the token to `opc@<JUMPHOST_PUBLIC_IP>:~/.ocir-token` — alternatively `ssh opc@<JUMPHOST_PUBLIC_IP> 'cat > ~/.ocir-token'` then paste-and-Ctrl-D; (5) `ssh opc@<JUMPHOST_PUBLIC_IP> 'chmod 600 ~/.ocir-token && ls -la ~/.ocir-token'` — confirm mode `-rw-------`; (6) `ssh opc@<JUMPHOST_PUBLIC_IP> 'cd /tmp/octo-apm-demo && bash deploy/oke/ocir-login.sh'` — confirm login succeeds), "Verify" (`docker info` on jumphost shows `phx.ocir.io` in registry list; `docker pull phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:latest` returns "not found" but not "unauthorized" — proves auth works against an empty repo), "Rollback" (delete `~/.ocir-token`, revoke the auth token in OCI Console, then re-run the steps; the token is non-recoverable so revoking is the only way to invalidate a leaked copy), "Audit + recording" (operator MUST run `history -c && unset HISTFILE` at end of session, confirm `cat ~/.bash_history` is empty on the jumphost, audit per PATTERNS shared pattern D). Reference `.planning/SECURITY.md` for the placeholder convention. Never inline a real auth token, real OCID, or real IP — verifier greps.
  </action>
  <verify>
    <automated>test -f site/operations/phoenix-auth-token-setup.md && grep -q '\-\-password-stdin' site/operations/phoenix-auth-token-setup.md && grep -q 'chmod 600' site/operations/phoenix-auth-token-setup.md && grep -q 'HISTFILE' site/operations/phoenix-auth-token-setup.md && grep -q 'Rollback' site/operations/phoenix-auth-token-setup.md && ! grep -E 'ocid1\.[a-z]+\.oc1\.[a-z0-9-]+\.[a-z0-9]{20,}' site/operations/phoenix-auth-token-setup.md && ! grep -E '(130\.|144\.|161\.|129\.153\.)[0-9]+\.[0-9]+\.[0-9]+' site/operations/phoenix-auth-token-setup.md</automated>
  </verify>
  <done>Runbook exists, covers token generation through revocation, references the script, uses only placeholders, documents bash-history hygiene.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator generates token, places it on jumphost, runs ocir-login.sh</name>
  <what-built>OCIR auth token flow with secret-hygiene guardrails: stdin-only token, mode 0600 file, no-history shell, post-session audit.</what-built>
  <how-to-verify>
    1. Follow `site/operations/phoenix-auth-token-setup.md` end-to-end (operator side — generate token in OCI Console, scp to jumphost, chmod 600).
    2. On jumphost: `bash deploy/oke/ocir-login.sh` (with `OCIR_USERNAME` env var set, e.g. `${OCIR_TENANCY}/operator-name`).
    3. Confirm output ends with "Login Succeeded" (docker's own message) followed by "Done. OCIR auth token was not printed."
    4. Confirm `cat ~/.docker/config.json | grep -c phx.ocir.io` returns ≥ 1 (auth recorded — but the credsStore may be `file` or `secretservice`, that's fine).
    5. Confirm `cat ~/.bash_history` on jumphost does NOT contain the token (it shouldn't — token never crossed a command line).
    6. Smoke test: `docker pull phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:latest` returns "manifest unknown" or "not found" — NOT "unauthorized". This proves auth works against an empty repo.
  </how-to-verify>
  <resume-signal>Type "ocir-login-ok" if login succeeded and token did not leak to history, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- `bash -n deploy/oke/ocir-login.sh` exits 0.
- Anti-pattern greps return zero hits (no `--password <val>`, no `TOKEN=`, no `cat ~/.ocir-token`, no `echo $TOKEN`).
- Runbook references the script and includes a rollback section.
- Operator confirms login succeeded against phx.ocir.io and token did not leak to bash history.
</verification>

<success_criteria>
- Jumphost is authenticated to `phx.ocir.io/${OCIR_TENANCY}` via Auth Token (not API key).
- Token file is mode 0600.
- Auth token never appeared as a CLI argument or in any history file.
- Runbook is committable (placeholders only, no live values).
</success_criteria>

<references>
- `08-PATTERNS.md` Plan 08-03 section (lines 88-131): stdin-only token pattern, mode-0600 enforcement, docker-registry secret analog.
- `08-PATTERNS.md` shared pattern D (no-secret-printing + bash-history hygiene).
- `deploy/oke/bootstrap-demo-secrets.sh:230-242` — kubectl-side docker-registry secret analog.
- `site/operations/stress-demo-lb-routing.md:1-31` — runbook structural analog.
</references>

<output>
After completion, create `.planning/phases/08-phoenix-native-build-registry-migration/08-03-SUMMARY.md` capturing: token-on-jumphost confirmed (yes), docker login outcome, bash-history audit result, and any deviations from runbook steps.
</output>
