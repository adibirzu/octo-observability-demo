---
phase: 08-phoenix-native-build-registry-migration
plan: 08
type: execute
wave: 7
depends_on: [08-07]
files_modified:
  - docs/adr/0001-phoenix-region-migration.md
  - site/operations/phoenix-build-runbook.md
  - mkdocs.yml
autonomous: true
requirements: [BUILD-05, BUILD-06, BUILD-07]
tags: [phoenix, docs, adr, runbook, rollback-drill, cold-pull-benchmark]
must_haves:
  truths:
    - "docs/adr/0001-phoenix-region-migration.md exists and captures context/decision/consequences/alternatives in Michael-Nygard ADR shape"
    - "site/operations/phoenix-build-runbook.md exists with sections: When to apply, Prerequisites, Steps, Verify, Rollback, Audit"
    - "Runbook documents the `helm rollback` drill — operator transcript captured during verify-work (BUILD-06)"
    - "Runbook explicitly instructs operator to edit ~/.claude/CLAUDE.md but does NOT modify it from this repo (out-of-repo file)"
    - "mkdocs nav includes the new runbook so `mkdocs build --strict` passes"
    - "Cold-pull benchmark transcript captured: `crictl pull phx.ocir.io/${OCIR_TENANCY}/octo-drone-shop:<head-tag>` vs `crictl pull eu-frankfurt-1.ocir.io/${OCIR_TENANCY}/octo-drone-shop:<head-tag>` measured on the SAME OKE worker, both pulls cold (image removed first). Phx wall-clock must be < 25% of frankfurt wall-clock to satisfy BUILD-05."
  artifacts:
    - path: "docs/adr/0001-phoenix-region-migration.md"
      provides: "First ADR in repo; canonical record of the region migration decision"
    - path: "site/operations/phoenix-build-runbook.md"
      provides: "Operator runbook for first-time setup, recurring image refresh, frankfurt rollback, AND the BUILD-05 cold-pull benchmark procedure + transcript table"
    - path: "mkdocs.yml"
      provides: "Nav entry pointing at the new runbook"
  key_links:
    - from: "docs/adr/0001-phoenix-region-migration.md"
      to: ".planning/REQUIREMENTS.md BUILD-01..07"
      via: "References section at bottom of ADR"
    - from: "site/operations/phoenix-build-runbook.md"
      to: "deploy/oke/build-push-all-images.sh + deploy/oke/rolling-cutover.sh"
      via: "Steps section invokes both scripts in order"
    - from: "mkdocs.yml nav"
      to: "site/operations/phoenix-build-runbook.md"
      via: "nav entry"
---

<objective>
Capture the Phase 8 architectural decision in repo history and ship the operator runbook that
makes the new pipeline maintainable beyond the initial cutover. Exercise the BUILD-06 rollback
drill (`helm rollback` to a frankfurt-pinned revision) and capture the transcript in the runbook
so the next operator does not have to re-discover the procedure.

Closes BUILD-06 (rollback drill documented + exercised) and BUILD-07 (ADR + runbook in repo). The
"global CLAUDE.md update" portion of BUILD-07 is OUT-OF-REPO — this plan only documents the
required edit as a runbook step. Do NOT modify `~/.claude/CLAUDE.md` from any task in this plan.

Security: per `.planning/SECURITY.md`, all placeholders are `<UPPERCASE_TOKEN>` or `${ENV_VAR}`.
The ADR and runbook explicitly call out the placeholder convention. No real OCIDs, IPs, or
tenancy namespace strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/SECURITY.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-PATTERNS.md
@site/operations/stress-demo-lb-routing.md
@mkdocs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create first ADR in repo — docs/adr/0001-phoenix-region-migration.md</name>
  <files>docs/adr/0001-phoenix-region-migration.md</files>
  <action>The `docs/adr/` directory does not yet exist — create it. Use the Michael-Nygard canonical
  ADR shape (template inlined in 08-PATTERNS.md plan 08-08 section).

  Required sections (in this order):
  - Title: `# ADR 0001: Phoenix-Native Build + Registry for octo-apm-demo`
  - Metadata block:
    - `**Status:** Accepted`
    - `**Date:** 2026-05-19`
    - `**Deciders:** Platform operators (role only — no personal names per .planning/SECURITY.md)`
  - `## Context` (2-3 paragraphs):
    - Describe the cross-region OCIR pull problem: octo-apm-demo-oke runs in us-phoenix-1, images
      lived in eu-frankfurt-1.ocir.io. Cold-pull latency observed during Phase 7 stress runs.
    - Cite BUILD-01..07 by ID.
    - Use placeholders for any infrastructure detail: `<OKE_WORKER_PRIV_IP>`, `<JUMPHOST_PUBLIC_IP>`,
      `${OCIR_TENANCY}`. Do NOT inline a real worker IP, instance OCID, or namespace.
    - Reference the Phase 7 finding that motivated the migration (without quoting tenancy data).
  - `## Decision` (1 paragraph):
    - Build on `octo-demo-jumphost-v5` (us-phoenix-1, x86_64 VM.Standard.E5.Flex).
    - Push to `phx.ocir.io/${OCIR_TENANCY}/*`.
    - Default `OCIR_REGION=us-phoenix-1` in `deploy/oke/build-push-images.sh` and
      `deploy/helm/octo-apm-demo/values.yaml`.
    - Frankfurt repos retained as fallback for ≥7 days, accessible via `OCIR_REGION=eu-frankfurt-1`
      operator override + `helm rollback`.
  - `## Consequences`:
    - Positive: cold-pull latency reduction (target: &lt;25% of frankfurt pull time per BUILD-05),
      single-region failure domain for the demo, image-pull traffic stays inside us-phoenix-1.
    - Negative: jumphost becomes a build SPOF (mitigation: control-plane-oci in frankfurt remains
      the documented fallback per global CLAUDE.md "Cloud-Based Docker Builds" — see BUILD-07
      runbook step in 08-08).
    - Neutral: image content is bit-identical between regions; only the registry endpoint changes.
      Deployment manifests already use `${OCIR_REGION}` envsubst so the migration is a default
      flip, not a manifest rewrite.
  - `## Alternatives considered`:
    - OCIR cross-region replication (rejected: not supported by OCIR Service today).
    - Local ARM builds with QEMU emulation (rejected per global CLAUDE.md "Cloud-Based Docker
      Builds" rule — slow and unreliable on Apple Silicon).
    - Status quo with frankfurt builds + cross-region pulls (rejected: fails BUILD-05 cold-pull
      target).
    - Mirror via Harbor or external pull-through cache (rejected: adds an SPOF outside OCI tenancy
      boundary, conflicts with "stay inside LogAnalytics compartment" rule from 08-CONTEXT.md).
  - `## References`:
    - `.planning/REQUIREMENTS.md` BUILD-01..07
    - `.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md`
    - `~/.claude/CLAUDE.md` "Cloud-Based Docker Builds & Deployments" section (referenced; not
      modified from this repo — see runbook for the required operator-side edit).
    - `deploy/oke/build-push-images.sh` (source of truth for `OCIR_REGION=us-phoenix-1` default)
    - `deploy/oke/build-push-all-images.sh` (wrapper from 08-05)
    - `deploy/oke/rolling-cutover.sh` (cutover script from 08-07)
    - `site/operations/phoenix-build-runbook.md` (operator runbook — Task 2 of this plan)

  Cite `.planning/SECURITY.md` in a short footer ("This ADR follows the placeholder convention in
  .planning/SECURITY.md — no real OCIDs, IPs, or tenancy namespace strings inline.").

  Length target: 60-120 lines, prose-heavy, no code blocks except the placeholder examples. Do
  NOT include real OCI CLI invocations in the ADR — those live in the runbook.</action>
  <verify>
    <automated>test -f docs/adr/0001-phoenix-region-migration.md &amp;&amp; grep -q "## Context" docs/adr/0001-phoenix-region-migration.md &amp;&amp; grep -q "## Decision" docs/adr/0001-phoenix-region-migration.md &amp;&amp; grep -q "## Consequences" docs/adr/0001-phoenix-region-migration.md &amp;&amp; grep -q "## Alternatives" docs/adr/0001-phoenix-region-migration.md &amp;&amp; ! grep -E 'ocid1\.[a-z]+\.oc1\.[a-z0-9-]+\.[a-z0-9]{20,}' docs/adr/0001-phoenix-region-migration.md</automated>
  </verify>
  <done>ADR exists with all four canonical sections. No real OCIDs or IPs (grep returns no matches).
  Length 60-120 lines. BUILD-01..07 referenced. `docs/adr/` directory created (first ADR in repo).</done>
</task>

<task type="auto">
  <name>Task 2: Create operator runbook + mkdocs nav entry + exercise helm rollback drill</name>
  <files>site/operations/phoenix-build-runbook.md, mkdocs.yml</files>
  <action>Mirror the shape of `site/operations/stress-demo-lb-routing.md` (the closest in-repo
  analog per 08-PATTERNS.md — same author voice, same redaction discipline).

  Required runbook sections (in this order, matching the analog):

  1. **Header + 2-3 sentence context paragraph**: explain what contract the runbook preserves
     (octo-apm-demo image pipeline pointing at phx OCIR), when to run (initial cutover, recurring
     image refreshes, rollback emergencies), why now (BUILD-05 cold-pull target).

  2. **When to apply** — three scenarios (sub-headings):
     - First-time setup (08-01..04 prerequisites: NSG, jumphost tools, OCIR login, pilot build)
     - Recurring image refresh (build-push-all-images.sh → rolling-cutover.sh cadence)
     - Rollback to frankfurt (helm rollback drill — BUILD-06)

  3. **Prerequisites** — markdown table, placeholder-only per `.planning/SECURITY.md`. Rows:
     | Item | How to set | Notes |
     | `JUMPHOST_PUBLIC_IP` | env var or oci compute instance get | Placeholder — never commit |
     | `JUMPHOST_INSTANCE_OCID` | `<JUMPHOST_INSTANCE_OCID>` | Source from terraform output |
     | `OCIR_REGION` | export `us-phoenix-1` | Default; override to `eu-frankfurt-1` for rollback |
     | `OCIR_TENANCY` | `oci os ns get --profile demo` | 12-char namespace — never inline |
     | `OCIR_USERNAME` | `<tenancy>/oracleidentitycloudservice/<user>@<domain>` | Per OCIR docs |
     | `LOGANALYTICS_COMPARTMENT_OCID` | `<LOGANALYTICS_COMPARTMENT_OCID>` | Placeholder — read from outputs.json |
     | `APM_DOMAIN_ID` | `<APM_DOMAIN_ID>` | Placeholder; sourced from outputs.json by rolling-cutover.sh |

  4. **Steps** — numbered, each with a bash block + notes paragraph:
     - Step 1: SSH to jumphost via the NSG rule added in 08-01.
     - Step 2: Run `deploy/oke/ocir-login.sh` (08-03) — token-via-stdin, audit
       `~/.bash_history` after.
     - Step 3: Run `APPLY=true deploy/oke/build-push-all-images.sh` (08-05). Capture the
       `obs-YYYYMMDDhhmmss` tag printed by the wrapper.
     - Step 4: From operator workstation (not jumphost), run
       `APPLY=true IMAGE_TAG=obs-<tag> deploy/oke/rolling-cutover.sh` (08-07). Watch the APM
       continuity probe.
     - Step 5: Post-cutover audit (the BUILD-04 success criterion grep — see 08-CONTEXT.md
       success criteria #1).

  5. **Verify** — three checks:
     a) All five phx repos `image_count > 0` (BUILD-01).
     b) `kubectl get pods -A -o jsonpath='{.items[*].spec.containers[*].image}' | tr ' ' '\n'
        | grep -c eu-frankfurt-1.ocir.io` returns 0.
     c) APM dashboard Last-30-minutes view shows 8/8 services healthy.

  6. **Rollback** — two options per the analog's pattern (`stress-demo-lb-routing.md:110-132`):
     - **Option A (standard rollback, BUILD-06):** `helm history octo-apm-demo -n octo` to find
       the pre-cutover revision, then `helm rollback octo-apm-demo <rev> -n octo`. Returns the
       cluster to frankfurt images within 5 min (BUILD-06 acceptance window).
     - **Option B (last-resort, per-service):** for each service, `kubectl set image
       deployment/<deploy> <container>=eu-frankfurt-1.ocir.io/${OCIR_TENANCY}/<repo>:<last-good-tag>`.
       Used only if helm state is corrupt or the chart no longer renders.

  7. **Rollback drill transcript** — Required by BUILD-06. The operator runs `helm rollback` in
     dry-run mode during verify-work and pastes the transcript here (redacted with placeholders).
     The drill MUST be exercised before declaring Phase 8 complete; the runbook docstring says so
     explicitly.

  8. **Audit + recording** — same shape as the analog. Workshop `run_id` (placeholder),
     change-management PR comment, OCI Audit log pointer. ADD a section about
     `~/.bash_history` audit on the jumphost (pattern D in 08-PATTERNS.md).

  9. **Required operator-side edit to ~/.claude/CLAUDE.md** (BUILD-07 second half):
     - Document the exact diff the operator must apply: in the "Cloud-Based Docker Builds &
       Deployments" section of `~/.claude/CLAUDE.md`, change "Preferred: Build on Control Plane
       VM" to make `octo-demo-jumphost-v5` (phx) the primary host and `control-plane-oci`
       (frankfurt) the fallback. Add a one-line note: "Use phx jumphost for octo-apm-demo; use
       frankfurt control-plane-oci for ObserveAI / enterprise-crm-portal-as-its-own-project /
       other tenants."
     - The runbook PROVIDES the exact diff as a markdown code block but DOES NOT execute it. The
       file `~/.claude/CLAUDE.md` is operator-private and outside this repo — never modify it
       from any task in this plan.

  10. **References** — link the ADR (`docs/adr/0001-phoenix-region-migration.md`), the requirement
      file, and the three scripts shipped in 08-05 / 08-07.

  Length target: 200-300 lines. No real OCIDs, IPs, tenancy strings (placeholders only).

  **mkdocs nav entry**: open `mkdocs.yml`, find the existing Operations / Runbooks section (the
  one that contains `stress-demo-lb-routing.md`), add the new runbook entry adjacent to it:
  ```yaml
  - Phoenix build pipeline: operations/phoenix-build-runbook.md
  ```
  Preserve YAML indentation. Verify with `mkdocs build --strict` — must pass.

  No new third-party dep. Markdown only. Cite `.planning/SECURITY.md` and BUILD-06/BUILD-07 in
  the runbook footer.</action>
  <verify>
    <automated>test -f site/operations/phoenix-build-runbook.md &amp;&amp; grep -q "## Rollback" site/operations/phoenix-build-runbook.md &amp;&amp; grep -q "helm rollback" site/operations/phoenix-build-runbook.md &amp;&amp; grep -q "phoenix-build-runbook.md" mkdocs.yml &amp;&amp; mkdocs build --strict 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Runbook exists with all 10 sections, ≥200 lines. mkdocs nav entry present. `mkdocs build
  --strict` passes (no broken links, no missing nav targets). `grep -E
  'ocid1\.[a-z]+\.oc1\.[a-z0-9-]+\.[a-z0-9]{20,}|130\.|161\.|144\.|129\.|10\.42\.'
  site/operations/phoenix-build-runbook.md` returns no matches. Operator-side `~/.claude/CLAUDE.md`
  diff documented as a code block, not applied from this plan. BUILD-06 helm rollback drill
  transcript section present (transcript itself filled in during verify-work, not by this plan).</done>
</task>

</tasks>

<verification>
1. ADR shape:
   `for s in Context Decision Consequences Alternatives References; do grep -q "## $s" docs/adr/0001-phoenix-region-migration.md || echo MISSING:$s; done`
   — no output (all sections present).
2. ADR redaction: `grep -E 'ocid1\.[a-z]+\.oc1\.[a-z0-9-]+\.[a-z0-9]{20,}' docs/adr/0001-phoenix-region-migration.md`
   returns no matches.
3. Runbook shape: `for s in "When to apply" "Prerequisites" "Steps" "Verify" "Rollback" "Audit"; do
   grep -q "## $s" site/operations/phoenix-build-runbook.md || echo MISSING:$s; done` — no output.
4. Runbook redaction: same regex as ADR — returns no matches.
5. mkdocs build: `mkdocs build --strict` exits 0.
6. BUILD-06 acceptance during verify-work: operator runs `helm rollback octo-apm-demo <prev-rev>
   -n octo --dry-run`, pastes the transcript into the runbook's "Rollback drill transcript"
   section, commits.
7. BUILD-07 acceptance: ADR present + runbook documents the required `~/.claude/CLAUDE.md` edit
   (which the operator then applies manually outside this repo).
</verification>

<success_criteria>
- BUILD-06 closed: rollback drill documented; runbook has the exact `helm rollback` invocation;
  drill exercised in dry-run with transcript captured.
- BUILD-07 closed: first ADR in repo; runbook in mkdocs nav; operator-side CLAUDE.md edit documented
  (not executed from this repo).
- `docs/adr/` directory exists (first ADR ships).
- `mkdocs build --strict` passes.
- No real OCIDs, IPs, or tenancy namespace strings in any new doc.
- All Phase 8 requirements BUILD-01..07 traceable to a shipped plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08-phoenix-native-build-registry-migration/08-08-SUMMARY.md`
with: list of files created/modified, mkdocs strict-build output, ADR + runbook line counts, helm
rollback drill transcript (redacted), BUILD-06 + BUILD-07 closure evidence.
</output>
