---
phase: 08-phoenix-native-build-registry-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/oke/jumphost-bootstrap.sh
autonomous: false
requirements: [BUILD-02]
tags: [phoenix, ocir, jumphost, nsg, reachability]
must_haves:
  truths:
    - "SSH to octo-demo-jumphost-v5 from current developer egress IP succeeds on tcp/22 (or tcp/2222 fallback)."
    - "deploy/oke/jumphost-bootstrap.sh exists, is mode 0755, and runs in default dry-run mode without mutating jumphost or NSG."
    - "Dry-run output records jumphost arch (x86_64), kernel, available boot-volume free space (GB), uptime, and `who`/`last -n 5` summary so the operator can confirm jumphost is dedicated."
    - "If the developer egress /32 is not present in octo-demo-jumphost-nsg ingress rules, the script prints a copy-pasteable `oci network nsg rules add` command scoped to <LOGANALYTICS_COMPARTMENT_OCID> for the operator to run interactively."
  artifacts:
    - path: "deploy/oke/jumphost-bootstrap.sh"
      provides: "Read-only inventory + NSG ingress-gap reporter (dry-run by default). Extended in Plan 08-02 with --install mode."
  key_links:
    - from: "deploy/oke/jumphost-bootstrap.sh"
      to: "<JUMPHOST_PUBLIC_IP>:22"
      via: "ssh -o BatchMode=yes -o ConnectTimeout=5 opc@<JUMPHOST_PUBLIC_IP> 'uname -a; df -h /; who'"
    - from: "deploy/oke/jumphost-bootstrap.sh"
      to: "octo-demo-jumphost-nsg (<JUMPHOST_NSG_OCID>)"
      via: "oci network nsg rules list --nsg-id <JUMPHOST_NSG_OCID> --compartment-id <LOGANALYTICS_COMPARTMENT_OCID> (read-only)"
---

<objective>

Establish reachability to the Phoenix build host `octo-demo-jumphost-v5` and create the inventory-capture skeleton that Plans 08-02 and 08-03 will extend. This plan must NOT install tools or mutate the NSG — it only verifies SSH works and reports the gap so the operator can decide whether to add a `/32` ingress rule manually.

This plan addresses **BUILD-02** (reproducible build pipeline on Phoenix jumphost) by getting the operator into a known reachable state and capturing the jumphost baseline (arch, disk, existing tool inventory) before any later plan installs software. All NSG mutation is operator-gated (the script prints commands but never executes `oci ... add` itself), per `.planning/SECURITY.md` and the `<security_rules>` for this phase.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/SECURITY.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-PATTERNS.md
@deploy/oke/bootstrap-demo-secrets.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy/oke/jumphost-bootstrap.sh skeleton (dry-run inventory only)</name>
  <files>deploy/oke/jumphost-bootstrap.sh</files>
  <action>
    Create the script following the header/usage pattern from `deploy/oke/bootstrap-demo-secrets.sh:1-31` (PATTERNS analog 08-02). Use `set -euo pipefail`. Defaults: `: "${OCI_PROFILE:=demo}"`, `: "${APPLY:=false}"`, `: "${INSTALL:=false}"` (INSTALL is wired in Plan 08-02 — declare here, no-op for now), `: "${JUMPHOST_USER:=opc}"`, `: "${JUMPHOST_HOST:=${JUMPHOST_PUBLIC_IP:?JUMPHOST_PUBLIC_IP is required}}"`, `: "${OUTPUTS_FILE:=${REPO_ROOT}/credentials/demo/outputs.json}"`. Refuse to run if `OUTPUTS_FILE` missing — compartment id is read from it via `jq -r '.deployment_compartment_id.value'` (see PATTERNS shared pattern C). Implement three functions: `report_ssh()` runs the one-shot SSH probe in must_haves.key_links and pipes output through `sed 's/[0-9]\{1,3\}\(\.[0-9]\{1,3\}\)\{3\}/<IP_REDACTED>/g'` so no live IPs land in operator scrollback; `report_jumphost_inventory()` SSHes again to capture `uname -m`, `cat /etc/os-release`, `df -h /`, `command -v docker podman oci git rsync jq helm` (one per line, OK if some are missing — the gap is the signal); `report_nsg_ingress()` calls `oci network nsg rules list --nsg-id <JUMPHOST_NSG_OCID> --compartment-id <LOGANALYTICS_COMPARTMENT_OCID> --all --query 'data[].{src:source,proto:protocol,desc:description}' --output table` and detects whether the current developer's egress IP (capture via `curl -s https://api.ipify.org` once at start of script and store in local variable, never write to file). If missing, print the exact `oci network nsg rules add` command the operator would run — but do NOT execute it. Script ends with the standard "Done. No mutations were performed." line (analog `deploy/oke/bootstrap-demo-secrets.sh:275`). Use only `<PLACEHOLDER>` tokens in script comments — never inline real IPs or OCIDs (binding rule from `.planning/SECURITY.md:9-18` and PATTERNS shared pattern F).
  </action>
  <verify>
    <automated>bash -n deploy/oke/jumphost-bootstrap.sh && grep -c 'set -euo pipefail' deploy/oke/jumphost-bootstrap.sh | grep -q '^1$' && grep -E '^[^#]*: "\$\{APPLY:=false\}"' deploy/oke/jumphost-bootstrap.sh | grep -c . | grep -q '^1$' && ! grep -E '(130\.|144\.|161\.|129\.153\.)[0-9]+\.[0-9]+\.[0-9]+' deploy/oke/jumphost-bootstrap.sh && ! grep -E 'ocid1\.[a-z]+\.oc1\.[a-z0-9-]+\.[a-z0-9]{20,}' deploy/oke/jumphost-bootstrap.sh</automated>
  </verify>
  <done>Script is syntactically valid, defaults to dry-run, contains no inline IPs, contains no real OCIDs, and uses only placeholder tokens in comments.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the inventory script and confirms SSH + records the NSG gap</name>
  <what-built>A dry-run reachability + inventory script for `octo-demo-jumphost-v5`. No NSG mutation, no tool install — purely informational.</what-built>
  <how-to-verify>
    1. Set required env vars locally (never commit): `export JUMPHOST_PUBLIC_IP=<your-jumphost-ip>` (resolved from your tenancy's `credentials/demo/outputs.json` or terraform output).
    2. Run: `bash deploy/oke/jumphost-bootstrap.sh`.
    3. Confirm output shows: jumphost arch=`x86_64`, OS=Oracle Linux 8.x, `/` partition free space ≥ 30GB (required for docker + image layers), at least `git`, `jq` present (others may be missing — that is fine, Plan 08-02 installs them).
    4. Confirm output shows the current NSG ingress rules table and either "Current egress already present" or a copy-pasteable `oci network nsg rules add` command. If the latter, decide whether to run it interactively before proceeding to Plan 08-02 (operator discretion — out of scope for this plan).
    5. Confirm the closing line "Done. No mutations were performed." appears.
  </how-to-verify>
  <resume-signal>Type "ssh-confirmed" if SSH works and inventory captured, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- `bash -n deploy/oke/jumphost-bootstrap.sh` exits 0.
- Operator confirms SSH reachability and at-least-30GB free disk on jumphost `/`.
- No live IPs / OCIDs / tenancy namespace in committed script.
- Script ends with "No mutations were performed." string.
</verification>

<success_criteria>
- Operator can SSH to jumphost from current egress IP (with or without an NSG rule add).
- Jumphost arch confirmed as `x86_64`, disk ≥ 30GB free.
- Baseline tool inventory captured (which of docker/podman/oci/git/rsync/jq/helm exist today).
- No script in `deploy/oke/` contains real OCIDs or public IPs (grep gates above).
</success_criteria>

<references>
- `08-PATTERNS.md` Plan 08-01 section (lines 34-42): operator-gated NSG mutation, compartment-scoped CLI, `/32` ingress hygiene.
- `08-PATTERNS.md` shared patterns A (operator-gated APPLY), C (compartment scoping), F (placeholder discipline).
- `deploy/oke/bootstrap-demo-secrets.sh:1-31` — header/usage skeleton analog.
- `deploy/oke/build-push-images.sh:54-59` — `require_tool` shape (inverted here: report-not-install).
</references>

<output>
After completion, create `.planning/phases/08-phoenix-native-build-registry-migration/08-01-SUMMARY.md` capturing: SSH-confirmed (yes/no), NSG gap (yes/no + whether operator ran the add command interactively), jumphost arch/disk/tool inventory line count, and any deviations from the must_haves.
</output>
