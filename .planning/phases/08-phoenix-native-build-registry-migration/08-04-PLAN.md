---
phase: 08-phoenix-native-build-registry-migration
plan: 04
type: tdd
wave: 3
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - tests/test_phoenix_build_surface.py
  - deploy/oke/build-push-one-image.sh
autonomous: false
requirements: [BUILD-01, BUILD-02, BUILD-05]
tags: [phoenix, ocir, tdd, traffic-generator, build-pipeline, crictl, cold-pull]
must_haves:
  truths:
    - "tests/test_phoenix_build_surface.py exists, was committed RED first, and asserts the structural contract of deploy/oke/build-push-one-image.sh BEFORE that script is written (TDD discipline)."
    - "deploy/oke/build-push-one-image.sh is parameterized over (image_dir, image_name) — it does NOT hard-code the traffic-generator path. It reuses the build_push / ensure_repo / verify_runtime_uid helpers from deploy/oke/build-push-images.sh."
    - "Running the script on the jumphost (rsync-from-operator → build on jumphost → push to phx.ocir.io) produces an immutable date-tagged image `phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:obs-YYYYMMDDhhmmss` AND a `:latest` alias (only when PUSH_LATEST=true)."
    - "A cold-pull benchmark from an OKE worker (`crictl pull phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:<tag>`) records wall-clock time and the equivalent eu-frankfurt-1 pull for the same image tag (when available), satisfying BUILD-05 measurement methodology for this single service."
    - "All tests in tests/test_phoenix_build_surface.py pass GREEN after the script is written."
  artifacts:
    - path: "tests/test_phoenix_build_surface.py"
      provides: "pytest surface tests for deploy/oke/build-push-one-image.sh (RED-first contract). Read-text-and-grep style matching tests/test_stress_demo_surface.py."
    - path: "deploy/oke/build-push-one-image.sh"
      provides: "Single-service build wrapper parameterized over (image_dir, image_name). Pilot service: octo-traffic-generator. Reusable in Plan 08-05 for the other 4 services."
  key_links:
    - from: "operator workstation"
      to: "jumphost /tmp/octo-apm-demo/tools/traffic-generator/"
      via: "rsync -avz --exclude .git tools/traffic-generator/ opc@<JUMPHOST_PUBLIC_IP>:/tmp/octo-apm-demo/tools/traffic-generator/"
    - from: "deploy/oke/build-push-one-image.sh"
      to: "phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:obs-YYYYMMDDhhmmss"
      via: "docker buildx build --platform linux/amd64 -f Dockerfile -t <image>:<tag> --push <context>"
    - from: "OKE worker node"
      to: "phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:<tag>"
      via: "kubectl debug node/<name> -it --image=busybox -- crictl pull <image>; or SSH via bastion"
---

<objective>

Pilot the Phoenix-native build pipeline on a single service — `octo-traffic-generator` — using strict TDD: write the RED test file first (asserting the structural contract of a new `deploy/oke/build-push-one-image.sh` wrapper), commit it failing, then implement the script GREEN. The pilot validates the end-to-end flow (rsync → build on jumphost → push to phx OCIR → cold-pull from an OKE worker) before Plan 08-05 generalizes it to the remaining 4 application services.

This plan addresses **BUILD-01** (image present in `phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator` with `image_count > 0`), **BUILD-02** (reproducible pipeline on jumphost, immutable date tag, `--password-stdin` upstream from Plan 08-03), and **BUILD-05** (cold-pull benchmark methodology — for this plan, only `octo-traffic-generator` is measured; full BUILD-05 comparison against `octo-drone-shop` lands in Plan 08-05 + 08-07). Per `<security_rules>`, every placeholder uses `<UPPERCASE_TOKEN>` or `${ENV_VAR}` — never inline tenancy namespace, IPs, or OCIDs.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/SECURITY.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-PATTERNS.md
@deploy/oke/build-push-images.sh
@tools/traffic-generator/Dockerfile
@tests/test_stress_demo_surface.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing tests for deploy/oke/build-push-one-image.sh — commit BEFORE script exists</name>
  <files>tests/test_phoenix_build_surface.py</files>
  <behavior>
    - test_script_exists_and_is_executable_or_runnable: ROOT/deploy/oke/build-push-one-image.sh exists and `bash -n` would parse it. (First version: file does not exist → test fails RED.)
    - test_script_has_strict_mode: contains `set -euo pipefail`.
    - test_script_defaults_ocir_region_to_phoenix: matches `: "${OCIR_REGION:=us-phoenix-1}"` (regression guard — never let frankfurt sneak back as default).
    - test_script_refuses_mutable_latest_tag_without_override: contains the `ALLOW_LATEST_IMAGE_TAG` gate (analog `deploy/oke/build-push-images.sh:50-52,153-156`).
    - test_script_uses_immutable_date_tag_default: contains `IMAGE_TAG:=$(date -u +%Y%m%d%H%M%S)` or an `obs-` prefixed equivalent.
    - test_script_accepts_image_dir_and_image_name_args: usage block documents two required positional args (or `--image-dir` + `--image-name`); the script must NOT hard-code `octo-traffic-generator`.
    - test_script_reuses_compartment_from_outputs_json: contains `jq -r '.deployment_compartment_id.value'` (PATTERNS shared pattern C — compartment scoping).
    - test_script_calls_docker_buildx_with_platform_amd64: contains `docker buildx build --platform "${PLATFORM}"` and default `PLATFORM:=linux/amd64`.
    - test_script_pushes_via_push_flag_not_separate_login: does NOT contain `docker login` (login is handled by deploy/oke/ocir-login.sh per Plan 08-03 — separation of concerns).
    - test_script_calls_verify_runtime_uid_or_skips_explicitly: contains either `verify_runtime_uid` invocation or an explicit `VERIFY_RUNTIME_UID:=false` for image_dir without a UID-10001 contract (traffic-gen runs as the default python:3.12-slim user, so the script either delegates UID verification per-image or accepts an override — test asserts the override pathway exists).
    - test_script_has_no_inline_ocids_or_public_ips: greps the file for `ocid1\.[a-z]+\.oc1\.[a-z0-9-]+\.[a-z0-9]{20,}` and OCI public IP ranges (130./144./161./129.153.) and asserts zero matches (PATTERNS shared pattern F).
    - test_script_emits_image_uri_line_for_downstream_parsing: contains a printf/echo line in the form `phx.ocir.io/${OCIR_TENANCY}/<repo>:<tag>` so the runbook can capture and reuse the tag.
    Use the same ROOT/read_text helper pattern as tests/test_stress_demo_surface.py:1-27. Mark the module with `pytest.mark.unit`. Use `pathlib.Path` and `frozen` style — pytest functions only, no shared state.
  </behavior>
  <action>
    Implement the test file with the 12 assertions above. Each test reads `deploy/oke/build-push-one-image.sh` via `(ROOT / "deploy/oke/build-push-one-image.sh").read_text(encoding="utf-8")` and asserts via `assert "<token>" in text` or regex via `re.search`. Add a top-of-file docstring explaining "RED-first contract for Plan 08-04 — script does not yet exist; all tests should fail on initial commit." Run `pytest tests/test_phoenix_build_surface.py -x` and confirm tests FAIL because the file does not exist (FileNotFoundError or `read_text` raises). Commit RED state: `git add tests/test_phoenix_build_surface.py && git commit -m "test(08-04): add failing surface tests for build-push-one-image.sh"`.
  </action>
  <verify>
    <automated>python3 -c "import pathlib,sys; p=pathlib.Path('tests/test_phoenix_build_surface.py'); sys.exit(0 if p.exists() and 'build-push-one-image.sh' in p.read_text() and 'us-phoenix-1' in p.read_text() and 'ALLOW_LATEST_IMAGE_TAG' in p.read_text() and 'PLAN 08-04' in p.read_text().upper() else 1)" && cd /Users/abirzu/dev/octo-apm-demo && (pytest tests/test_phoenix_build_surface.py -x 2>&1 | grep -E '(FAILED|failed|error)' | head -1)</automated>
  </verify>
  <done>Test file exists, contains all 12 assertions referencing build-push-one-image.sh, runs RED (pytest reports failures because the target script does not yet exist), and is committed before Task 2 begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement deploy/oke/build-push-one-image.sh parameterized over (image_dir, image_name)</name>
  <files>deploy/oke/build-push-one-image.sh</files>
  <behavior>
    - Running `bash -n deploy/oke/build-push-one-image.sh` exits 0.
    - Running `pytest tests/test_phoenix_build_surface.py -x` exits 0 (all 12 RED tests now GREEN).
    - With env `OCIR_USERNAME=<placeholder>` and a stub `credentials/demo/outputs.json` containing `{"deployment_compartment_id":{"value":"<LOGANALYTICS_COMPARTMENT_OCID>"}}`, the script runs through its dry-run preview (set `DRY_RUN=true` if added, or check the help text path) without invoking docker.
    - The script's image-URI output line matches the regex `phx\.ocir\.io/\$\{?OCIR_TENANCY\}?/[a-z0-9-]+:[a-z0-9-]+` so downstream runbooks can capture it.
  </behavior>
  <action>
    Following PATTERNS analog 08-04 (lines 135-176), create the script with usage parsing: `bash deploy/oke/build-push-one-image.sh --image-dir <abs-path-on-jumphost> --image-name <repo-name>` (e.g. `--image-dir /tmp/octo-apm-demo/tools/traffic-generator --image-name octo-traffic-generator`). Reuse the helper shapes from `deploy/oke/build-push-images.sh`: copy `require_tool`, `ensure_repo` (lines 54-81), `build_push` (lines 83-103), and `verify_runtime_uid` (lines 105-136) verbatim (or `source` from the multi-image script if path-safe — the simpler path is to inline). Set defaults exactly as PATTERNS spec'd (lines 161-168): `: "${OCIR_REGION:=us-phoenix-1}"`, `: "${IMAGE_TAG:=obs-$(date -u +%Y%m%d%H%M%S)}"` (note: prefix `obs-` to match brief's tag convention), `: "${PLATFORM:=linux/amd64}"`, `: "${PUSH_LATEST:=false}"`, `: "${ALLOW_LATEST_IMAGE_TAG:=false}"`, `: "${VERIFY_RUNTIME_UID:=false}"` (default OFF — traffic-gen is python:3.12-slim base, not UID-10001-pinned; the multi-image script in Plan 08-05 will set VERIFY_RUNTIME_UID=true per-service for the UID-pinned services). The script MUST NOT contain `docker login` (handled by `deploy/oke/ocir-login.sh` per Plan 08-03). Dockerfile is assumed at `<image_dir>/Dockerfile` unless `DOCKERFILE` override is set. Print the final image URI on a dedicated line for downstream parsing: `echo "PUSHED: phx.ocir.io/${OCIR_TENANCY}/${IMAGE_NAME}:${IMAGE_TAG}"`. Run pytest GREEN; if any assertion fails, tighten the script until all 12 pass. Commit: `git add deploy/oke/build-push-one-image.sh && git commit -m "feat(08-04): single-service build-push wrapper, traffic-gen pilot"`.
  </action>
  <verify>
    <automated>bash -n deploy/oke/build-push-one-image.sh && cd /Users/abirzu/dev/octo-apm-demo && pytest tests/test_phoenix_build_surface.py -x</automated>
  </verify>
  <done>Script parses, all 12 surface tests pass GREEN, no inline OCIDs/IPs, image-URI output line is present and grep-parseable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator runs the pilot end-to-end on the jumphost (rsync → build → push → cold-pull)</name>
  <what-built>End-to-end Phoenix-native build pipeline for `octo-traffic-generator`: source rsynced to jumphost, image built native on x86_64 jumphost, pushed to `phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:obs-<ts>`, cold-pulled from an OKE worker.</what-built>
  <how-to-verify>
    1. On operator workstation: `rsync -avz --exclude '.git' --exclude '__pycache__' --exclude '.env' --exclude '*.egg-info' ./tools/traffic-generator/ opc@<JUMPHOST_PUBLIC_IP>:/tmp/octo-apm-demo/tools/traffic-generator/` and `rsync -avz ./deploy/oke/ opc@<JUMPHOST_PUBLIC_IP>:/tmp/octo-apm-demo/deploy/oke/` and `rsync -avz ./credentials/demo/outputs.json opc@<JUMPHOST_PUBLIC_IP>:/tmp/octo-apm-demo/credentials/demo/outputs.json`.
    2. SSH to jumphost (Plan 08-03 already logged docker in to phx.ocir.io).
    3. Run: `OCIR_USERNAME="${OCIR_TENANCY}/<your-user>" bash /tmp/octo-apm-demo/deploy/oke/build-push-one-image.sh --image-dir /tmp/octo-apm-demo/tools/traffic-generator --image-name octo-traffic-generator`.
    4. Confirm the script prints `PUSHED: phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:obs-<timestamp>` and exits 0.
    5. Confirm in OCI Console → Container Registry → LogAnalytics compartment → `octo-traffic-generator` shows `image_count: 1` (or more) and the new tag.
    6. **Cold-pull benchmark (BUILD-05 scaffold for this service):** pick one OKE worker, e.g. `kubectl get nodes -o wide`. Use `kubectl debug node/<name> -it --image=alpine -- sh` (or `ssh` via your bastion); inside, run `time crictl pull phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:obs-<timestamp>`. Record the wall-clock time. If a frankfurt equivalent tag exists for traffic-gen (Phase 7 may have left one), run `time crictl pull eu-frankfurt-1.ocir.io/${OCIR_TENANCY}/octo-traffic-generator:<existing-tag>` from a fresh node (image not cached); record that time as well. Write both numbers into `08-04-SUMMARY.md`. (Full BUILD-05 < 25% comparison happens in Plan 08-07.)
    7. Verify nothing leaked to bash history: `ssh opc@<JUMPHOST_PUBLIC_IP> 'tail -50 ~/.bash_history | grep -E "(password|token|ocid1)"'` returns no matches.
  </how-to-verify>
  <resume-signal>Type "pilot-shipped" with the image tag and cold-pull seconds, or describe the failure (rsync / build / push / pull).</resume-signal>
</task>

</tasks>

<verification>
- RED: `tests/test_phoenix_build_surface.py` fails before Task 2 (file missing causes FileNotFoundError on read_text).
- GREEN: `pytest tests/test_phoenix_build_surface.py -x` exits 0 after Task 2.
- `bash -n deploy/oke/build-push-one-image.sh` exits 0.
- No inline OCIDs or public IPs in either file (grep gate inside the test suite enforces this).
- Operator confirms the image appears in `phx.ocir.io/${OCIR_TENANCY}/octo-traffic-generator` with a date tag.
- Cold-pull wall-clock time recorded for at least the phoenix pull (frankfurt comparison best-effort).
</verification>

<success_criteria>
- BUILD-01: `octo-traffic-generator` repo in `phx.ocir.io/${OCIR_TENANCY}/*` has `image_count > 0`.
- BUILD-02: pipeline reproduces on jumphost via rsync → script → push, with token piped via stdin (upstream of this plan).
- BUILD-05 (partial): cold-pull benchmark methodology proven for one service; full comparison gate lands in 08-07.
- TDD discipline: RED commit precedes GREEN commit in git log for this plan.
</success_criteria>

<references>
- `08-PATTERNS.md` Plan 08-04 section (lines 135-176): build_push / ensure_repo / verify_runtime_uid analogs, default-preservation, `--password-stdin` separation of concerns.
- `08-PATTERNS.md` Plan 08-04 "Verify-on-OKE-node step" (lines 172-173): crictl pull mirroring verify_runtime_uid shape.
- `08-PATTERNS.md` "No Analog Found" section: `crictl pull` benchmark utility — new, called out as small utility for this plan.
- `08-PATTERNS.md` shared pattern F (placeholder discipline) and E (immutable-tag refusal of `latest`).
- `tests/test_stress_demo_surface.py:1-27` — ROOT + read_text test-shape analog.
- `deploy/oke/build-push-images.sh:54-176` — helper functions (require_tool, ensure_repo, build_push, verify_runtime_uid).
- `tools/traffic-generator/Dockerfile` — pilot image (python:3.12-slim, ENTRYPOINT octo-traffic).
</references>

<output>
After completion, create `.planning/phases/08-phoenix-native-build-registry-migration/08-04-SUMMARY.md` capturing: RED/GREEN commit SHAs, pushed image tag, cold-pull wall-clock seconds (phoenix + frankfurt if measured), OCI Console screenshot of `image_count` for `octo-traffic-generator` (placeholder-redacted), any toolchain gaps surfaced during the pilot.
</output>
