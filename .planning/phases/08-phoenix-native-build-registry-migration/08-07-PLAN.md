---
phase: 08-phoenix-native-build-registry-migration
plan: 07
type: execute
wave: 6
depends_on: [08-06]
files_modified:
  - deploy/oke/rolling-cutover.sh
  - tests/test_phoenix_build_surface.py
autonomous: false
requirements: [BUILD-04]
tags: [phoenix, rolling-deploy, apm-continuity, operator-gated]
must_haves:
  truths:
    - "rolling-cutover.sh executes the canary order traffic-generator → java-demo → workflow-gateway → drone-shop → crm-portal"
    - "Between each canary step, the script polls OCI APM Service Monitoring for ≥5 minutes and FAILs if any of the 8 expected services drops out for >60s"
    - "Script defaults to APPLY=false (dry-run prints kubectl set image commands without executing) per operator-gating convention"
    - "Each kubectl set image step is followed by `kubectl rollout status --timeout=10m` and ABORTs the cutover (returns non-zero) on rollout failure"
  artifacts:
    - path: "deploy/oke/rolling-cutover.sh"
      provides: "Idempotent 5-service canary cutover with embedded APM continuity probe between each step"
    - path: "tests/test_phoenix_build_surface.py"
      provides: "Extended surface assertions for the cutover script (canary order, APM probe presence, operator gate)"
  key_links:
    - from: "deploy/oke/rolling-cutover.sh"
      to: "deployment/<svc> in cluster"
      via: "kubectl set image deployment/<svc> <container>=phx.ocir.io/${OCIR_TENANCY}/<repo>:${IMAGE_TAG}"
    - from: "deploy/oke/rolling-cutover.sh"
      to: "OCI APM Service Monitoring API"
      via: "oci apm-synthetics ... + oci monitoring metric-data summarize-metrics-data (polled every 30s for 5 min)"
    - from: "tests/test_phoenix_build_surface.py"
      to: "deploy/oke/rolling-cutover.sh"
      via: "pathlib read + content assertions"
---

<objective>
Ship the rolling-cutover script that flips `octo-apm-demo-oke` from frankfurt-pulled images to
phx-pulled images one service at a time, with APM signal continuity verified between every step.
The script is the BUILD-04 deliverable: canary order + maxUnavailable=0 (already in the deployment
manifests) + ≤60s service gap window.

Operator-gated (`autonomous: false`) because the script mutates the live cluster. Default is
dry-run; live apply requires `APPLY=true` and an interactive confirmation phrase.

Security: per `.planning/SECURITY.md`, the script never inlines OCIDs or worker IPs. APM domain ID,
namespace, and compartment OCID are sourced from `credentials/demo/outputs.json` (gitignored) or
env vars. The runbook in 08-08 documents the operator's pre-flight checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/SECURITY.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-PATTERNS.md
@deploy/oke/build-push-images.sh
@deploy/k8s/oke/shop/deployment.yaml
@tools/monitoring-alarms/apply.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write rolling-cutover.sh with embedded APM continuity probe</name>
  <files>deploy/oke/rolling-cutover.sh</files>
  <action>Create the cutover script. Header + usage follows
  `deploy/oke/bootstrap-demo-secrets.sh:1-31` (08-PATTERNS.md). Operator gate follows pattern A
  in 08-PATTERNS.md (`tools/monitoring-alarms/apply.sh:44-46, 105-121`):
  - `: "${APPLY:=false}"` default.
  - `: "${OCI_PROFILE:=demo}"`.
  - `: "${OCIR_REGION:=us-phoenix-1}"` — refuse to run unless this equals `us-phoenix-1` UNLESS
    `--allow-non-phx` is passed (rollback mode hint).
  - `: "${IMAGE_TAG:?IMAGE_TAG is required — set to the obs-YYYYMMDDhhmmss tag printed by build-push-all-images.sh}"`.
  - `: "${OCIR_TENANCY:?OCIR_TENANCY is required}"`.
  - `: "${APM_DOMAIN_ID:?APM_DOMAIN_ID required — source from credentials/demo/outputs.json}"`.
  - `: "${COMPARTMENT_ID:=$(jq -r .deployment_compartment_id.value credentials/demo/outputs.json)}"`.

  Canary order (BUILD-04 — exact sequence locked):
  1. `octo-traffic-generator` → deployment `octo-traffic-generator` namespace `octo-traffic` container `traffic`
  2. `octo-apm-java-demo` → deployment `octo-java-app-server-oke` namespace `octo-drone-shop` container `octo-apm-java-demo`
  3. `octo-workflow-gateway` → deployment `octo-workflow-gateway-oke` namespace `octo-drone-shop` container `octo-workflow-gateway`
  4. `octo-drone-shop` → deployment `octo-drone-shop` namespace `octo-drone-shop` container `octo-drone-shop`
  5. `enterprise-crm-portal` → deployment `enterprise-crm-portal` namespace `enterprise-crm` container `enterprise-crm-portal`

  Note: the executor MUST verify the actual deployment name + container name per service before
  hardcoding (use `kubectl get deploy -A -o wide` against a current cluster). If a name differs
  from the list above, update both the script and the SUMMARY — do not silently mismatch.

  For each canary step:
  ```bash
  cutover_one() {
      local svc="$1" deploy="$2" ns="$3" container="$4" repo="$5"
      local image="${OCIR_REGION}.ocir.io/${OCIR_TENANCY}/${repo}:${IMAGE_TAG}"
      echo "── Canary: ${svc} ─────────────────────────────────────"
      if [[ "${APPLY}" != "true" ]]; then
          echo "DRY-RUN: would run: kubectl -n ${ns} set image deployment/${deploy} ${container}=${image}"
          return
      fi
      kubectl -n "${ns}" set image "deployment/${deploy}" "${container}=${image}"
      kubectl -n "${ns}" rollout status "deployment/${deploy}" --timeout=10m
      probe_apm_continuity "${svc}" 300 30  # window_seconds=300, interval_seconds=30
  }
  ```

  APM continuity probe (`probe_apm_continuity`) — heart of BUILD-04:
  - Inputs: just-cut-over service name, window seconds (default 300 = 5 min), poll interval
    (default 30s).
  - Expected service set (per 08-CONTEXT.md success criteria #4, "all 8 services healthy"):
    `octo-drone-shop`, `octo-apm-java-demo`, `octo-workflow-gateway`, `enterprise-crm-portal`,
    `octo-traffic-generator`, `octo-java-app-server-oke`, `octo-frontend-ingress`,
    `octo-stress-runner`. (Executor verifies the exact 8 names against the live APM dashboard
    before hardcoding — adjust if APM uses different labels in this tenancy.)
  - Implementation: use `oci apm-traces query` or `oci monitoring metric-data summarize-metrics-data`
    (the project already uses oci-cli for APM in `tools/apm-saved-queries/apply.sh`). For each
    poll, request the last 60 seconds of "service.health" or equivalent metric for the 8 services.
    Track the most recent "seen" timestamp per service. If any service's last-seen lag exceeds
    60 seconds at any point in the window, FAIL the probe (exit non-zero — cutover aborts).
  - The exact OCI CLI invocation depends on the APM SDK shape; the executor should:
    a) First check if there is an existing helper in `tools/apm-saved-queries/` or
       `tools/monitoring-alarms/` that already polls APM — reuse it.
    b) If not, write a minimal `query_apm_service_seen` helper inside this same script using
       `oci monitoring metric-data summarize-metrics-data --compartment-id "${COMPARTMENT_ID}"
       --namespace oci_apm --query-text 'SystemMetrics[1m]{ServiceName="<svc>"}.mean()' --start-time
       "$(date -u -d '60 seconds ago' +%FT%TZ)" --end-time "$(date -u +%FT%TZ)" --profile "${OCI_PROFILE}"`.
       Empty result for a service → that service is silent → count toward the 60s gap budget.
    c) Print a one-line status per poll: `[+60s] traffic-gen ✓ java-demo ✓ workflow ✓ shop ✓ crm ✓
       (5/8 reporting)` so the operator sees the probe live.
  - On FAIL, the probe MUST `set -e` propagate so the outer script aborts WITHOUT running the next
    canary step. Print a recovery hint: "APM continuity broken on <svc>. Run: kubectl -n <ns>
    rollout undo deployment/<deploy>".

  Idempotency:
  - Before each `kubectl set image`, read the current image with
    `kubectl -n "${ns}" get deploy "${deploy}" -o jsonpath='{.spec.template.spec.containers[?(@.name=="'"${container}"'")].image}'`.
    If it already equals the target image, skip with `echo "  ${svc}: already at target image, skipping."`.

  Helm rollback escape hatch (BUILD-06 hook — fully exercised in 08-08):
  - At the very end of the script, print the helm history command operator should record for
    rollback: `helm -n octo history octo-apm-demo` and quote the pre-cutover revision number into
    the SUMMARY. This is documentation-only; the actual rollback drill happens in 08-08.

  No tenancy / OCID / IP inlined. The script reads `COMPARTMENT_ID` + `APM_DOMAIN_ID` from
  `credentials/demo/outputs.json` or env. Make the file executable (`chmod +x`).</action>
  <verify>
    <automated>bash -n deploy/oke/rolling-cutover.sh &amp;&amp; deploy/oke/rolling-cutover.sh --help | grep -q "canary" &amp;&amp; APPLY=false IMAGE_TAG=obs-test OCIR_TENANCY=PLACEHOLDER APM_DOMAIN_ID=PLACEHOLDER COMPARTMENT_ID=PLACEHOLDER deploy/oke/rolling-cutover.sh 2>&amp;1 | grep -q "DRY-RUN"</automated>
  </verify>
  <done>Script exists, executable, dry-run produces 5 canary preview blocks in the locked order, no
  live cluster mutation occurs without APPLY=true. Manual smoke against live cluster: `APPLY=true
  IMAGE_TAG=obs-<recent> ./deploy/oke/rolling-cutover.sh` cuts over all 5 services with no APM gap
  &gt;60s.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test_phoenix_build_surface.py with cutover script assertions</name>
  <files>tests/test_phoenix_build_surface.py</files>
  <action>Append assertions to the surface-test module created in 08-05. Same file, additional
  tests — keep granular (one function per assertion).

  Required new tests:
  - `test_rolling_cutover_script_exists`: file `deploy/oke/rolling-cutover.sh` exists and is
    executable.
  - `test_rolling_cutover_canary_order_locked`: file content contains the five service names in
    the BUILD-04 canary order. Implementation: read text, find the index of each name
    (`octo-traffic-generator`, `octo-apm-java-demo`, `octo-workflow-gateway`, `octo-drone-shop`,
    `enterprise-crm-portal`), assert each index > the previous. This is order-sensitive — guards
    against a future drive-by edit that reshuffles the canary.
  - `test_rolling_cutover_has_apm_continuity_probe`: content contains either
    `"probe_apm_continuity"` or `"query_apm_service_seen"` AND contains the literal `"60"` near
    the word `"gap"` or `"lag"` (the 60s SLO from BUILD-04). Implementation: grep for the function
    name + a line-window match.
  - `test_rolling_cutover_apply_gated`: content contains `"APPLY"`, contains
    `"Type 'APPLY' to confirm"` literal, and contains `'${APPLY:=false}'` (the default).
  - `test_rolling_cutover_uses_kubectl_set_image_not_apply`: content contains `"kubectl"` and
    `"set image"` but does NOT contain `"kubectl apply"` (BUILD-04 demands per-deployment image
    swap, not whole-manifest apply — a kubectl apply would risk reverting in-cluster autoscaler
    state set by Phase 7).
  - `test_rolling_cutover_uses_rollout_status`: content contains `"rollout status"` and
    `"--timeout="` (mandatory wait between canary steps).
  - `test_rolling_cutover_refuses_non_phx_by_default`: content contains both `"us-phoenix-1"` and
    `"--allow-non-phx"` (the rollback escape hatch).

  All assertions are pathlib + string ops — no subprocess, no live OCI calls.</action>
  <verify>
    <automated>pytest tests/test_phoenix_build_surface.py -v -k "rolling_cutover"</automated>
  </verify>
  <done>All seven new tests pass (combined with the seven from 08-05, the module now has 14
  PASSED). No real OCIDs / IPs in the test file (`grep -E 'ocid1\.|130\.|161\.|144\.' tests/test_phoenix_build_surface.py`
  returns no matches).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → OKE control plane | kubectl set image mutates live workloads — credential exposure risk if KUBECONFIG leaks |
| operator → OCI APM API | APM query needs read access to the LogAnalytics compartment metrics |
| OKE pod → phx OCIR | image pull crosses cluster boundary; pull secret must be present in target namespace (already seeded by Phase 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | rolling-cutover.sh | mitigate | APPLY=false default + interactive confirm phrase prevents accidental cutover. Script refuses if `OCIR_REGION` != `us-phoenix-1` without explicit override. |
| T-08-07-02 | Denial of service | live cluster | mitigate | maxUnavailable=0 in all 5 deployments (already in manifests), `kubectl rollout status --timeout=10m` gates each step, APM continuity probe aborts on >60s gap. |
| T-08-07-03 | Information disclosure | script logs | mitigate | Script reads `APM_DOMAIN_ID`, `COMPARTMENT_ID`, `OCIR_TENANCY` from env / outputs.json — never inlines. Logs print `${OCIR_TENANCY}` as a literal env-var name, not its resolved value. |
| T-08-07-04 | Repudiation | post-cutover audit | mitigate | Script prints `helm history octo-apm-demo` revision number at the end, operator copies it into the SUMMARY — establishes the rollback target. |
| T-08-07-05 | Spoofing | OCI auth | accept | Operator's OCI CLI profile (`demo`) is the same one used by every other operator script in this repo; risk is no higher than existing baseline. |
</threat_model>

<verification>
Manual verification (operator runs after build-push-all-images.sh succeeds):
1. Dry-run: `APPLY=false IMAGE_TAG=obs-<tag> OCIR_TENANCY=PLACEHOLDER APM_DOMAIN_ID=PLACEHOLDER
   COMPARTMENT_ID=PLACEHOLDER deploy/oke/rolling-cutover.sh` — prints 5 canary previews in locked
   order, exits 0, no kubectl mutations.
2. Live: `APPLY=true IMAGE_TAG=obs-<tag> ...` — cuts over all 5 services. Watch for:
   - All 5 deployments report `successfully rolled out` from `kubectl rollout status`.
   - APM probe between each step prints a 5-min trail of "8/8 reporting" lines, no FAIL.
   - Final output: `helm history octo-apm-demo` revision number captured.
3. Post-cutover audit:
   `kubectl get pods -A -o jsonpath='{.items[*].spec.containers[*].image}' | tr ' ' '\n' \
     | grep -c eu-frankfurt-1.ocir.io` MUST return 0 (success criterion #1 from 08-CONTEXT.md).
4. `pytest tests/test_phoenix_build_surface.py -v` — all 14 tests pass.
</verification>

<success_criteria>
- BUILD-04 closed: 5-service canary cutover script exists, APM continuity probe in place, ≤60s gap
  enforced.
- `deploy/oke/rolling-cutover.sh` is executable, operator-gated, idempotent.
- Surface tests guard the script's structural invariants.
- Helm history revision number captured for the BUILD-06 rollback drill in 08-08.
- No frankfurt OCIR references in any cluster image after live cutover.
</success_criteria>

<output>
After completion, create `.planning/phases/08-phoenix-native-build-registry-migration/08-07-SUMMARY.md`
with: pre-cutover helm revision, dry-run transcript, live cutover transcript (with APM probe
output per step), post-cutover image audit (kubectl get pods | grep -c eu-frankfurt-1 = 0),
requirements closed.
</output>
