---
phase: 08-phoenix-native-build-registry-migration
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - deploy/oke/jumphost-bootstrap.sh
autonomous: false
requirements: [BUILD-02]
tags: [phoenix, jumphost, dnf, docker, oci-cli, bootstrap]
must_haves:
  truths:
    - "Running `deploy/oke/jumphost-bootstrap.sh --install` with APPLY=true installs missing tools on octo-demo-jumphost-v5 via `sudo dnf install -y`, but installs nothing that is already present (idempotent — verify-then-install)."
    - "After install completes, the jumphost has: docker (or podman+buildah), oci-cli, git, rsync, jq, helm — each verified by `command -v` + a `--version` probe."
    - "Docker daemon is configured and started (`sudo systemctl enable --now docker`); `docker info` exits 0; current user `opc` is in the `docker` group (logged-out warning printed if newly added)."
    - "Script defaults to APPLY=false and refuses to mutate without both `--install` flag and `APPLY=true` env var (two-step opt-in)."
  artifacts:
    - path: "deploy/oke/jumphost-bootstrap.sh"
      provides: "Extended with --install mode (idempotent dnf installs, docker daemon enable, group membership setup). The inventory-only mode from 08-01 remains the default."
  key_links:
    - from: "deploy/oke/jumphost-bootstrap.sh --install (APPLY=true)"
      to: "/etc/yum.repos.d/docker-ce.repo + dnf install docker-ce"
      via: "ssh opc@<JUMPHOST_PUBLIC_IP> 'sudo dnf install -y <pkg>'"
    - from: "deploy/oke/jumphost-bootstrap.sh --install (APPLY=true)"
      to: "docker daemon"
      via: "sudo systemctl enable --now docker && sudo usermod -aG docker opc"
---

<objective>

Extend `deploy/oke/jumphost-bootstrap.sh` (created in Plan 08-01) with an `--install` mode that idempotently installs the build toolchain (docker / oci-cli / git / rsync / jq / helm) on `octo-demo-jumphost-v5` via `sudo dnf install -y` only when each tool is missing. Configure the docker daemon to start on boot and add the `opc` user to the `docker` group.

This plan addresses **BUILD-02** (reproducible build pipeline on Phoenix jumphost) by making the toolchain installation reproducible and operator-gated. Per `<security_rules>` and PATTERNS shared pattern A, the script defaults to dry-run and requires explicit `APPLY=true` to mutate. The jumphost may be shared (per 08-CONTEXT.md risk-surface row 1), so every install MUST be guarded by `command -v <tool>` first — do not blindly reinstall.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/SECURITY.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-PATTERNS.md
@deploy/oke/bootstrap-demo-secrets.sh
@deploy/oke/build-push-images.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --install mode + idempotent dnf installer to deploy/oke/jumphost-bootstrap.sh</name>
  <files>deploy/oke/jumphost-bootstrap.sh</files>
  <action>
    Extend the script from Plan 08-01 (do not rewrite — append/modify). Parse `--install` flag in the existing arg-handling `case` block. When `--install` is set, require `APPLY=true` (PATTERNS shared pattern A — operator-gated mutation). If APPLY≠true, print a dry-run summary of which tools are missing and exit 0 with the line "DRY-RUN: would install: <list>. Re-run with APPLY=true to mutate." (mirrors `tools/apm-saved-queries/apply.sh:84-87`). Define `ensure_pkg()` taking `(probe_cmd, dnf_pkg_name)` — runs `ssh opc@${JUMPHOST_PUBLIC_IP} "command -v ${probe_cmd}"` and only installs if the probe fails (invert-polarity analog of `require_tool` at `deploy/oke/build-push-images.sh:54-59`, per PATTERNS Plan 08-02 guidance). For docker specifically: also enable the daemon with `sudo systemctl enable --now docker` and run `sudo usermod -aG docker opc`; print a clear warning if the user was newly added ("opc must log out + back in for the docker group to take effect"). Cover the full set: `docker`/`docker-ce`, `oci-cli` (probe `oci`), `git`, `rsync`, `jq`, `helm`. The interactive confirm prompt (`read -r -p "Type 'APPLY' to confirm: " confirm`) MUST appear before any `sudo dnf` runs (analog `tools/apm-saved-queries/apply.sh:92-97`). End with "Done. Installed tools were the only mutation; no secret values were printed." (analog `deploy/oke/bootstrap-demo-secrets.sh:275`). Never write to `~/.bash_history` on the jumphost — all install commands run via single `ssh ... 'sudo dnf ...'` invocations (history stays on operator workstation, where the operator unsets HISTFILE per PATTERNS shared pattern D).
  </action>
  <verify>
    <automated>bash -n deploy/oke/jumphost-bootstrap.sh && grep -c '\-\-install' deploy/oke/jumphost-bootstrap.sh | awk '$1>=2' | grep -q . && grep -c 'APPLY:=false' deploy/oke/jumphost-bootstrap.sh | grep -q '^1$' && grep -q "Type 'APPLY' to confirm" deploy/oke/jumphost-bootstrap.sh && grep -q 'command -v docker' deploy/oke/jumphost-bootstrap.sh && grep -q 'sudo systemctl enable --now docker' deploy/oke/jumphost-bootstrap.sh && ! grep -E 'ocid1\.[a-z]+\.oc1\.[a-z0-9-]+\.[a-z0-9]{20,}' deploy/oke/jumphost-bootstrap.sh && ! grep -E '(130\.|144\.|161\.|129\.153\.)[0-9]+\.[0-9]+\.[0-9]+' deploy/oke/jumphost-bootstrap.sh</automated>
  </verify>
  <done>Script accepts `--install`, requires `APPLY=true`, includes interactive confirm phrase, guards every install behind `command -v`, configures docker daemon + group, contains no inline secrets/IPs/OCIDs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs --install on the jumphost and confirms toolchain is ready</name>
  <what-built>Idempotent installer for docker, oci-cli, git, rsync, jq, helm on `octo-demo-jumphost-v5`, guarded by APPLY=true + interactive confirm.</what-built>
  <how-to-verify>
    1. Dry-run first: `bash deploy/oke/jumphost-bootstrap.sh --install` (APPLY unset). Expect: list of missing tools printed, exit 0, no mutation.
    2. Then mutate: `APPLY=true bash deploy/oke/jumphost-bootstrap.sh --install`. At the "Type 'APPLY' to confirm:" prompt, type `APPLY`.
    3. Wait for installs to complete (5-15 min depending on what was missing).
    4. Verify post-install: `ssh opc@<JUMPHOST_PUBLIC_IP> 'docker --version; oci --version; git --version; jq --version; helm version --short; rsync --version | head -1'`. Each MUST exit 0.
    5. Verify docker daemon: `ssh opc@<JUMPHOST_PUBLIC_IP> 'sudo systemctl is-active docker'` returns `active`.
    6. If `opc` was newly added to the `docker` group, log out + log back in (or `newgrp docker`) and verify `ssh opc@<JUMPHOST_PUBLIC_IP> 'docker info'` exits 0 without `sudo`.
  </how-to-verify>
  <resume-signal>Type "toolchain-ready" if all six tools verified + docker daemon active, or describe the failure (which tool, which step).</resume-signal>
</task>

</tasks>

<verification>
- `bash -n deploy/oke/jumphost-bootstrap.sh` exits 0.
- Dry-run mode does not mutate jumphost.
- Operator confirms all 6 tools are present and docker daemon is active.
- No live IPs / OCIDs in the committed script.
</verification>

<success_criteria>
- `docker`, `oci`, `git`, `rsync`, `jq`, `helm` all installed and version-probed on jumphost.
- Docker daemon enabled on boot; `opc` in `docker` group.
- Script remains idempotent — second run reports "nothing to install".
- BUILD-02 build host is toolchain-ready (next plan handles auth).
</success_criteria>

<references>
- `08-PATTERNS.md` Plan 08-02 section (lines 44-85): header/usage analog, `require_tool` polarity inversion, idempotent upsert.
- `08-PATTERNS.md` shared pattern A (operator-gated APPLY), D (no-secret-printing).
- `tools/apm-saved-queries/apply.sh:84-97` — dry-run + confirm-phrase analog.
- `deploy/oke/bootstrap-demo-secrets.sh:275` — closing "no secrets printed" line.
</references>

<output>
After completion, create `.planning/phases/08-phoenix-native-build-registry-migration/08-02-SUMMARY.md` capturing: per-tool install outcome (already-present vs newly-installed), docker daemon status, whether opc group change required re-login.
</output>
