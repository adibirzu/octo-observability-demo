---
phase: 08-phoenix-native-build-registry-migration
plan: 05
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - deploy/oke/build-push-all-images.sh
  - deploy/oke/build-push-images.sh
  - tests/test_phoenix_build_surface.py
autonomous: true
requirements: [BUILD-01, BUILD-02]
tags: [phoenix, ocir, build, rollout]
must_haves:
  truths:
    - "All four phx OCIR repos (octo-drone-shop, octo-apm-java-demo, octo-workflow-gateway, enterprise-crm-portal) report image_count > 0 after this plan runs"
    - "Each push uses an immutable date tag (obs-YYYYMMDDhhmmss) AND a :latest alias gated by PUSH_LATEST=true"
    - "Re-running build-push-all-images.sh is idempotent — ensure_repo skips existing repos, build_push retags without error"
    - "Auth token never appears in build logs or shell history (verified by grep audit step in script)"
  artifacts:
    - path: "deploy/oke/build-push-all-images.sh"
      provides: "wrapper that loops the 4 application repos via build-push-one-image.sh from 08-04, prints a summary table"
    - path: "tests/test_phoenix_build_surface.py"
      provides: "pytest assertions that the wrapper exists, is executable, and references all 4 repos by exact name"
  key_links:
    - from: "deploy/oke/build-push-all-images.sh"
      to: "deploy/oke/build-push-one-image.sh"
      via: "for entry in services; do ./build-push-one-image.sh --image-dir \"$dir\" --image-name \"$name\" done — service→(dir,name) lookup table matches 08-04 CLI contract exactly"
    - from: "deploy/oke/build-push-all-images.sh"
      to: "phx.ocir.io/${OCIR_TENANCY}/<repo>"
      via: "docker buildx ... --push (inherited from per-image script)"
    - from: "tests/test_phoenix_build_surface.py"
      to: "deploy/oke/build-push-all-images.sh"
      via: "pathlib read + content assertions"
---

<objective>
Roll the Phoenix-native build pipeline (proven in 08-04 with `octo-traffic-generator`) across the four
remaining application services: `octo-drone-shop`, `octo-apm-java-demo`, `octo-workflow-gateway`,
`enterprise-crm-portal`. Output is a single wrapper script `deploy/oke/build-push-all-images.sh` that
calls `build-push-one-image.sh` (from 08-04) once per service, plus a surface test that prevents
regression of the four-repo set.

Closes BUILD-01 (image_count > 0 in all phx repos) and BUILD-02 (reproducible, idempotent pipeline
that authenticates via Auth Token, builds linux/amd64, and tags immutably). Frankfurt repos remain
untouched per the ≥7-day fallback window.

Security: per `.planning/SECURITY.md`, the wrapper never inlines `${OCIR_TENANCY}` or real OCIDs.
All registry coordinates come from env vars set by `deploy/oke/ocir-login.sh` (08-03) plus
`credentials/demo/outputs.json` (gitignored). The wrapper enforces `--password-stdin` semantics
inherited from 08-03 — it MUST NOT call `docker login` itself.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/SECURITY.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-PATTERNS.md
@deploy/oke/build-push-images.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write build-push-all-images.sh wrapper</name>
  <files>deploy/oke/build-push-all-images.sh</files>
  <action>Create a new shell wrapper that loops over the four application repos and invokes
  `deploy/oke/build-push-one-image.sh --image-dir <dir> --image-name <name>` for each. The per-service
  entrypoint is produced by 08-04 and accepts `--image-dir` + `--image-name` (NOT a `--service`
  selector — verify 08-04's actual CLI before writing this wrapper). Header + usage pattern follows
  `deploy/oke/bootstrap-demo-secrets.sh:1-31` per 08-PATTERNS.md (`set -euo pipefail`, `usage()`,
  `case "${1:-}"` dispatch).

  Required behavior (per BUILD-01, BUILD-02):
  - Service→(image_dir, image_name) lookup table, hardcoded in this order:
    - `octo-drone-shop` → image_dir=`shop`, image_name=`octo-drone-shop`
    - `octo-apm-java-demo` → image_dir=`services/apm-java-demo`, image_name=`octo-apm-java-demo`
    - `octo-workflow-gateway` → image_dir=`services/workflow-gateway`, image_name=`octo-workflow-gateway`
    - `enterprise-crm-portal` → image_dir=`crm`, image_name=`enterprise-crm-portal`
    Verify these paths against the repo layout at execute time; adjust if 08-04 settled on different
    canonical dirs.
  - Do NOT add traffic-generator (already shipped in 08-04).
  - Inherit `OCIR_REGION=us-phoenix-1` default from `build-push-images.sh:43`. Refuse to run if
    `OCIR_REGION` is overridden to anything other than `us-phoenix-1` UNLESS the operator passes
    `--allow-non-phx` (rollback escape hatch for the 7-day soak window).
  - Compute one immutable `IMAGE_TAG="obs-$(date -u +%Y%m%d%H%M%S)"` at the top of the wrapper so
    ALL four images get the same date tag in a single invocation. Export it so the per-service
    script reuses it (do not let each per-service invocation roll its own timestamp).
  - Refuse `IMAGE_TAG=latest` unless `ALLOW_LATEST_IMAGE_TAG=true` (mirror
    `build-push-images.sh:153-156`, pattern E in 08-PATTERNS.md).
  - Pre-flight checks before the loop: `require_tool oci`, `require_tool jq`, `require_tool docker`
    (or `podman` if `BUILDER=podman`). Verify `docker info >/dev/null 2>&1` succeeds — fail loudly
    if not logged in to phx OCIR (08-03 is a prerequisite).
  - Compartment scoping pattern C (08-PATTERNS.md): read `COMPARTMENT_ID` from
    `credentials/demo/outputs.json` via jq. Pass `--compartment-id "${COMPARTMENT_ID}"` to any
    `oci artifacts container repository ...` calls inside (delegated to per-service script).
  - Print a summary table at the end with one row per repo:
    `| repo | image_count_before | image_count_after | head_tag |`
    Use `oci artifacts container image list --compartment-id ... --repository-name <repo>` to count
    before and after. Use the placeholder `${OCIR_TENANCY}` in any echoed registry path — never
    inline.
  - Audit hygiene: final line MUST be `echo "Done. OCIR auth token was not printed."` per pattern D
    in 08-PATTERNS.md. Add a trailing `grep -E 'OCIR_AUTH_TOKEN|--password ' ~/.bash_history` audit
    hint in the usage block (comment-only, do not execute).

  Operator-gate (per `.planning/SECURITY.md`): the wrapper IS itself a mutator (pushes images to
  OCIR + auto-creates repos via `ensure_repo`). Apply pattern A from 08-PATTERNS.md: `APPLY=false`
  default with dry-run preview ("Would build and push N images to phx.ocir.io/${OCIR_TENANCY}/*"),
  then `read -r -p "Type 'APPLY' to confirm: " confirm`. APPLY=true skips the prompt for CI use.

  Make the file executable (`chmod +x` after Write). Do NOT use `--no-cache` by default — leave
  buildx layer cache on for speed; expose `BUILDX_NO_CACHE=true` env opt-in for clean rebuilds.

  This task implements per BUILD-01 and BUILD-02. Do not regress the `OCIR_REGION=us-phoenix-1`
  default in `build-push-images.sh:43` — that default is the source of truth (BUILD-03).</action>
  <verify>
    <automated>bash -n deploy/oke/build-push-all-images.sh &amp;&amp; deploy/oke/build-push-all-images.sh --help | grep -q "octo-drone-shop" &amp;&amp; deploy/oke/build-push-all-images.sh --help | grep -q "enterprise-crm-portal"</automated>
  </verify>
  <done>Wrapper script exists, is executable, lists all four target repos in --help output, refuses
  to run without APPLY=true (dry-run print only), refuses `OCIR_REGION!=us-phoenix-1` without
  `--allow-non-phx`. Manual smoke: `APPLY=false ./deploy/oke/build-push-all-images.sh` prints a
  4-row dry-run table and exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the four-repo surface in tests/test_phoenix_build_surface.py</name>
  <files>tests/test_phoenix_build_surface.py</files>
  <action>Create a new pytest module that asserts the structural shape of the new wrapper. This file
  is created in this plan and extended in 08-07 (rolling-cutover script assertion). Follow
  `tests/test_unified_deploy_surface.py` patterns: top-of-file `ROOT = Path(__file__).resolve().parents[1]`,
  `read_text` helper, one assertion per test, pytest-mark.unit on each.

  Required assertions:
  - `test_build_push_all_images_script_exists`: file `deploy/oke/build-push-all-images.sh` exists
    and `os.access(path, os.X_OK)` returns True.
  - `test_build_push_all_images_lists_four_application_repos`: file content contains all four
    exact strings: `"octo-drone-shop"`, `"octo-apm-java-demo"`, `"octo-workflow-gateway"`,
    `"enterprise-crm-portal"`. Asserts `"octo-traffic-generator"` is NOT in the file (that one
    belongs to 08-04, not 08-05) — prevents a future drive-by edit from duplicating the 08-04
    pilot.
  - `test_build_push_all_images_defaults_to_phoenix`: content contains `"us-phoenix-1"` and does
    NOT contain `"eu-frankfurt-1"` (region default flip per BUILD-03 source-of-truth).
  - `test_build_push_all_images_uses_password_stdin_or_delegates`: content contains either
    `"--password-stdin"` literal OR a comment referencing `ocir-login.sh` (the script must NOT
    embed `docker login -p` — pattern from 08-03, enforced by 08-PATTERNS.md plan 08-03 section).
    The test rejects any occurrence of the substring `"docker login -p"` or `"--password "`
    (with trailing space, distinguishing from `--password-stdin`).
  - `test_build_push_all_images_refuses_latest_by_default`: content contains
    `"ALLOW_LATEST_IMAGE_TAG"` (the gate name from `build-push-images.sh:50-52`).
  - `test_build_push_all_images_uses_immutable_date_tag`: content contains the pattern
    `"obs-"` and `"date -u +%Y%m%d%H%M%S"` (immutable tag convention from 08-CONTEXT.md
    "Locked decisions").
  - `test_build_push_all_images_apply_gate_present`: content contains `"APPLY"` and
    `"Type 'APPLY' to confirm"` (pattern A from 08-PATTERNS.md).

  All tests use plain pathlib + string assertions — NO subprocess, NO live OCI calls. Run cost is
  millisecond-scale so they belong in the default pytest selection (no marker filter needed). Add
  module-level docstring citing BUILD-01, BUILD-02 and `.planning/SECURITY.md`.

  Do NOT inline real OCIDs, IPs, or tenancy namespace strings anywhere in this file. Placeholders
  only.</action>
  <verify>
    <automated>pytest tests/test_phoenix_build_surface.py -v</automated>
  </verify>
  <done>All seven assertions pass. `pytest tests/test_phoenix_build_surface.py -v` shows 7 PASSED.
  `grep -E 'ocid1\.|130\.|161\.|144\.|129\.|10\.42\.' tests/test_phoenix_build_surface.py` returns
  no matches (no leaked tenancy data).</done>
</task>

</tasks>

<verification>
End-to-end smoke (operator runs on jumphost after 08-04):
1. `cd /tmp/octo-apm-demo &amp;&amp; APPLY=false ./deploy/oke/build-push-all-images.sh` — must print dry-run
   summary, exit 0, NOT push.
2. `APPLY=true ./deploy/oke/build-push-all-images.sh` — must build and push 4 images, end with the
   "OCIR auth token was not printed" line.
3. Image count audit (per BUILD-01):
   `for r in octo-drone-shop octo-apm-java-demo octo-workflow-gateway enterprise-crm-portal; do
      oci artifacts container image list --profile demo \
        --compartment-id "$(jq -r .deployment_compartment_id.value credentials/demo/outputs.json)" \
        --repository-name "$r" --query 'data.items[] | length(@)' --raw-output;
    done` — every row MUST be ≥ 1.
4. Bash history hygiene: `grep -E '(OCIR_AUTH_TOKEN|--password )' ~/.bash_history` MUST return no
   matches.
5. `pytest tests/test_phoenix_build_surface.py -v` — 7 PASSED.
</verification>

<success_criteria>
- BUILD-01: all four phx OCIR repos image_count ≥ 1 after wrapper runs (manual oci CLI audit).
- BUILD-02: wrapper is idempotent (second run completes without error, just re-tags).
- `deploy/oke/build-push-all-images.sh` is executable and present in git.
- `tests/test_phoenix_build_surface.py` passes.
- No frankfurt OCIR string in either new file.
- No real OCID / IP / tenancy namespace inlined in any new file.
</success_criteria>

<output>
After completion, create `.planning/phases/08-phoenix-native-build-registry-migration/08-05-SUMMARY.md`
with: image_count before/after table (placeholders only for tenancy), wrapper invocation transcript
(with token redacted), audit-step output, requirements closed.
</output>
