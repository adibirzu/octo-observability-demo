---
phase: 08-phoenix-native-build-registry-migration
plan: 06
type: execute
wave: 5
depends_on: [08-05]
files_modified:
  - deploy/helm/octo-apm-demo/values.yaml
  - deploy/k8s/shop/secret-provider-class.yaml
  - tests/test_unified_deploy_surface.py
autonomous: true
requirements: [BUILD-03]
tags: [phoenix, ocir, helm, manifest-repoint]
must_haves:
  truths:
    - "deploy/helm/octo-apm-demo/values.yaml has global.ociRegion: us-phoenix-1 AND global.image.region: us-phoenix-1 as defaults"
    - "helm template ./deploy/helm/octo-apm-demo with default values renders zero references to eu-frankfurt-1.ocir.io"
    - "Operator override remains functional: OCIR_REGION=eu-frankfurt-1 envsubst < deploy/k8s/oke/shop/deployment.yaml | kubectl diff -f - still works for rollback"
    - "test_unified_deploy_surface.py has a new assertion that fails CI if any deploy/ file regresses to eu-frankfurt-1.ocir.io"
  artifacts:
    - path: "deploy/helm/octo-apm-demo/values.yaml"
      provides: "Helm defaults flipped from eu-frankfurt-1 to us-phoenix-1 (two lines: ociRegion + image.region)"
    - path: "deploy/k8s/shop/secret-provider-class.yaml"
      provides: "Comment-only update of the region example string (no functional change)"
    - path: "tests/test_unified_deploy_surface.py"
      provides: "New grep-gate assertion blocking frankfurt OCIR regression"
  key_links:
    - from: "deploy/helm/octo-apm-demo/values.yaml"
      to: "deploy/helm/octo-apm-demo/templates/_helpers.tpl (octo.image)"
      via: "the helper reads global.image.region as default unless component overrides it — no template edit needed"
    - from: "tests/test_unified_deploy_surface.py"
      to: "deploy/ (whole subtree)"
      via: "recursive read + grep gate (filtered to skip comments? — see Task 2 for the resolution)"
---

<objective>
Atomic source-of-truth flip: switch the Helm chart defaults from `eu-frankfurt-1` to `us-phoenix-1`
so that running `helm template` (and by extension `helm install/upgrade`) with default values
points every workload at `phx.ocir.io/${OCIR_TENANCY}/*`. Frankfurt rollback remains available via
the documented operator override (`OCIR_REGION=eu-frankfurt-1`) and via `helm rollback` to a
pre-cutover revision.

Closes BUILD-03. Pre-deploy gate is shipped as a pytest assertion (not a shell pre-commit) so it
runs in CI and on every local pytest invocation. The 08-PATTERNS.md analysis confirms only TWO
files need an actual value edit: `deploy/helm/octo-apm-demo/values.yaml` (lines 18 + 29) and
`deploy/k8s/shop/secret-provider-class.yaml:45` (comment-only). The raw `deploy/k8s/oke/*/deployment.yaml`
files already use `${OCIR_REGION}` and inherit the new default from `build-push-images.sh:43` —
no source edit required.

Security: per `.planning/SECURITY.md`, this plan touches only repo files. No OCI mutations, no
OCIDs, no IPs. The test assertion is line-and-comment-aware (per `<critical_rules>` in
get-shit-done) — uses `grep -v '^#' | grep -c` so a header comment mentioning frankfurt does not
self-invalidate the gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/SECURITY.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-CONTEXT.md
@.planning/phases/08-phoenix-native-build-registry-migration/08-PATTERNS.md
@deploy/helm/octo-apm-demo/values.yaml
@deploy/helm/octo-apm-demo/templates/_helpers.tpl
@tests/test_unified_deploy_surface.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Helm defaults to us-phoenix-1</name>
  <files>deploy/helm/octo-apm-demo/values.yaml, deploy/k8s/shop/secret-provider-class.yaml</files>
  <action>Two surgical edits, atomic in one commit.

  (1) `deploy/helm/octo-apm-demo/values.yaml`:
  - Line 18: change `ociRegion: eu-frankfurt-1` → `ociRegion: us-phoenix-1`.
  - Line 29: change `region: eu-frankfurt-1` (under `global.image:`) → `region: us-phoenix-1`.
  - Update the surrounding comments at lines 14-33 to reflect the new default and to note that
    `OCIR_REGION=eu-frankfurt-1` remains available as an operator override for the 7-day fallback
    window (cite BUILD-06 explicitly in the comment).
  - Do NOT touch per-component overrides (`shop.image.region`, `crm.image.region`,
    `javaGateway.image.region`, `workflowGateway.image.region`, `stressRunner.image.region`) —
    these stay empty strings so they inherit from `global.image.region`. That inheritance is the
    helper's contract (`templates/_helpers.tpl:37-51` per 08-PATTERNS.md) — leave the helper file
    untouched.
  - Do NOT change `global.dnsDomain`, `global.serviceNamespace`, or any non-region key.

  (2) `deploy/k8s/shop/secret-provider-class.yaml:45`:
  - Change the comment `# Region slug without the ocir.io suffix, e.g. eu-frankfurt-1` →
    `# Region slug without the ocir.io suffix, e.g. us-phoenix-1`.
  - No other change to this file. This is purely documentation hygiene (08-PATTERNS.md flagged it
    as the sole non-Helm frankfurt residue).

  This task implements per BUILD-03 — source-of-truth flip. The `${OCIR_REGION}` env-var pattern
  in `deploy/k8s/oke/*/deployment.yaml` and `tools/traffic-generator/k8s/deployment.yaml` is NOT
  touched (those files use `${OCIR_REGION}.ocir.io/...` and inherit the new default from
  `deploy/oke/build-push-images.sh:43` which is already `us-phoenix-1`).

  Do NOT run `helm template` or `kubectl apply` as part of this task — that lives in 08-07. This
  task is repo edits only.</action>
  <verify>
    <automated>grep -n "us-phoenix-1" deploy/helm/octo-apm-demo/values.yaml | grep -E "ociRegion|region:" &amp;&amp; ! grep -E "^[^#]*eu-frankfurt-1" deploy/helm/octo-apm-demo/values.yaml &amp;&amp; grep -c "us-phoenix-1" deploy/k8s/shop/secret-provider-class.yaml</automated>
  </verify>
  <done>`grep -n "eu-frankfurt-1" deploy/helm/octo-apm-demo/values.yaml` returns only matches on
  comment lines (lines starting with `#` after optional whitespace) referencing the rollback
  fallback. `grep -E "^[^#]*eu-frankfurt-1" deploy/helm/octo-apm-demo/values.yaml` returns no
  matches (zero non-comment frankfurt references). Manual `helm template ./deploy/helm/octo-apm-demo
  --set global.image.tenancy=PLACEHOLDER | grep -c 'eu-frankfurt-1\.ocir\.io'` returns 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add frankfurt-OCIR regression gate to test_unified_deploy_surface.py</name>
  <files>tests/test_unified_deploy_surface.py</files>
  <action>Append a new pytest function to the existing surface-test module that catches any future
  drive-by edit that re-introduces `eu-frankfurt-1.ocir.io` into a non-comment line of any file
  under `deploy/`.

  Function name: `test_deploy_tree_has_no_frankfurt_ocir_references` (single function — keep
  granularity per file).

  Implementation (use the patterns already in this module — pathlib, no subprocess):
  - `deploy_root = ROOT / "deploy"`
  - Walk `deploy_root.rglob("*")`, filter to files only, skip binary files (use a small allowlist of
    extensions: `.sh`, `.yaml`, `.yml`, `.tf`, `.md`, `.tpl`, `.json`).
  - For each file, read text, split into lines, strip leading whitespace, KEEP only lines that do
    NOT start with `#` (covers shell + YAML comments) AND do NOT start with `//` (covers any TF
    line comment).
  - Concatenate the surviving lines and assert `"eu-frankfurt-1.ocir.io" not in survived`.
  - On failure, the assertion message MUST list the offending file paths (use a list comprehension
    to collect, then `assert not offenders, f"frankfurt OCIR refs leaked into: {offenders}"`).
  - Mark with `@pytest.mark.unit` to match the existing module style if other tests have markers
    (check the existing module — only add the marker if other tests use it; otherwise leave bare).

  Documented exceptions:
  - `deploy/helm/octo-apm-demo/values.yaml` comment block referencing the rollback override is
    permitted (lines starting with `#` are filtered out — no special-case needed).
  - `deploy/compute/terraform/variables.tf:111` has the string in a `description = "..."` of a TF
    variable (08-PATTERNS.md grep). The line is NOT a comment, but the variable describes an
    optional override. RESOLUTION: change the description in this same file to read
    `"Optional OCIR registry override, e.g. us-phoenix-1.ocir.io (frankfurt available as 7-day fallback)."`.
    Do this as part of this task — keep the test strict (no allowlist needed).
  - `deploy/pre-flight-check.sh:21` is a shell comment (`#   OCIR_REPO=eu-frankfurt-1.ocir.io/...`),
    so the line-filter handles it automatically. Do NOT change that file.

  Cite BUILD-03 and `.planning/SECURITY.md` in the test docstring. Do not introduce any new third-
  party deps — pathlib + stdlib only.</action>
  <verify>
    <automated>pytest tests/test_unified_deploy_surface.py::test_deploy_tree_has_no_frankfurt_ocir_references -v</automated>
  </verify>
  <done>Test passes after Task 1's edits. Verify the test actually fails on regression: temporarily
  add a single line `image: eu-frankfurt-1.ocir.io/x/y:z` to `deploy/k8s/oke/shop/deployment.yaml`,
  re-run the test, confirm FAIL with the file path in the message, revert.
  `deploy/compute/terraform/variables.tf:111` description updated. Full `pytest
  tests/test_unified_deploy_surface.py -v` passes (all prior tests still green + new test).</done>
</task>

</tasks>

<verification>
1. Helm render gate (BUILD-03 acceptance test):
   `helm template octo-apm-demo deploy/helm/octo-apm-demo --set global.image.tenancy=PLACEHOLDER \
     | grep -c 'eu-frankfurt-1\.ocir\.io'`
   MUST output `0`.
2. Rollback escape hatch still functional:
   `helm template octo-apm-demo deploy/helm/octo-apm-demo \
     --set global.image.tenancy=PLACEHOLDER \
     --set global.ociRegion=eu-frankfurt-1 \
     --set global.image.region=eu-frankfurt-1 \
     | grep -c 'eu-frankfurt-1\.ocir\.io'`
   MUST output ≥ 1 (proves override path works for BUILD-06).
3. Raw envsubst rollback still functional:
   `OCIR_REGION=eu-frankfurt-1 OCIR_TENANCY=PLACEHOLDER IMAGE_TAG=test K8S_NAMESPACE_SHOP=octo-drone-shop \
     envsubst < deploy/k8s/oke/shop/deployment.yaml | grep -c 'eu-frankfurt-1\.ocir\.io'`
   MUST output ≥ 1.
4. `pytest tests/test_unified_deploy_surface.py -v` — all green including new assertion.
5. No real OCID / IP / tenancy namespace introduced.
</verification>

<success_criteria>
- BUILD-03 closed: phx is the default everywhere in deploy/, frankfurt is reachable only via
  explicit operator override.
- Helm-render grep gate returns 0 with defaults, ≥1 with override.
- Surface test guards against regression.
- No functional change to `templates/_helpers.tpl` (the existing helper already supports the new
  default).
</success_criteria>

<output>
After completion, create `.planning/phases/08-phoenix-native-build-registry-migration/08-06-SUMMARY.md`
with: diff summary of the two value edits (3 lines total), grep-gate output before/after, override
verification transcript, requirements closed.
</output>
