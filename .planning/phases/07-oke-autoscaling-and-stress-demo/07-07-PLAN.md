---
phase: 07-oke-autoscaling-and-stress-demo
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/apm-saved-queries/README.md
  - tools/apm-saved-queries/oke-pod-count-over-time.json
  - tools/apm-saved-queries/oke-latency-percentiles-during-scale.json
  - tools/apm-saved-queries/oke-trace-propagation-new-pods.json
  - tools/apm-saved-queries/oke-error-saturation-slow-spans.json
  - tools/apm-saved-queries/apply.sh
autonomous: true
requirements: [SCALE-04]
tags: [apm, saved-queries, observability]
must_haves:
  truths:
    - "Four APM saved queries exist as JSON specs under `tools/apm-saved-queries/`, one per D-16 saved query"
    - "Each saved query scopes to `service.namespace=octo_apm_demo` (Phase 1 OBS-05 contract)"
    - "An `apply.sh` operator script mirrors `tools/la-saved-searches/apply.sh` structure: dry-run by default, scoped to OCTO compartment, no live mutation without confirm"
    - "Saved-query metadata includes outbound drilldown link metadata per D-20 (lm/phoenix/openlit/grafana .<DNS_DOMAIN>)"
  artifacts:
    - path: "tools/apm-saved-queries/oke-pod-count-over-time.json"
      provides: "Pod-count-over-time query (D-16 #1)"
      contains: "k8s.pod.name"
    - path: "tools/apm-saved-queries/oke-latency-percentiles-during-scale.json"
      provides: "p50/p95/p99 query (D-16 #2)"
      contains: "p95"
    - path: "tools/apm-saved-queries/oke-trace-propagation-new-pods.json"
      provides: "Trace-to-new-pod filter (D-16 #3)"
      contains: "k8s.pod.name"
    - path: "tools/apm-saved-queries/oke-error-saturation-slow-spans.json"
      provides: "Error/saturation top-N (D-16 #4)"
      contains: "span.status"
    - path: "tools/apm-saved-queries/apply.sh"
      provides: "Operator-gated apply script (dry-run default)"
      contains: "APPLY:=false"
  key_links:
    - from: "Saved query JSON files"
      to: "OCI APM domain"
      via: "apply.sh → oci apm-traces ... or operator manual import"
      pattern: "octo_apm_demo namespace filter"
---

<objective>
Ship the four APM saved queries from D-16 as committed JSON specs in a new
`tools/apm-saved-queries/` directory, plus an operator-gated apply
script. These queries are the APM half of the Phase 7 scale-narrative
(pod count, latency percentiles, trace propagation to new pods,
error/saturation top-N). Each query scopes to the `octo_apm_demo`
service namespace per Phase 1 OBS-05.

Purpose: Reproducible, version-controlled APM saved queries that an
operator can import during the scaling demo window. Saved-query
metadata also carries the D-20 external drilldown links so the APM UI
exposes the operator path from "I see a slow span" to "click here for
Langfuse/Phoenix/OpenLIT/Grafana".

Output: New directory + 4 JSON files + README + apply.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md

<interfaces>
<!-- Pattern analog: tools/la-saved-searches/errors-by-route.json (structural shape) -->
{
  "name": "<slug>",
  "description": "...",
  "displayName": "OCTO — ...",
  "queryString": "...",
  "widgetType": "BAR_CHART"
}

<!-- APM query string syntax (OCI APM filter language) -->
attributes.{service.name, service.namespace, k8s.pod.name, span.status}
Functions: stats count() by ..., percentile, histogram, sort, head limit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pytest assertions for APM saved queries</name>
  <read_first>
    - tests/test_stress_demo_surface.py (extend)
    - tools/la-saved-searches/errors-by-route.json (analog JSON shape)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md §D-16, §D-20
  </read_first>
  <behavior>
    - test_apm_saved_queries_directory_exists() — `tools/apm-saved-queries/` exists with README.md
    - test_apm_saved_query_pod_count_valid() — `oke-pod-count-over-time.json` parses, has `name`, `displayName`, `queryString`, contains `k8s.pod.name` and `service.namespace = 'octo_apm_demo'` (or `'octo-apm-demo'` per APM conventions — accept either) and `bucket = 1m` or `1min`
    - test_apm_saved_query_latency_percentiles_valid() — `oke-latency-percentiles-during-scale.json` contains `p50`, `p95`, `p99`, `/api/shop/checkout` operation filter, 30s bucket
    - test_apm_saved_query_trace_to_new_pods_valid() — `oke-trace-propagation-new-pods.json` filters on `k8s.pod.name` first-appearance window logic
    - test_apm_saved_query_error_saturation_valid() — `oke-error-saturation-slow-spans.json` contains `span.status` filter and top-N pattern
    - test_apm_saved_queries_drilldown_links_present() — at least one JSON contains the drilldown link metadata for `lm.<DNS_DOMAIN>`, `phoenix.<DNS_DOMAIN>`, `openlit.<DNS_DOMAIN>`, `grafana.<DNS_DOMAIN>` (an `external_drilldowns` array or similar — schema is operator-readable, not strictly validated by APM)
    - test_apm_saved_queries_apply_script_dry_run_default() — `tools/apm-saved-queries/apply.sh` exists, executable, contains `: "${APPLY:=false}"`, contains `read -p` confirm prompt
    - test_apm_saved_queries_no_live_ocids() — `grep -rE "ocid1\\." tools/apm-saved-queries/` returns no matches
  </behavior>
  <action>Append 8 test functions to `tests/test_stress_demo_surface.py`. Use `json.loads(read_text(...))` for the 4 query JSON tests. The drilldown_links test iterates all 4 JSON files looking for any of the 4 host strings. Commit `test(07-07): RED assertions for APM saved queries`.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "apm_saved" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 8 test functions defined; all fail (directory does not exist)
  </acceptance_criteria>
  <done>RED state committed.</done>
</task>

<task type="auto">
  <name>Task 2: Author 4 APM saved queries + README + apply.sh</name>
  <read_first>
    - tools/la-saved-searches/apply.sh (full file — apply script analog)
    - tools/la-saved-searches/README.md (README convention)
    - tools/la-saved-searches/errors-by-route.json (JSON shape analog)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md §D-16, §D-20
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md §APM saved query patterns (if present)
  </read_first>
  <action>Create the directory `tools/apm-saved-queries/` with the following files:

(1) `README.md` — explain the directory contains JSON specs for OCI APM saved queries; each JSON is a single saved query; `apply.sh` imports them via `oci apm-traces ...` CLI; dry-run is default; live apply happens during operator window; scope is `octo_apm_demo` service namespace.

(2) `oke-pod-count-over-time.json` — JSON object with `name: "octo-oke-pod-count"`, `displayName: "OCTO OKE — pod count over time (shop + java-apm)"`, `description: "..."`, `queryString` something like `service.namespace = 'octo_apm_demo' and (service.name = 'octo-drone-shop' or service.name = 'octo-apm-java-demo') | stats count_distinct(k8s.pod.name) by service.name, bucket(1min)`, `widgetType: "LINE_CHART"`, and an `external_drilldowns` array with the 4 D-20 hosts as `{name, url, why}` objects.

(3) `oke-latency-percentiles-during-scale.json` — `name: "octo-oke-latency-percentiles"`, query something like `service.namespace = 'octo_apm_demo' and (operationName = '/api/shop/checkout' or service.name = 'octo-apm-java-demo') | stats percentile(duration, 50) as p50, percentile(duration, 95) as p95, percentile(duration, 99) as p99 by service.name, bucket(30s)`, `widgetType: "LINE_CHART"`.

(4) `oke-trace-propagation-new-pods.json` — `name: "octo-oke-trace-to-new-pods"`, query something like `service.namespace = 'octo_apm_demo' and k8s.pod.name not in (<baseline_pod_set>) | stats count() by traceId, k8s.pod.name | sort -count | head 50`. The "baseline_pod_set" is a parameter the operator fills at apply time — document this in the README. `widgetType: "TABLE"`.

(5) `oke-error-saturation-slow-spans.json` — `name: "octo-oke-error-saturation"`, query `service.namespace = 'octo_apm_demo' and (span.status = 'ERROR' or duration > 1000ms) | stats count() by service.name, k8s.pod.name, span.status | sort -count | head 50`, `widgetType: "BAR_CHART"`.

(6) `apply.sh` — mirror `tools/la-saved-searches/apply.sh` structure: `#!/usr/bin/env bash`, `set -euo pipefail`, defaults `: "${OCI_PROFILE:=demo}"`, `: "${APPLY:=false}"`, `: "${COMPARTMENT_ID:?required}"`, `require_tool oci`, `require_tool jq`, iterate `*.json` in script directory, build `oci apm-traces saved-search create-or-update` (verify exact CLI subcommand at apply time — fallback to operator manual import if subcommand unstable in current OCI CLI), confirm prompt before any mutation when APPLY=true. Make script executable.

Notes on APM query language: OCI APM filter syntax may differ slightly from the strings above — these are illustrative shapes. The JSON files must contain the listed substring tokens (`k8s.pod.name`, `service.namespace`, etc.) so the tests pass; the exact operator-visible queries will be tuned during the live operator window per the README.

Do NOT include live OCIDs, IPs, or tenancy labels. Drilldown link URLs use the public `*.<DNS_DOMAIN>` hosts only.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "apm_saved" 2>&1 | tail -15 && for f in tools/apm-saved-queries/*.json; do echo "$f"; python -c "import json; json.load(open('$f'))"; done && bash -n tools/apm-saved-queries/apply.sh</automated>
  </verify>
  <acceptance_criteria>
    - All 8 apm_saved tests PASS
    - All 4 JSON files parse via `json.load`
    - `bash -n tools/apm-saved-queries/apply.sh` exits 0
    - `tools/apm-saved-queries/apply.sh --help` exits 0
    - `grep -rE 'ocid1\.' tools/apm-saved-queries/` returns no matches
    - `grep -rE '([0-9]{1,3}\.){3}[0-9]{1,3}' tools/apm-saved-queries/` returns no matches (no leaked IPs)
  </acceptance_criteria>
  <done>Four APM queries committed as JSON; operator script ready for live apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → OCI APM domain | apply.sh issues `oci apm-traces` CLI calls under operator profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-36 | Information Disclosure | Live OCIDs or tenancy labels committed in JSON | mitigate | Test `test_apm_saved_queries_no_live_ocids` greps the directory; CI catches |
| T-07-37 | Tampering | apply.sh hardcoded to APPLY=true | mitigate | `: "${APPLY:=false}"` default; confirm prompt; test asserts |
| T-07-38 | Repudiation | Saved query mutations untraceable | accept | OCI APM CLI audit log captures saved-search mutations natively |
</threat_model>

<verification>
- `pytest tests/test_stress_demo_surface.py -x -k "apm_saved"` green
- All 4 JSON files parse
- `bash -n tools/apm-saved-queries/apply.sh` exits 0
- No live secrets / OCIDs / IPs committed
</verification>

<success_criteria>
- 4 APM saved queries exist with octo_apm_demo namespace filter
- Apply script idempotent + dry-run default + confirm-on-apply
- D-20 drilldown link metadata present
- All plan 07 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-oke-autoscaling-and-stress-demo/07-07-SUMMARY.md`
</output>
