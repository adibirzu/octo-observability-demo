---
phase: 07-oke-autoscaling-and-stress-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/oke/configure-cluster-autoscaler.sh
  - deploy/oke/cluster-autoscaler-config.json
  - deploy/helm/octo-apm-demo/charts/prometheus-adapter-values.yaml
  - tests/test_stress_demo_surface.py
autonomous: true
requirements: [SCALE-01, SCALE-02]
tags: [oke, cluster-autoscaler, prometheus-adapter, scripts]
must_haves:
  truths:
    - "An operator can run `configure-cluster-autoscaler.sh` in dry-run mode (default) without mutating live OCI state (D-04)"
    - "The script is idempotent: re-running with --apply detects existing add-on and switches install-addon→update-addon (D-04)"
    - "Cluster Autoscaler config pins worker pool to min=2, max=4 nodes (D-04)"
    - "Prometheus adapter values.yaml declares the rule that publishes shop_request_rate + java_request_rate as External Metrics for HPA consumption (D-02)"
    - "No live OCIDs are committed; the node pool OCID is supplied via env var at apply time (SEC-04, KB-456)"
  artifacts:
    - path: "deploy/oke/configure-cluster-autoscaler.sh"
      provides: "Idempotent operator-gated CA add-on install/update wrapper"
      contains: "install-addon"
      min_lines: 100
    - path: "deploy/oke/cluster-autoscaler-config.json"
      provides: "CA add-on JSON config with nodes: 2:4:${OKE_NODE_POOL_OCID} envsubst placeholder"
      contains: "nodes"
    - path: "deploy/helm/octo-apm-demo/charts/prometheus-adapter-values.yaml"
      provides: "Prometheus adapter chart values exposing shop/java RPS as External Metrics"
      contains: "external"
  key_links:
    - from: "configure-cluster-autoscaler.sh"
      to: "cluster-autoscaler-config.json"
      via: "--from-json file://$(dirname \"$0\")/cluster-autoscaler-config.json"
      pattern: "cluster-autoscaler-config.json"
    - from: "prometheus-adapter-values.yaml rules"
      to: "shop HPA External metric (plan 01)"
      via: "metric name shop_request_rate"
      pattern: "shop_request_rate"
---

<objective>
Ship the OCI control-plane assets needed to enable Cluster Autoscaler and
the External Metrics path for HPA RPS — without mutating live OCI state.
All actual `oci ce cluster install-addon` / `helm install prometheus-adapter`
calls happen during an operator-gated window (per CONTEXT.md `<deferred>`);
this plan ships the scripts, configs, and Helm values so that window is a
review-then-apply step instead of a write-from-scratch step.

Purpose: Provide the operator with three artifacts (script, config, helm
values) that map 1:1 to the live commands needed at apply time, all
idempotent, all dry-run by default, all scoped to the OCTO cluster.

Output: New shell script + JSON config under `deploy/oke/`; new helm
sub-chart values file under `deploy/helm/octo-apm-demo/charts/`; extended
test assertions in `tests/test_stress_demo_surface.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-VALIDATION.md

<interfaces>
<!-- Pattern analog: deploy/oke/install-oci-kubernetes-monitoring.sh -->
Header: set -euo pipefail
usage() { cat <<'EOF' ... EOF }
case "${1:-}" in -h|--help) usage; exit 0 ;; ...; esac
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
: "${OCI_PROFILE:=demo}"
: "${OCI_REGION:=us-phoenix-1}"
: "${OKE_CLUSTER_NAME:=octo-apm-demo-oke}"
: "${APPLY:=false}"  # opposite default vs install-oci-kubernetes-monitoring; dry-run by default
require_tool() { command -v "$1" >/dev/null 2>&1 || { echo "Missing: $1" >&2; exit 1; }; }
require_tool oci; require_tool jq; require_tool kubectl
SKIP_CONTEXT_CHECK guard from analog
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pytest assertions for CA script + adapter values</name>
  <read_first>
    - tests/test_stress_demo_surface.py (after plan 01 task 1 — extend the file)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`tests/test_stress_demo_surface.py` (Adapt list)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-VALIDATION.md §Validation Architecture (row "Cluster Autoscaler decision → node add")
  </read_first>
  <behavior>
    - test_configure_cluster_autoscaler_script_exists() asserts file `deploy/oke/configure-cluster-autoscaler.sh` exists and `os.access(p, os.X_OK)` is True
    - test_configure_cluster_autoscaler_dry_run_default() asserts script body contains `: "${APPLY:=false}"` (dry-run default — opposite of install-oci-kubernetes-monitoring.sh which has APPLY:=true)
    - test_configure_cluster_autoscaler_has_install_and_update_branches() asserts both `install-addon` AND `update-addon` substrings present (idempotency: first run installs, subsequent runs update)
    - test_configure_cluster_autoscaler_has_confirm_prompt() asserts script body contains `read -p` (interactive confirmation gate per D-04)
    - test_configure_cluster_autoscaler_has_list_addons_precheck() asserts `list-addons` substring present (the idempotency precheck)
    - test_configure_cluster_autoscaler_reads_config_json() asserts `cluster-autoscaler-config.json` substring present (script loads the JSON config)
    - test_cluster_autoscaler_config_has_min_max_2_4() asserts `deploy/oke/cluster-autoscaler-config.json` parses as JSON and contains the substring `2:4:` and `${OKE_NODE_POOL_OCID}` (no live OCID committed)
    - test_prometheus_adapter_values_publishes_shop_rps() asserts `deploy/helm/octo-apm-demo/charts/prometheus-adapter-values.yaml` exists and contains `shop_request_rate` and `java_request_rate` and `external` and `octo_apm_demo` (namespace pin)
  </behavior>
  <action>Append 8 test functions to `tests/test_stress_demo_surface.py`. Each function uses the same `ROOT` + `read_text` pattern already established. The JSON test uses `json.loads(read_text(...))` and validates the parsed dict has a `nodes` key. The shell tests are pure substring assertions against the script body (no execution). Commit with message `test(07-02): add failing assertions for CA script + adapter values`.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "cluster_autoscaler or prometheus_adapter" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 8 new test functions present with exact names per behavior block
    - All 8 fail (RED) before remaining tasks land — the files do not yet exist
  </acceptance_criteria>
  <done>RED-state tests committed; remaining tasks deliver each artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Create configure-cluster-autoscaler.sh + cluster-autoscaler-config.json</name>
  <read_first>
    - deploy/oke/install-oci-kubernetes-monitoring.sh (lines 1-120 — full analog: shebang, usage, defaults, tool checks, context guard)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`deploy/oke/configure-cluster-autoscaler.sh (NEW, script)`
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md §Pattern 2 (lines 337-368 for the cluster-autoscaler-config.json shape)
  </read_first>
  <action>Create `deploy/oke/configure-cluster-autoscaler.sh` following install-oci-kubernetes-monitoring.sh structure verbatim for: shebang `#!/usr/bin/env bash`, `set -euo pipefail`, `usage()` function with HEREDOC documenting required env vars (`OCI_PROFILE`, `OCI_REGION`, `OKE_CLUSTER_NAME`, `COMPARTMENT_ID`, `OKE_NODE_POOL_OCID`), arg parser for `--apply` / `-h|--help`, `SCRIPT_DIR` and `REPO_ROOT` derivation, defaults via `: "${VAR:=default}"` block (set `: "${APPLY:=false}"` — opposite of analog), `require_tool oci`, `require_tool jq`, `require_tool kubectl`, and the `SKIP_CONTEXT_CHECK` guard from analog lines 96-103. Body: (a) hard-fail if `COMPARTMENT_ID` unset; (b) resolve `CLUSTER_ID` via `oci ce cluster list --compartment-id "$COMPARTMENT_ID" --name "$OKE_CLUSTER_NAME"` piped through `jq -r '.data[0].id'`; (c) call `oci ce cluster list-addons --cluster-id "$CLUSTER_ID"` and detect via jq whether `ClusterAutoscaler` is present; (d) substitute `${OKE_NODE_POOL_OCID}` into the JSON config using `envsubst < "$SCRIPT_DIR/cluster-autoscaler-config.json"` to a temp file; (e) if APPLY=false, echo `[dry-run] would call oci ce cluster install-addon|update-addon ...` with the resolved command; (f) if APPLY=true, prompt `read -p "Type the cluster name '$OKE_CLUSTER_NAME' to confirm apply: " CONFIRM` and abort if mismatch; (g) call `oci ce cluster install-addon` (first run) or `oci ce cluster update-addon` (re-run) with `--from-json file://$temp_config --addon-name ClusterAutoscaler`. Make the script executable (`chmod +x`). Create `deploy/oke/cluster-autoscaler-config.json` per RESEARCH §Pattern 2: a JSON object with `addonName: ClusterAutoscaler`, `configurations` array containing `{key: "nodes", value: "2:4:${OKE_NODE_POOL_OCID}"}` and any other RESEARCH-listed required keys (e.g., `scaleDownDelayAfterAdd`, `scaleDownUnneededTime`). NEVER hardcode a live OCID — keep `${OKE_NODE_POOL_OCID}` as the substitution placeholder.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "cluster_autoscaler" 2>&1 | tail -15 && bash -n deploy/oke/configure-cluster-autoscaler.sh && deploy/oke/configure-cluster-autoscaler.sh --help</automated>
  </verify>
  <acceptance_criteria>
    - All 7 cluster-autoscaler-related tests PASS
    - `bash -n deploy/oke/configure-cluster-autoscaler.sh` exits 0 (shellcheck-equivalent syntax check)
    - `deploy/oke/configure-cluster-autoscaler.sh --help` exits 0 and prints usage
    - `os.access('deploy/oke/configure-cluster-autoscaler.sh', os.X_OK)` returns True
    - `python -c "import json; print(json.load(open('deploy/oke/cluster-autoscaler-config.json')))"` exits 0
    - `grep -c '130\.\|10\.' deploy/oke/cluster-autoscaler-config.json` returns 0 (no leaked IPs or live OCIDs — pattern checks for digits that would indicate a real OCID prefix; the placeholder `${OKE_NODE_POOL_OCID}` is safe)
  </acceptance_criteria>
  <done>Operator can `--help` the script, dry-run it (default), or `--apply` after confirmation. Config JSON has no live OCIDs.</done>
</task>

<task type="auto">
  <name>Task 3: Create prometheus-adapter-values.yaml sub-chart values</name>
  <read_first>
    - deploy/helm/octo-apm-demo/values.yaml (top-level structure for context)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md §Standard Stack §prometheus-adapter (lines 119-125) and §Don't Hand-Roll (RPS rule shape)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §No Analog Found row for `prometheus-adapter-values.yaml`
  </read_first>
  <action>Create `deploy/helm/octo-apm-demo/charts/prometheus-adapter-values.yaml` (new directory `charts/` under the chart). File is a Helm values file for the upstream `prometheus-community/prometheus-adapter` chart. Required structure: top-level `prometheus:` block pointing at `url: http://prom-prometheus-server.octo-autoscaling.svc.cluster.local` and `port: 80` (or whichever the operator window will use — comment that the value is a placeholder reviewed at apply time). Top-level `rules:` block with `external:` list containing two entries: one for `shop_request_rate` and one for `java_request_rate`. Each external rule maps a Prometheus query (e.g., `sum(rate(http_server_requests_seconds_count{service="octo-drone-shop",namespace="octo_apm_demo"}[1m])) by (pod)`) to an `external.metrics.k8s.io/v1beta1` metric of the same name; `seriesQuery` filters on `service` label; `name.as` echoes the metric name verbatim. Add a top-level comment header documenting: (a) this file is consumed by `helm install prometheus-adapter prometheus-community/prometheus-adapter -f ...`; (b) the rules expose `shop_request_rate` / `java_request_rate` to HPA as External Metrics; (c) operator apply happens during the same window as `configure-cluster-autoscaler.sh --apply`. Do NOT include live OCIDs, public IPs, or tenancy labels.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "prometheus_adapter" 2>&1 | tail -15 && python -c "import yaml; print(list(yaml.safe_load_all(open('deploy/helm/octo-apm-demo/charts/prometheus-adapter-values.yaml'))))"</automated>
  </verify>
  <acceptance_criteria>
    - `test_prometheus_adapter_values_publishes_shop_rps` PASSES
    - YAML parses without exception
    - File contains the strings `shop_request_rate`, `java_request_rate`, `external`, and `octo_apm_demo`
    - File contains `service="octo-drone-shop"` and `service="octo-apm-java-demo"` Prometheus label filters
    - `grep -E '(^|\s)([0-9]{1,3}\.){3}[0-9]{1,3}' deploy/helm/octo-apm-demo/charts/prometheus-adapter-values.yaml` returns 0 (no leaked IPs)
  </acceptance_criteria>
  <done>Adapter values file exists, parses, declares both External metrics with octo_apm_demo namespace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → OCI control plane | `configure-cluster-autoscaler.sh --apply` issues `oci ce cluster install-addon` |
| Repo file → cluster config | `cluster-autoscaler-config.json` injected via `oci ce ... --from-json` |
| HPA controller → prometheus-adapter | Adapter serves External Metrics API; HPA trusts its rule evaluation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | configure-cluster-autoscaler.sh accidentally hardcoded `--apply` | mitigate | `: "${APPLY:=false}"` defaults to dry-run (verified by `test_configure_cluster_autoscaler_dry_run_default`); `read -p` confirm prompt blocks accidental apply |
| T-07-06 | Information Disclosure | Committed OCIDs leak compartment/pool identity | mitigate | `${OKE_NODE_POOL_OCID}` envsubst placeholder; grep gate in acceptance forbids digit patterns matching OCIDs; KB-456 / SEC-04 enforced |
| T-07-07 | Denial of Service | Misconfigured `nodes: 2:4` value over-scales cluster | accept | min=2 max=4 matches D-04 explicit; node pool quotas in OCI enforce hard cap independently |
| T-07-08 | Elevation of Privilege | Script requires OCI admin profile to install add-on | mitigate | `OCI_PROFILE:=demo` documented in usage; operator window gating per CONTEXT.md `<deferred>` |
| T-07-09 | Repudiation | Apply action not auditable | mitigate | `oci ce cluster install-addon` events flow to OCI audit log natively; confirm prompt forces operator to type cluster name |
</threat_model>

<verification>
- `pytest tests/test_stress_demo_surface.py -x -k "cluster_autoscaler or prometheus_adapter"` green
- `bash -n deploy/oke/configure-cluster-autoscaler.sh` exits 0
- `deploy/oke/configure-cluster-autoscaler.sh --help` prints usage (no live OCI calls)
- `bash deploy/verify.sh` continues to pass (no regression on existing surface)
</verification>

<success_criteria>
- Script is idempotent (list-addons precheck), dry-run by default, requires confirm to apply
- Config JSON pins min=2 max=4 nodes with envsubst placeholder for the pool OCID
- Adapter values declare both `shop_request_rate` and `java_request_rate` External Metrics
- No live OCIDs, IPs, or secrets in any committed file
- All plan 02 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-oke-autoscaling-and-stress-demo/07-02-SUMMARY.md`
</output>
