---
phase: 07-oke-autoscaling-and-stress-demo
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/k8s/oke/stress-runner/namespace.yaml
  - deploy/k8s/oke/stress-runner/deployment.yaml
  - deploy/k8s/oke/stress-runner/service.yaml
  - deploy/k8s/oke/stress-runner/rbac.yaml
  - deploy/helm/octo-apm-demo/templates/stress-runner-deployment.yaml
  - deploy/helm/octo-apm-demo/templates/stress-runner-service.yaml
  - deploy/helm/octo-apm-demo/templates/stress-runner-rbac.yaml
  - tools/stress-runner/octo_stress_runner/__init__.py
  - tools/stress-runner/octo_stress_runner/main.py
  - tools/stress-runner/scenarios/checkout_journey.js
  - tools/stress-runner/scenarios/catalog_browse.js
  - tools/stress-runner/scenarios/login_burst.js
  - tools/stress-runner/Dockerfile
  - tools/stress-runner/pyproject.toml
autonomous: true
requirements: [SCALE-03]
tags: [k6, stress-runner, fastapi, oke, otlp]
must_haves:
  truths:
    - "A long-lived `octo-stress-runner` Deployment runs in `octo-stress` namespace, replicas=1, no Ingress (D-07)"
    - "FastAPI wrapper exposes `/internal/run`, `/internal/clear`, `/internal/state`, `/internal/healthz`, all gated by `X-Internal-Service-Key` header (D-12 internal contract)"
    - "Concurrency=1 enforced inside the wrapper: second `/internal/run` returns HTTP 409 with active run_id (D-14)"
    - "`/internal/clear` sends SIGTERM to the active k6 subprocess for graceful drain (D-14)"
    - "Three k6 scenarios (checkout_journey, catalog_browse, login_burst) target `https://shop.${DNS_DOMAIN}` with headers `X-Octo-Stress-Target: oke` and `X-Run-Id: <uuid>` (D-08, D-09)"
    - "k6 invocation uses `--out experimental-opentelemetry` so spans correlate with shop/java spans (D-06)"
    - "Helm templates for the runner are gated by `.Values.stressRunner.enabled` (default false, D-05 spirit). This plan does NOT edit values.yaml — the `stressRunner:` top-level block is owned by Plan 07-01 (single-writer)."
  artifacts:
    - path: "deploy/k8s/oke/stress-runner/deployment.yaml"
      provides: "Long-lived runner Deployment with k6 binary + FastAPI wrapper"
      contains: "octo-stress-runner"
    - path: "tools/stress-runner/octo_stress_runner/main.py"
      provides: "FastAPI wrapper with concurrency=1, SIGTERM, run_id propagation"
      exports: ["app"]
      min_lines: 150
    - path: "tools/stress-runner/scenarios/checkout_journey.js"
      provides: "k6 scenario adapted from shop/k6/checkout-load.js"
      contains: "X-Octo-Stress-Target"
    - path: "tools/stress-runner/scenarios/catalog_browse.js"
      provides: "k6 scenario for catalog browsing only (GET /api/products)"
      contains: "X-Octo-Stress-Target"
    - path: "tools/stress-runner/scenarios/login_burst.js"
      provides: "k6 scenario for repeated login attempts"
      contains: "X-Octo-Stress-Target"
    - path: "deploy/helm/octo-apm-demo/templates/stress-runner-deployment.yaml"
      provides: "Helm template, gated, default off — references `.Values.stressRunner` block authored by Plan 07-01"
      contains: ".Values.stressRunner.enabled"
  key_links:
    - from: "FastAPI wrapper `/internal/run`"
      to: "k6 subprocess"
      via: "asyncio.create_subprocess_exec('k6', 'run', scenario_path)"
      pattern: "create_subprocess"
    - from: "k6 scenarios"
      to: "shop.${DNS_DOMAIN} via OCI LB"
      via: "HTTP requests with X-Octo-Stress-Target: oke header"
      pattern: "X-Octo-Stress-Target"
    - from: "stress-runner pod"
      to: "OCI APM"
      via: "OTel OTLP export from both FastAPI (wrapper spans) and k6 (--out experimental-opentelemetry)"
      pattern: "OTEL_SERVICE_NAME"
    - from: "Helm stress-runner templates"
      to: "values.yaml `stressRunner:` block (Plan 07-01 single-writer)"
      via: "{{- if .Values.stressRunner.enabled }} gates referencing the block authored upstream"
      pattern: ".Values.stressRunner"
---

<objective>
Ship the in-cluster load engine: a long-lived `octo-stress-runner`
Deployment containing a FastAPI wrapper plus the k6 binary in the same
pod. The wrapper exposes a 4-endpoint internal HTTP API (run, clear, state,
healthz), enforces concurrency=1 with HTTP 409 on race, sends SIGTERM on
clear, and tags every run with a UUID `run_id` that propagates to APM
spans (wrapper) and k6 OTLP output (load). Three k6 scenarios live in
`tools/stress-runner/scenarios/` adapted from the existing `shop/k6/` +
`crm/k6/` scenario libraries.

This plan is the SOLE writer of `deploy/k8s/oke/stress-runner/*`,
`deploy/helm/octo-apm-demo/templates/stress-runner-*`, and
`tools/stress-runner/*`. It does NOT edit `deploy/helm/octo-apm-demo/values.yaml`
— the top-level `stressRunner:` values block is staged by Plan 07-01
(single-owner of values.yaml in Wave 1). Plan 07-03 templates reference that
block read-only.

Purpose: Provides the actual load amplification mechanism the admin
stress-test UI will trigger in plan 05. By splitting the wrapper into its
own plan/pod, the admin route only orchestrates (HTTP POST) — all k6
lifecycle complexity is encapsulated.

Output: Raw OKE manifests under `deploy/k8s/oke/stress-runner/`; Helm
templates under `deploy/helm/octo-apm-demo/templates/`; Python package
under `tools/stress-runner/`; three k6 scenario `.js` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-VALIDATION.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-01-PLAN.md

<interfaces>
<!-- Pattern analog: tools/traffic-generator/k8s/deployment.yaml -->
Namespace: octo-stress, labels app.kubernetes.io/component: stress-runner
Deployment: replicas=1, container port 8080, image OCIR-pinned, env from secrets
Service: ClusterIP, port 8080 → 8080, no Ingress

<!-- Pattern analog: crm/server/chaos/admin.py for Pydantic + APIRouter shape -->
from fastapi import APIRouter, Depends, HTTPException, Request, status
from pydantic import BaseModel, Field, field_validator
class RunRequest(BaseModel):
  scenario: str = Field(pattern=r"^(checkout_journey|catalog_browse|login_burst)$")
  rps: int = Field(ge=1, le=200)
  duration_seconds: int = Field(ge=10, le=600)
  run_id: str  # injected by CRM
  target_host: str  # e.g., shop.${DNS_DOMAIN}

<!-- Pattern analog: crm/server/modules/simulation.py:673-675 internal-key auth -->
headers["X-Internal-Service-Key"] = cfg.drone_shop_internal_key

<!-- Pattern analog: shop/k6/checkout-load.js — k6 scenario shape -->
export const options = { vus, duration, thresholds, tags }
export default function () { http.get/post(...) }

<!-- Plan 07-01 staged values.yaml block (read-only reference here) -->
stressRunner:
  enabled: false
  namespace: octo-stress
  image: { repository: octo-stress-runner, region: "", tenancy: "", tag: "" }
  internalKeySecret: octo-stress-runner-key
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pytest assertions for stress-runner manifests + wrapper + scenarios</name>
  <read_first>
    - tests/test_stress_demo_surface.py (extend further)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`tests/test_stress_demo_surface.py` (manifest assertion list)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-VALIDATION.md §Validation Architecture (rows: Stress concurrency=1, Stop button → SIGTERM)
  </read_first>
  <behavior>
    - test_stress_runner_namespace_manifest() — `deploy/k8s/oke/stress-runner/namespace.yaml` exists, contains `name: octo-stress`
    - test_stress_runner_deployment_manifest() — deployment.yaml contains `replicas: 1`, `name: octo-stress-runner`, `containerPort: 8080`, env `OCTO_STRESS_RUNNER_INTERNAL_KEY`, env `OTEL_SERVICE_NAME` with value `octo-stress-runner`, imagePullSecret `ocir-pull-secret`
    - test_stress_runner_service_manifest() — service.yaml has `type: ClusterIP`, `port: 8080`, no Ingress reference
    - test_stress_runner_rbac_minimal() — rbac.yaml contains `kind: ServiceAccount` named `octo-stress-runner`, NO `RoleBinding` or `ClusterRoleBinding` (minimal posture per RESEARCH; pod does not call k8s API)
    - test_stress_runner_helm_template_default_off() — `helm template deploy/helm/octo-apm-demo` does NOT contain `octo-stress-runner` Deployment when `.Values.stressRunner.enabled` is the default
    - test_stress_runner_helm_template_enabled_on() — `helm template deploy/helm/octo-apm-demo --set stressRunner.enabled=true` DOES contain `octo-stress-runner`
    - test_stress_runner_wrapper_main_concurrency_lock() — `tools/stress-runner/octo_stress_runner/main.py` contains `asyncio.Lock` OR a `_active_run` module-level guard, AND a literal substring `status_code=409` (concurrency=1 → HTTP 409 per D-14)
    - test_stress_runner_wrapper_sigterm_path() — main.py contains both `signal.SIGTERM` and `process.send_signal` OR `process.terminate()` (the SIGTERM-on-clear path)
    - test_stress_runner_wrapper_internal_key_header() — main.py contains `X-Internal-Service-Key`
    - test_stress_runner_wrapper_otel_service_name() — main.py reads `OTEL_SERVICE_NAME` from env (`os.getenv("OTEL_SERVICE_NAME")`) OR pyproject sets it; assert the literal in main.py
    - test_stress_runner_scenarios_have_required_headers() — for each of the 3 scenario `.js` files, contains both `X-Octo-Stress-Target` and `X-Run-Id` and `experimental-opentelemetry` (the k6 OTLP output flag in the scenario OR documented in the wrapper as the `--out` flag — assert in main.py if not in the scenario itself)
    - test_stress_runner_scenarios_target_lb_host() — each scenario `.js` references `__ENV.STRESS_TARGET_URL` or similar env-driven base URL (no hardcoded host)
    - test_stress_runner_internal_run_returns_202() — main.py has `@router.post("/internal/run", status_code=status.HTTP_202_ACCEPTED)` or equivalent literal `202`
    - test_stress_runner_pyproject_exists() — `tools/stress-runner/pyproject.toml` exists, declares `fastapi`, `uvicorn`, `pydantic` dependencies
    - test_stress_runner_dockerfile_uses_k6_image() — `tools/stress-runner/Dockerfile` references `grafana/k6` (multi-stage copy of k6 binary) or installs k6 via package
    - test_plan_07_03_does_not_edit_values_yaml() — confirms by `git diff` or by reading the in-progress plan output that `deploy/helm/octo-apm-demo/values.yaml` was NOT modified by this plan's commits (sole-writer guard: Plan 07-01 owns values.yaml). Implementation: assert the string `stressRunner:` appears in values.yaml exactly once (staged by Plan 07-01) — re-edits would create either drift or duplicates.
  </behavior>
  <action>Append the 16 test functions enumerated above to `tests/test_stress_demo_surface.py`. Use the same ROOT + read_text pattern. For the two Helm template tests, call `subprocess.run(["helm", "template", "deploy/helm/octo-apm-demo", ...], capture_output=True, text=True, check=True)` and assert substring presence/absence in `result.stdout`. Skip the helm tests with `pytest.importorskip` equivalent (`pytest.skip` if `shutil.which("helm") is None`) so the suite runs offline. Commit with `test(07-03): add failing assertions for stress-runner manifests + wrapper + scenarios`.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "stress_runner or scenarios or plan_07_03_does_not_edit" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 16 new test functions present with exact names
    - All fail (RED) — files do not yet exist
    - Helm-dependent tests skip gracefully when helm CLI absent
  </acceptance_criteria>
  <done>RED-state tests committed; remaining tasks deliver each artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the FastAPI wrapper (tools/stress-runner/) with concurrency=1 + SIGTERM + run_id</name>
  <read_first>
    - crm/server/chaos/admin.py (lines 19-80 for FastAPI/Pydantic skeleton; lines 160-219 for endpoint shape)
    - crm/server/modules/simulation.py (lines 660-715 for httpx + internal-key header pattern; reverse-direction context for how CRM will call THIS wrapper)
    - shop/k6/checkout-load.js (full file — k6 options + headers pattern)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`tools/stress-runner/octo_stress_runner/main.py`
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md §Pattern 3 (FastAPI wrapper with subprocess lifecycle)
    - tools/traffic-generator/pyproject.toml (closest pyproject analog)
  </read_first>
  <behavior>
    - POST /internal/run with valid `X-Internal-Service-Key` and body `{scenario, rps, duration_seconds, run_id, target_host}` returns HTTP 202 with `{status: "started", run_id, pid}`
    - POST /internal/run while another run is active returns HTTP 409 with `{status: "busy", active_run_id, started_at}`
    - POST /internal/run with missing/invalid `X-Internal-Service-Key` returns HTTP 401
    - POST /internal/clear sends SIGTERM to the active subprocess and returns `{status: "stopping", run_id}`; second clear when idle returns `{status: "idle"}`
    - GET /internal/state returns `{run_id, scenario, rps, duration_seconds, target_host, started_at, status}` or `{status: "idle"}`
    - GET /internal/healthz returns `{ok: true, has_k6: true|false}` based on `shutil.which("k6")`
    - Pydantic validates rps ∈ [1, 200], duration_seconds ∈ [10, 600], scenario ∈ {checkout_journey, catalog_browse, login_burst}; out-of-range returns HTTP 422
    - On startup, missing `OCTO_STRESS_RUNNER_INTERNAL_KEY` env raises RuntimeError (fail-fast)
    - Hard timeout: server-side asyncio task cancels the run at `duration_seconds + 30s` (D-14 safety net), emitting `status: "expired"`
  </behavior>
  <action>Create the Python package: `tools/stress-runner/octo_stress_runner/__init__.py` (empty); `tools/stress-runner/octo_stress_runner/main.py` (~200 LoC) containing the FastAPI `app`, dependency `require_internal_key` (Header dependency reading `X-Internal-Service-Key` and comparing constant-time to `os.environ["OCTO_STRESS_RUNNER_INTERNAL_KEY"]`), `RunRequest` Pydantic model with field validators per behavior, an `ActiveRun` dataclass tracking `run_id, process, started_at, scenario, rps, duration, target_host, status`, a module-level `_lock = asyncio.Lock()` plus `_active: ActiveRun | None`. The `/internal/run` handler acquires the lock non-blocking via `if _active is not None: raise HTTPException(409, ...)`, then `await asyncio.create_subprocess_exec("k6", "run", "--out", "experimental-opentelemetry", "-e", f"STRESS_TARGET_URL={target_host}", "-e", f"K6_VUS={rps}", "-e", f"K6_DURATION={duration}s", "-e", f"RUN_ID={run_id}", scenarios_dir / f"{scenario}.js")`. Schedule an asyncio task that calls `await process.wait()` and updates `_active.status` to `completed`. Schedule a second asyncio task with `await asyncio.sleep(duration + 30); if _active is process: process.terminate(); _active.status = "expired"` for hard timeout. `/internal/clear` calls `_active.process.terminate()` (SIGTERM) and waits up to 30s. Use `pydantic.BaseModel` with `model_config = ConfigDict(extra="forbid")` to reject unknown fields. Create `tools/stress-runner/pyproject.toml` cloning the shape of `tools/traffic-generator/pyproject.toml` but with project name `octo-stress-runner`, deps `fastapi`, `uvicorn[standard]`, `pydantic>=2`, `opentelemetry-sdk`, `opentelemetry-instrumentation-fastapi`. Do NOT inline implementation YAML or fenced code in this action — write the actual Python in main.py.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "wrapper or pyproject" 2>&1 | tail -20 && python -c "import sys; sys.path.insert(0,'tools/stress-runner'); from octo_stress_runner.main import app; print(app.routes)"</automated>
  </verify>
  <acceptance_criteria>
    - All wrapper-related tests PASS (concurrency_lock, sigterm_path, internal_key_header, otel_service_name, internal_run_returns_202, pyproject_exists)
    - `python -c "from octo_stress_runner.main import app"` imports without error (with appropriate PYTHONPATH or after editable install)
    - Routes include POST /internal/run, POST /internal/clear, GET /internal/state, GET /internal/healthz
    - Sending request without `X-Internal-Service-Key` to a TestClient instance returns 401
  </acceptance_criteria>
  <done>FastAPI wrapper is importable, validates inputs, enforces concurrency=1, sends SIGTERM on clear, propagates run_id.</done>
</task>

<task type="auto">
  <name>Task 3: Author k6 scenarios + Dockerfile</name>
  <read_first>
    - shop/k6/checkout-load.js (full file — primary analog for checkout_journey + catalog_browse)
    - crm/k6/stress_test.js (analog for login_burst burst pattern)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`tools/stress-runner/scenarios/*.js`
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md §Pattern 4 (k6 scenario shape, lines 536-569)
  </read_first>
  <action>Create three k6 scenario files under `tools/stress-runner/scenarios/`: (a) `checkout_journey.js` — clone `shop/k6/checkout-load.js` structure: GET catalog → POST add-to-cart → POST checkout; substitute base URL with `const TARGET = __ENV.STRESS_TARGET_URL || 'https://shop.<DNS_DOMAIN>';`, RUN_ID with `const RUN_ID = __ENV.RUN_ID || 'unset';`, headers on every request with `'X-Octo-Stress-Target': 'oke'` and `'X-Run-Id': RUN_ID` and `'User-Agent': 'k6/octo-stress-runner'`; (b) `catalog_browse.js` — minimal version of checkout_journey, just GET `/api/products` and GET `/api/products/{id}`; (c) `login_burst.js` — repeated POST `/api/auth/login` with throwaway credentials (use the demo test users — never real PII), `sleep(0.1)` between iterations. All three set `export const options = { vus: Number(__ENV.K6_VUS || 25), duration: __ENV.K6_DURATION || '60s', thresholds: { http_req_failed: ['rate<0.10'] }, tags: { scenario: '<name>', run_id: RUN_ID } }`. Create `tools/stress-runner/Dockerfile` multi-stage: stage 1 `FROM grafana/k6:0.55-alpine AS k6` (or whatever current latest stable is at execution time — verify against k6 docker hub at apply time); stage 2 `FROM python:3.12-slim`, copy k6 binary from stage 1 to `/usr/local/bin/k6`, install Python deps from pyproject, copy package source and scenarios, set `ENTRYPOINT ["uvicorn", "octo_stress_runner.main:app", "--host", "0.0.0.0", "--port", "8080"]`. Image build happens on control-plane VM per global cloud-build rule — Dockerfile only.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "scenarios or dockerfile" 2>&1 | tail -20 && for f in tools/stress-runner/scenarios/*.js; do echo "Checking $f"; node -c "$f" 2>/dev/null || echo "(node syntax check skipped — k6 dialect)"; done</automated>
  </verify>
  <acceptance_criteria>
    - `test_stress_runner_scenarios_have_required_headers`, `test_stress_runner_scenarios_target_lb_host`, `test_stress_runner_dockerfile_uses_k6_image` PASS
    - All three scenario files exist under `tools/stress-runner/scenarios/`
    - Each scenario contains `X-Octo-Stress-Target`, `X-Run-Id`, `User-Agent`, and `__ENV.STRESS_TARGET_URL`
    - Dockerfile contains `FROM grafana/k6` and `COPY --from=k6 /usr/bin/k6 /usr/local/bin/k6` (or equivalent multi-stage copy)
    - `grep -E '([0-9]{1,3}\.){3}[0-9]{1,3}' tools/stress-runner/scenarios/*.js` returns 0 (no hardcoded IPs)
  </acceptance_criteria>
  <done>Three k6 scenarios reusable; Dockerfile builds a single image with both runtimes.</done>
</task>

<task type="auto">
  <name>Task 4: Ship raw k8s manifests + Helm templates (NO values.yaml edit — Plan 07-01 owns it)</name>
  <read_first>
    - tools/traffic-generator/k8s/deployment.yaml (lines 1-80 — primary manifest analog: namespace + deployment + env)
    - deploy/k8s/oke/apm-java-demo/deployment.yaml (lines 38-45 for ocir-pull-secret pattern)
    - deploy/helm/octo-apm-demo/templates/shop-deployment.yaml (analog for Helm template shape)
    - deploy/helm/octo-apm-demo/values.yaml (READ ONLY — confirm Plan 07-01 staged the `stressRunner:` block; if absent, halt and ask developer to re-run Plan 07-01 first)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`deploy/k8s/oke/stress-runner/*` and §`deploy/helm/octo-apm-demo/templates/stress-runner-*`
  </read_first>
  <action>Before any other edit, verify Plan 07-01 has landed by checking `grep -c '^stressRunner:' deploy/helm/octo-apm-demo/values.yaml` returns 1. If 0, this plan executor must halt and surface the dependency violation — do NOT edit values.yaml in this plan. Under `deploy/k8s/oke/stress-runner/` create: (a) `namespace.yaml` — single Namespace `octo-stress` with labels `app.kubernetes.io/part-of: octo-apm-demo`, `app.kubernetes.io/component: stress-runner`; (b) `deployment.yaml` — Deployment `octo-stress-runner` in namespace `octo-stress`, `replicas: 1`, container `runner` with `image: ${OCIR_REGION}.ocir.io/${OCIR_TENANCY}/octo-stress-runner:${IMAGE_TAG:-latest}`, `containerPort: 8080`, env block including `OCTO_STRESS_RUNNER_INTERNAL_KEY` from `secretKeyRef: name=octo-stress-runner-key key=value`, `OTEL_SERVICE_NAME: octo-stress-runner`, `OTEL_RESOURCE_ATTRIBUTES: service.name=octo-stress-runner,service.namespace=octo_apm_demo,k8s.namespace.name=octo-stress`, `OTEL_EXPORTER_OTLP_ENDPOINT` from `octo-apm` secret (same shape as traffic-generator analog), `STRESS_TARGET_URL: https://shop.${DNS_DOMAIN}`, resources requests `cpu: 250m memory: 256Mi` / limits `cpu: 2 memory: 512Mi`, `imagePullSecrets: [ocir-pull-secret]`, `serviceAccountName: octo-stress-runner`, readinessProbe `httpGet path=/internal/healthz port=8080` initialDelay 5s, livenessProbe same with initialDelay 30s; (c) `service.yaml` — ClusterIP Service `octo-stress-runner` in `octo-stress`, port 8080 → 8080, selector `app: octo-stress-runner`, NO Ingress; (d) `rbac.yaml` — ONLY a ServiceAccount named `octo-stress-runner` in `octo-stress` namespace, with a header comment explaining "no Role/RoleBinding — pod does not call Kubernetes API; subprocess only spawns k6 binary" (per RESEARCH §Anti-Patterns — minimal RBAC). Under `deploy/helm/octo-apm-demo/templates/` create three Helm templates wrapping each manifest in `{{- if .Values.stressRunner.enabled }}` (default `false` per the block authored by Plan 07-01): `stress-runner-deployment.yaml`, `stress-runner-service.yaml`, `stress-runner-rbac.yaml`. Image repository/tag references `.Values.stressRunner.image.repository`, `.Values.stressRunner.image.tag`, `.Values.stressRunner.image.region`, `.Values.stressRunner.image.tenancy`. DO NOT edit `deploy/helm/octo-apm-demo/values.yaml` in this plan — sole-writer is Plan 07-01. If you discover the `stressRunner:` values block is missing fields needed by these templates, surface that to the developer rather than patching values.yaml here (preserves wave-1 ordering invariant).</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "stress_runner and (namespace or deployment or service or rbac or helm) or plan_07_03_does_not_edit" 2>&1 | tail -25 && python -c "import yaml; list(yaml.safe_load_all(open('deploy/k8s/oke/stress-runner/deployment.yaml')))" && helm template deploy/helm/octo-apm-demo --set stressRunner.enabled=true 2>&1 | grep -c "octo-stress-runner"</automated>
  </verify>
  <acceptance_criteria>
    - All stress_runner manifest tests PASS (namespace, deployment, service, rbac, helm_template_default_off, helm_template_enabled_on)
    - `test_plan_07_03_does_not_edit_values_yaml` PASSES (the `stressRunner:` block appears exactly once in values.yaml)
    - All four raw YAML files parse via `yaml.safe_load_all` with no exception
    - `helm template deploy/helm/octo-apm-demo` (default values) output does NOT contain `octo-stress-runner` (gated OFF by default — Plan 07-01's staged block defaults `enabled: false`)
    - `helm template deploy/helm/octo-apm-demo --set stressRunner.enabled=true` output contains `octo-stress-runner` Deployment + Service + ServiceAccount
    - `grep -c "kind: ClusterRoleBinding\|kind: RoleBinding" deploy/k8s/oke/stress-runner/rbac.yaml` returns 0 (minimal RBAC enforced)
    - `grep -E '([0-9]{1,3}\.){3}[0-9]{1,3}' deploy/k8s/oke/stress-runner/*.yaml` returns 0 (no hardcoded IPs)
    - `git diff deploy/helm/octo-apm-demo/values.yaml` for this plan's commits is empty (single-writer guard)
  </acceptance_criteria>
  <done>Stress-runner pod can be deployed via raw OKE apply OR helm install with `--set stressRunner.enabled=true`; default helm path remains unchanged; values.yaml untouched by this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRM Admin → octo-stress-runner | Cross-namespace internal HTTP call gated by `X-Internal-Service-Key` |
| octo-stress-runner → public LB | k6 subprocess generates outbound HTTPS load with `X-Octo-Stress-Target: oke` header |
| Wrapper subprocess → k6 binary | Wrapper spawns k6 with operator-controlled args (rps, duration, scenario) |
| Pod → k8s API | Intentionally NOT a trust boundary — SA has no role bindings; no API access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Spoofing | Caller forges X-Internal-Service-Key | mitigate | Constant-time compare in `require_internal_key`; key sourced from k8s Secret (`octo-stress-runner-key`); rotate per ops runbook |
| T-07-11 | Tampering | k6 args injected via RunRequest (scenario path traversal) | mitigate | Pydantic `Field(pattern=r"^(checkout_journey\|catalog_browse\|login_burst)$")` allow-list; scenario path constructed via Path join with allow-listed basename |
| T-07-12 | Denial of Service | Operator triggers load > cluster capacity | mitigate | Pydantic caps rps ≤ 200, duration ≤ 600s; concurrency=1 prevents stacking; hard timeout `duration + 30s` reaps stuck k6 |
| T-07-13 | Denial of Service | Stress run amplifies past tenancy LB quotas | mitigate | LB header pinning (D-09) keeps load on OKE backend set; OCI LB QPS quotas in effect; runbook documents back-off |
| T-07-14 | Information Disclosure | k6 logs include sensitive request bodies | mitigate | login_burst uses demo test users only (no PII); k6 default log level INFO does not echo bodies; scenario tags include `run_id` not session tokens |
| T-07-15 | Elevation of Privilege | Stress-runner pod gains k8s API access | mitigate | ServiceAccount has zero Role/RoleBinding; rbac.yaml test asserts this; pod cannot list/exec other workloads |
| T-07-16 | Repudiation | Run lifecycle untraceable | mitigate | Wrapper emits OTel span per run with `run_id` attribute; CRM emits the audit log (plan 05); 3-channel emit binds them |
</threat_model>

<verification>
- `pytest tests/test_stress_demo_surface.py -x -k "stress_runner or scenarios or wrapper"` green
- `helm template deploy/helm/octo-apm-demo` default render contains no stress-runner references (D-05 backward compat)
- `helm template ... --set stressRunner.enabled=true` includes Deployment + Service + ServiceAccount
- `python -c "from octo_stress_runner.main import app"` succeeds with PYTHONPATH set
- `git diff deploy/helm/octo-apm-demo/values.yaml` empty for Plan 07-03 commits (single-writer guard)
- `bash deploy/verify.sh` still passes (no regression)
</verification>

<success_criteria>
- Long-lived stress-runner Deployment ships in raw k8s + Helm forms
- Wrapper enforces concurrency=1, SIGTERM on clear, hard timeout, run_id propagation
- Three k6 scenarios target the public LB with `X-Octo-Stress-Target: oke` and `X-Run-Id` headers
- ServiceAccount has no Role bindings (minimal RBAC)
- Helm gating defaults to off (via Plan 07-01's staged values block); flipping `stressRunner.enabled=true` produces the Deployment
- Plan 07-03 never wrote values.yaml — single-owner invariant preserved
- All plan 03 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-oke-autoscaling-and-stress-demo/07-03-SUMMARY.md`
</output>
