---
phase: 07-oke-autoscaling-and-stress-demo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crm/server/modules/_admin_host.py
  - crm/server/modules/coordinator.py
  - tests/test_admin_host_helper.py
autonomous: true
requirements: [SCALE-03]
tags: [admin, refactor, host-bound, phase5-extension]
must_haves:
  truths:
    # (refactor-scope, intentional) — primary goal is single-source helper extraction; refactor by nature has internal truths.
    - "`_require_admin_host` + `_request_host` + `_configured_admin_hosts` live in a single shared module (`crm/server/modules/_admin_host.py`) and are imported, not re-implemented, by both `coordinator.py` and (in plan 05) `stress_test.py` (refactor-scope, intentional)"
    - "The Phase 5 admin-host contract continues to work bit-identically: `/api/admin/coordinator/*` still rejects non-admin hosts with HTTP 403 (refactor-scope, intentional — regression guard)"
    - "User-observable: an admin request to any stress-test endpoint from a non-admin host returns HTTP 403 with the `_admin_host`-emitted audit log entry. This truth is verified end-to-end downstream in Plan 07-05 (test_stress_apply_rejects_non_admin_host_403); Plan 07-04 ensures the helper that produces that 403 is structurally singular so the downstream test cannot drift away from coordinator behavior."
  artifacts:
    - path: "crm/server/modules/_admin_host.py"
      provides: "Shared host-bound enforcement helpers"
      exports: ["_request_host", "_configured_admin_hosts", "_require_admin_host", "_ADMIN_SURFACE", "_LOCAL_HOSTS"]
      min_lines: 40
  key_links:
    - from: "crm/server/modules/coordinator.py"
      to: "crm/server/modules/_admin_host.py"
      via: "from server.modules._admin_host import _require_admin_host, _request_host"
      pattern: "from server.modules._admin_host"
---

<objective>
Extract the host-bound admin enforcement primitives currently embedded in
`crm/server/modules/coordinator.py` into a shared helper module
`crm/server/modules/_admin_host.py`. Coordinator imports from the helper.
The behavior is unchanged: the only goal is making the same logic reusable
by the new `stress_test.py` module (plan 05) without copy-paste.

Per PATTERNS.md §Authentication and RESEARCH §Anti-Patterns §4: "do NOT
re-implement `_require_admin_host`". This plan satisfies that explicit
constraint as a small, isolated refactor that other Wave 2 plans depend on.

Purpose: Single source of truth for the admin-host boundary; eliminates
drift risk between coordinator and stress_test enforcement.

Output: New `_admin_host.py` module, edited `coordinator.py` (import +
delete duplicated helpers), regression test ensuring coordinator behavior
unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-ai-and-secure-operations/05-CONTEXT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md

<interfaces>
<!-- From crm/server/modules/coordinator.py:300-361 (helpers to extract) -->
_ADMIN_SURFACE = "admin.<DNS_DOMAIN>"
_LOCAL_HOSTS = {"localhost", "127.0.0.1", "::1", "testserver"}

def _request_host(request: Request) -> str: ...
def _configured_admin_hosts() -> set[str]: ...
def _require_admin_host(request: Request) -> str: ...

<!-- Imports used by helpers -->
from urllib.parse import urlparse
from fastapi import HTTPException, Request, status
from server.config import cfg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — regression + new-import tests</name>
  <read_first>
    - crm/server/modules/coordinator.py (lines 290-365 — exact helpers + constants to extract)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §Authentication (says: "Refactor recommendation: extract... do NOT re-implement")
  </read_first>
  <behavior>
    - test_admin_host_module_exports_helpers() — `from server.modules._admin_host import _request_host, _configured_admin_hosts, _require_admin_host, _ADMIN_SURFACE, _LOCAL_HOSTS` succeeds
    - test_admin_host_request_host_strips_port() — `_request_host(req)` with Host header `admin.example.com:8443` returns `admin.example.com`
    - test_admin_host_request_host_uses_xff_first() — when `X-Forwarded-Host: admin.example.com` AND `Host: backend.internal`, returns `admin.example.com`
    - test_admin_host_request_host_handles_ipv6_brackets() — Host `[::1]:8080` returns `::1`
    - test_admin_host_local_hosts_pass() — `_require_admin_host` with Host=`localhost` returns `localhost` (no exception)
    - test_admin_host_admin_surface_passes() — Host=`admin.<DNS_DOMAIN>` returns `admin.<DNS_DOMAIN>`
    - test_admin_host_unknown_host_403() — Host=`evil.example.com` raises HTTPException 403
    - test_admin_host_dns_domain_env_extends_allowlist() — when `cfg.dns_domain="<DNS_DOMAIN>"`, Host=`admin.<DNS_DOMAIN>` AND Host=`crm.<DNS_DOMAIN>` both pass
    - test_coordinator_still_imports_admin_host_helpers() — `crm/server/modules/coordinator.py` source contains `from server.modules._admin_host import` (proves it doesn't re-implement)
    - test_coordinator_admin_host_regression_403() — existing coordinator endpoint behavior: a POST to `/api/admin/coordinator/query` with Host=`evil.example.com` still returns 403 (no regression on Phase 5 contract)
  </behavior>
  <action>Create `tests/test_admin_host_helper.py` with the 10 test functions above. Use FastAPI `TestClient` + a minimal `Request` builder via `starlette.testclient`. For the regression test, instantiate the actual CRM app (via the existing test fixture, if any, or a new fixture importing `crm.server.main:app`). Commit with `test(07-04): RED tests for shared _admin_host helper extraction`. Tests SHOULD fail initially because `_admin_host.py` does not yet exist.</action>
  <verify>
    <automated>pytest tests/test_admin_host_helper.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 10 tests defined with exact function names
    - 9 of 10 tests fail (RED) — the regression test on the existing coordinator endpoint may already pass
  </acceptance_criteria>
  <done>RED state committed; task 2 turns them green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract helpers into _admin_host.py and update coordinator.py to import</name>
  <read_first>
    - crm/server/modules/coordinator.py (lines 290-365 — full helper bodies to move; also check for any other coordinator function that calls these and ensure they continue to work via import)
    - crm/server/modules/__init__.py (to confirm package layout)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §Authentication §Refactor recommendation
  </read_first>
  <action>Create new file `crm/server/modules/_admin_host.py` containing exactly the constants `_ADMIN_SURFACE` and `_LOCAL_HOSTS` and the three functions `_request_host`, `_configured_admin_hosts`, `_require_admin_host` copied verbatim from `crm/server/modules/coordinator.py:300-361`, along with their required imports (`from urllib.parse import urlparse`, `from fastapi import HTTPException, Request, status`, `from server.config import cfg`). Add a module docstring stating "Shared admin-host enforcement helpers; do not duplicate this logic — import from here." In `crm/server/modules/coordinator.py`: delete the inline definitions of `_ADMIN_SURFACE`, `_LOCAL_HOSTS`, `_request_host`, `_configured_admin_hosts`, `_require_admin_host`. Replace with a single import line `from server.modules._admin_host import (_ADMIN_SURFACE, _LOCAL_HOSTS, _request_host, _configured_admin_hosts, _require_admin_host)` near the other imports (preserve import ordering style: stdlib → third-party → server.* per existing file convention). Do NOT change any call sites in coordinator.py — they continue to reference the same names, now imported. Do NOT change function signatures or behavior. Run the existing coordinator test suite (whatever covers `/api/admin/coordinator/*`) to confirm no regression.</action>
  <verify>
    <automated>pytest tests/test_admin_host_helper.py -x 2>&1 | tail -15 && pytest tests/ -x -k "coordinator" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 10 tests in `tests/test_admin_host_helper.py` PASS (GREEN)
    - All existing `pytest tests/ -k "coordinator"` tests continue to PASS (no regression)
    - `grep -c 'def _require_admin_host' crm/server/modules/coordinator.py` returns 0 (the function moved out)
    - `grep -c 'def _require_admin_host' crm/server/modules/_admin_host.py` returns 1
    - `grep -c 'from server.modules._admin_host import' crm/server/modules/coordinator.py` returns 1
  </acceptance_criteria>
  <done>Helpers live in one place; coordinator.py imports them; Phase 5 admin-host contract intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Request → admin-only FastAPI router | Host header inspected before any handler runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Spoofing | Caller forges X-Forwarded-Host to claim admin host | mitigate | Helper uses `x-forwarded-host` only when set by trusted ingress; ingress strips client-supplied XFH per Phase 5 contract; helper unchanged from coordinator — regression suite confirms |
| T-07-18 | Tampering | Drift between coordinator and stress_test host enforcement | mitigate | Single shared module makes drift structurally impossible; test asserts coordinator imports rather than re-implements |
| T-07-19 | Elevation of Privilege | Local-hosts allow-list accidentally extended | accept | `_LOCAL_HOSTS = {"localhost", "127.0.0.1", "::1", "testserver"}` is unchanged from coordinator and constrained to dev/test hostnames |
</threat_model>

<verification>
- `pytest tests/test_admin_host_helper.py -x` green (10/10)
- `pytest tests/ -k "coordinator"` green (no Phase 5 regression)
- `pytest tests/ -x` (full suite) green
- `bash deploy/verify.sh` still passes
</verification>

<success_criteria>
- `crm/server/modules/_admin_host.py` exists with the three helpers + two constants
- `coordinator.py` imports them; no duplicate definitions
- Phase 5 admin-host enforcement behavior is bit-identical (regression suite proves it)
- All plan 04 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-oke-autoscaling-and-stress-demo/07-04-SUMMARY.md`
</output>
