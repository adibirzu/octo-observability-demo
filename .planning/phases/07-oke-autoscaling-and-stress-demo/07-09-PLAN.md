---
phase: 07-oke-autoscaling-and-stress-demo
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/la-saved-searches/oke-autoscaling-hpa-events.json
  - tools/la-saved-searches/oke-autoscaling-ca-events.json
  - tools/la-saved-searches/oke-autoscaling-kubelet-pressure.json
  - tools/la-saved-searches/oke-autoscaling-stress-audit.json
  - tools/la-saved-searches/oke-autoscaling-dashboard.json
autonomous: true
requirements: [SCALE-04]
tags: [log-analytics, dashboard, observability]
must_haves:
  truths:
    - "Four new Log Analytics saved searches drop into `tools/la-saved-searches/`; the existing `apply.sh` auto-discovers them (D-19)"
    - "A dashboard JSON `oke-autoscaling-dashboard.json` defines the 'OKE Autoscaling Timeline' dashboard linking the four saved searches"
    - "Stress audit search filters on `run_id is not null` to surface only stress-run events"
    - "Kubelet pressure search matches `NodeNotReady`, `ImagePullBackOff`, `OOMKilled`"
    - "No live OCIDs, IPs, or tenancy labels in any JSON"
  artifacts:
    - path: "tools/la-saved-searches/oke-autoscaling-hpa-events.json"
      provides: "Saved search: HPA scale events"
      contains: "hpa-controller"
    - path: "tools/la-saved-searches/oke-autoscaling-ca-events.json"
      provides: "Saved search: Cluster Autoscaler events"
      contains: "cluster-autoscaler"
    - path: "tools/la-saved-searches/oke-autoscaling-kubelet-pressure.json"
      provides: "Saved search: kubelet pressure events"
      contains: "NodeNotReady"
    - path: "tools/la-saved-searches/oke-autoscaling-stress-audit.json"
      provides: "Saved search: stress audit log"
      contains: "run_id"
    - path: "tools/la-saved-searches/oke-autoscaling-dashboard.json"
      provides: "Dashboard linking the four saved searches"
      contains: "OKE Autoscaling Timeline"
  key_links:
    - from: "Stress audit search"
      to: "Phase 7 audit log lines (plan 05)"
      via: "run_id field filter"
      pattern: "run_id"
    - from: "Dashboard JSON"
      to: "Four saved searches"
      via: "savedSearchId or query reference"
      pattern: "oke-autoscaling-"
---

<objective>
Ship the four Log Analytics saved searches and the "OKE Autoscaling
Timeline" dashboard from D-19. Files drop into the existing
`tools/la-saved-searches/` directory; the existing `apply.sh` auto-
discovers them (PATTERNS.md §LA saved-search apply — no apply.sh edits
needed).

Purpose: Log Analytics half of the Phase 7 scale narrative; time-aligned
with APM saved queries (plan 07) and Monitoring alarms (plan 08).

Output: 4 saved-search JSONs + 1 dashboard JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md
@.planning/phases/03-log-analytics-detection-reliability/

<interfaces>
<!-- Pattern analog: tools/la-saved-searches/errors-by-route.json (full file) -->
{
  "name": "<slug>",
  "description": "...",
  "displayName": "OCTO — ...",
  "queryString": "<Log Analytics query>",
  "widgetType": "BAR_CHART|LINE_CHART|TABLE"
}

<!-- Existing apply.sh auto-discovers *.json — no edit needed -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pytest assertions for LA saved searches + dashboard</name>
  <read_first>
    - tests/test_stress_demo_surface.py (extend)
    - tools/la-saved-searches/errors-by-route.json (analog)
    - tools/la-saved-searches/README.md (for dashboard JSON shape if mentioned)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md §D-19
  </read_first>
  <behavior>
    - test_la_oke_hpa_events_search() — `oke-autoscaling-hpa-events.json` parses, contains `hpa-controller` and `Kubernetes Logs`
    - test_la_oke_ca_events_search() — `oke-autoscaling-ca-events.json` parses, contains `cluster-autoscaler` (the add-on subsystem name)
    - test_la_oke_kubelet_pressure_search() — `oke-autoscaling-kubelet-pressure.json` parses, contains all three: `NodeNotReady`, `ImagePullBackOff`, `OOMKilled`
    - test_la_oke_stress_audit_search() — `oke-autoscaling-stress-audit.json` parses, contains `run_id` and filters where run_id is not null
    - test_la_oke_dashboard_links_four_searches() — `oke-autoscaling-dashboard.json` parses, contains `OKE Autoscaling Timeline` display name, references all four saved-search names (`oke-autoscaling-hpa-events`, `oke-autoscaling-ca-events`, `oke-autoscaling-kubelet-pressure`, `oke-autoscaling-stress-audit`)
    - test_la_oke_searches_no_live_ocids() — `grep -rE "ocid1\\." tools/la-saved-searches/oke-autoscaling-*.json` returns no matches
    - test_la_apply_sh_unchanged() — `tools/la-saved-searches/apply.sh` line count is the same as before this plan (the script auto-discovers; no edits should land in apply.sh from this plan) — store the line count via subprocess `git show HEAD:tools/la-saved-searches/apply.sh | wc -l` and compare; OR a softer check: assert `oke-autoscaling` substring does NOT appear in apply.sh (i.e., not hardcoded)
  </behavior>
  <action>Append 7 test functions to `tests/test_stress_demo_surface.py`. Commit `test(07-09): RED assertions for OKE autoscaling LA saved searches + dashboard`.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "la_oke" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 7 test functions defined; all fail
  </acceptance_criteria>
  <done>RED state committed.</done>
</task>

<task type="auto">
  <name>Task 2: Author 4 saved searches + dashboard JSON</name>
  <read_first>
    - tools/la-saved-searches/errors-by-route.json (full file)
    - tools/la-saved-searches/slow-checkout-spans.json (additional reference)
    - tools/la-saved-searches/trace-to-logs.json (additional reference)
    - tools/la-saved-searches/apply.sh (full file — confirm auto-discovery; do NOT edit)
    - tools/la-saved-searches/README.md (for any dashboard schema/convention)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md §D-19
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`tools/la-saved-searches/oke-autoscaling-*.json`
  </read_first>
  <action>Create the following files in `tools/la-saved-searches/`:

(1) `oke-autoscaling-hpa-events.json` — `name: "octo-oke-autoscaling-hpa-events"`, `displayName: "OCTO OKE — HPA scale events"`, `description: "..."`, `queryString: "'Log Source' = 'Kubernetes Logs' and Subsystem = 'hpa-controller' | stats count() by hpa_event, deployment, namespace | sort -Time"`, `widgetType: "TABLE"`.

(2) `oke-autoscaling-ca-events.json` — `name: "octo-oke-autoscaling-ca-events"`, `displayName: "OCTO OKE — Cluster Autoscaler events"`, `queryString` filtering on the cluster-autoscaler add-on log source/subsystem (verify exact OCI add-on log naming at apply time — illustrative: `'Log Source' = 'Kubernetes Logs' and Subsystem = 'cluster-autoscaler' | stats count() by event, node | sort -Time`), `widgetType: "TABLE"`.

(3) `oke-autoscaling-kubelet-pressure.json` — `queryString: "'Log Source' = 'Kubernetes Logs' and (Message like '%NodeNotReady%' or Message like '%ImagePullBackOff%' or Message like '%OOMKilled%') | stats count() by node, reason | sort -count"`, `widgetType: "BAR_CHART"`.

(4) `oke-autoscaling-stress-audit.json` — `queryString: "'Log Source' = 'octo-shop-app-json' and run_id is not null | stats count() by status, scenario, run_id | sort -Time"` (the existing parser accepts `run_id` as first-class per Phase 1 OBS-02 contract), `widgetType: "TABLE"`.

(5) `oke-autoscaling-dashboard.json` — top-level object with `displayName: "OKE Autoscaling Timeline"`, `description`, `tiles` array with one entry per saved search (each tile references the saved search by name and assigns a grid position), `parameters` array if time-range parameterization is supported. Schema may need to mirror an OCI Log Analytics dashboard export format — verify against `tools/la-saved-searches/README.md` if it documents one; otherwise use a reasonable shape that includes the names of all four saved searches as string references (the test asserts substring presence, not full dashboard schema fidelity).

Do NOT edit `tools/la-saved-searches/apply.sh` — its auto-discovery is the contract this plan relies on. Do NOT include live OCIDs, IPs, tenancy labels.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "la_oke" 2>&1 | tail -15 && for f in tools/la-saved-searches/oke-autoscaling-*.json; do python -c "import json; json.load(open('$f'))"; done && python tools/la-saved-searches/smoke-test.py 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 7 la_oke tests PASS
    - All 5 new JSON files parse via `json.load`
    - `tools/la-saved-searches/smoke-test.py` (existing offline validator) reports no errors for the new files
    - `grep -rE 'ocid1\.' tools/la-saved-searches/oke-autoscaling-*.json` returns no matches
    - `grep -rE '([0-9]{1,3}\.){3}[0-9]{1,3}' tools/la-saved-searches/oke-autoscaling-*.json` returns no matches
    - Diff to `tools/la-saved-searches/apply.sh` is zero lines (auto-discovery used; no script edit)
  </acceptance_criteria>
  <done>Five new JSON files drop into the la-saved-searches directory; existing apply.sh discovers them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → OCI Log Analytics workspace | apply.sh imports saved searches under operator profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-42 | Information Disclosure | Live workspace OCID in dashboard JSON | mitigate | Grep test asserts; envsubst placeholder if needed at apply time |
| T-07-43 | Tampering | Saved-search query injection | accept | Saved-search query strings are static and reviewed in PR; no user-controlled inputs |
</threat_model>

<verification>
- `pytest tests/test_stress_demo_surface.py -x -k "la_oke"` green
- `python tools/la-saved-searches/smoke-test.py` clean
- No leaked OCIDs / IPs / tenancy labels
</verification>

<success_criteria>
- 4 saved searches + 1 dashboard JSON drop in; existing apply.sh discovers
- Stress audit search filters on run_id (cross-channel pivot key with plan 05)
- All plan 09 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-oke-autoscaling-and-stress-demo/07-09-SUMMARY.md`
</output>
