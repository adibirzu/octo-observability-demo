---
phase: 07-oke-autoscaling-and-stress-demo
plan: 10
type: execute
wave: 3
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06, 07-07, 07-08, 07-09]
files_modified:
  - site/workshop/lab-11-oke-autoscaling.md
  - site/operations/stress-demo-lb-routing.md
  - mkdocs.yml
  - tests/test_unified_deploy_surface.py
autonomous: true
requirements: [SCALE-01, SCALE-02, SCALE-03, SCALE-04]
tags: [docs, workshop, runbook, mkdocs]
must_haves:
  truths:
    - "Workshop Lab 11 walks the full Phase 7 demo end-to-end: baseline → trigger → HPA → CA → alarm → APM/Logan drilldown → cool-down (D-22)"
    - "Lab 11 cross-links Labs 01, 05, 09 (D-22)"
    - "Lab 11 includes the four D-20 external drilldown link blocks (`lm`, `phoenix`, `openlit`, `grafana` .<DNS_DOMAIN>)"
    - "Operator-gated live-run sections are clearly marked (CA apply, LB header rule, prometheus-adapter install) per CONTEXT.md `<deferred>`"
    - "`stress-demo-lb-routing.md` documents the OCI LB header-routing rule for `X-Octo-Stress-Target: oke` (D-09)"
    - "`mkdocs.yml` nav includes Lab 11 + the operations runbook in the right sections"
    - "`tests/test_unified_deploy_surface.py` extended with stress-runner manifest + CA script presence checks (PATTERNS.md §`tests/test_unified_deploy_surface.py (EDIT)`)"
    - "`mkdocs build --strict` passes (DOC-03 carries through)"
  artifacts:
    - path: "site/workshop/lab-11-oke-autoscaling.md"
      provides: "Workshop Lab 11 narrative"
      contains: "Lab 11"
      min_lines: 120
    - path: "site/operations/stress-demo-lb-routing.md"
      provides: "Operator runbook for LB header-routing rule"
      contains: "X-Octo-Stress-Target"
    - path: "mkdocs.yml"
      provides: "Lab 11 + runbook nav entries"
      contains: "lab-11-oke-autoscaling.md"
    - path: "tests/test_unified_deploy_surface.py"
      provides: "Extended manifest surface coverage for stress-runner + CA"
      contains: "stress-runner"
  key_links:
    - from: "Lab 11"
      to: "Admin stress-test page (plan 06)"
      via: "step 2 instructs operator to open /admin/stress-test on admin host"
      pattern: "/admin/stress-test"
    - from: "Lab 11"
      to: "APM saved queries + Monitoring alarms + Logan dashboard"
      via: "step 3-6 link to plans 07/08/09 artifacts"
      pattern: "octo-oke"
    - from: "stress-demo-lb-routing.md"
      to: "Cluster Autoscaler runbook (plan 02 script)"
      via: "documents both must be applied in the same operator window"
      pattern: "configure-cluster-autoscaler.sh"
---

<objective>
Close the Phase 7 narrative with operator-facing docs:

1. Workshop Lab 11 — the full walking-tour of the elasticity demo,
   cross-linked to Labs 01/05/09 per D-22.
2. LB header-routing runbook — documents the operator steps to apply the
   `X-Octo-Stress-Target: oke` LB rule (D-09; live apply gated).
3. MkDocs nav update so both files appear in the published site.
4. Unified deploy surface test extension — catches the new
   stress-runner manifests + CA script in `deploy/verify.sh`-style
   assertions (PATTERNS.md §`tests/test_unified_deploy_surface.py (EDIT)`).

Purpose: Without this plan, the Phase 7 assets exist but are not
discoverable by workshop attendees or by the canonical surface test.

Output: Two new markdown files + mkdocs.yml edit + test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md

<interfaces>
<!-- Workshop lab analogs -->
- site/workshop/lab-01-first-trace.md
- site/workshop/lab-05-metric-and-alarm.md
- site/workshop/lab-09-chaos-drill.md

<!-- Operations runbook analog -->
- site/operations/chaos.md
- site/operations/alarms.md

<!-- Unified deploy surface test -->
tests/test_unified_deploy_surface.py:1-80 (ROOT + read_text + manifest assertions)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — assertions for workshop + runbook + surface test extension</name>
  <read_first>
    - tests/test_stress_demo_surface.py (extend with lab + runbook checks)
    - tests/test_unified_deploy_surface.py (review existing tests before extending)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md §D-22, §D-09
  </read_first>
  <behavior>
    - test_lab11_file_exists() — `site/workshop/lab-11-oke-autoscaling.md` exists
    - test_lab11_cross_links_labs() — file contains links to `lab-01`, `lab-05`, `lab-09` (markdown links or relative file refs)
    - test_lab11_includes_drilldown_hosts() — file contains all 4 of `lm.<DNS_DOMAIN>`, `phoenix.<DNS_DOMAIN>`, `openlit.<DNS_DOMAIN>`, `grafana.<DNS_DOMAIN>`
    - test_lab11_walks_full_arc() — file contains all 7 step markers (baseline, trigger, HPA, CA, alarm, drill, cool-down) — assert presence of each keyword in the document
    - test_lab11_admin_path_referenced() — file contains `/admin/stress-test` (the admin page from plan 06)
    - test_lab11_run_id_pivot_pattern() — file contains `run_id` referenced as the cross-channel pivot key
    - test_lb_routing_runbook_exists() — `site/operations/stress-demo-lb-routing.md` exists, contains `X-Octo-Stress-Target` and `oci lb routing-policy`
    - test_lb_routing_runbook_no_live_ocids() — `grep -E "ocid1\\." site/operations/stress-demo-lb-routing.md` no matches
    - test_mkdocs_nav_includes_lab11() — `mkdocs.yml` contains `lab-11-oke-autoscaling.md`
    - test_mkdocs_nav_includes_runbook() — `mkdocs.yml` contains `stress-demo-lb-routing.md`
    - test_unified_deploy_surface_stress_runner_check() — `tests/test_unified_deploy_surface.py` contains a function asserting `deploy/k8s/oke/stress-runner/deployment.yaml` exists (the test extension lands inside the existing surface-test file)
    - test_unified_deploy_surface_ca_script_check() — same file contains assertion for `deploy/oke/configure-cluster-autoscaler.sh` existence + executable
  </behavior>
  <action>Append the 12 test functions above to `tests/test_stress_demo_surface.py` (or split: the test_unified_deploy_* ones may instead be added to `tests/test_unified_deploy_surface.py` directly — planner-choice; either way they must execute under `pytest tests/`). Commit `test(07-10): RED assertions for Lab 11 + LB runbook + unified surface coverage`.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py tests/test_unified_deploy_surface.py -x -k "lab11 or lb_routing or mkdocs or unified_deploy_surface" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - 12 test functions defined; all fail (docs do not yet exist; surface test not yet extended)
  </acceptance_criteria>
  <done>RED state committed.</done>
</task>

<task type="auto">
  <name>Task 2: Author Lab 11 + LB routing runbook</name>
  <read_first>
    - site/workshop/lab-09-chaos-drill.md (lines 1-80 — header + objective + run_id propagation pattern; primary structural analog)
    - site/workshop/lab-05-metric-and-alarm.md (lines 50-80 — alarm CLI shape for drilldown sections)
    - site/workshop/lab-01-first-trace.md (header/style reference)
    - site/operations/chaos.md (operator runbook analog for LB routing doc)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md §D-22, §D-09, §D-20
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`site/workshop/lab-11-oke-autoscaling.md` and §`site/operations/stress-demo-lb-routing.md`
  </read_first>
  <action>Create `site/workshop/lab-11-oke-autoscaling.md` (≥ 120 lines) following the lab-09 structural template:

- **Header**: `# Lab 11 — OKE Autoscaling and Stress Demo` plus a one-sentence description.
- **Objective**: Two short paragraphs: (a) what the workshop attendee will see; (b) the cross-channel run_id pivot — APM ↔ Monitoring ↔ Log Analytics, all keyed by the same `run_id`.
- **Time budget**: ~15 minutes.
- **Prerequisites**: Labs 01, 05, 09 completed (cross-links here). Operator window must have applied: Cluster Autoscaler add-on (plan 02), prometheus-adapter (plan 02), LB header-routing rule (this plan's runbook), stress-runner Helm release (plan 03 with `stressRunner.enabled=true`).
- **Steps (7)**:
  1. Verify baseline: 2 shop pods, 2 nodes (kubectl + OCI Console screenshots).
  2. Trigger Medium preset (50 RPS / 3 min) from `/admin/stress-test` on `admin.${DNS_DOMAIN}` — show the page, the form, the `Apply stress run` button, the response with `run_id`.
  3. Watch HPA add pods: OCI Console + `kubectl get hpa -w` + the APM saved query `octo-oke-pod-count`. Expected: with the Medium preset (50 RPS / 3 min), shop scales 2→~6 within 60s; the HPA ceiling is 10 (`maxReplicas: 10` per Plan 07-01) and the Medium preset is calibrated to drive ~6 pods, leaving 10 as headroom for the Heavy preset (120 RPS).
  4. Watch Cluster Autoscaler add a node: `kubectl get nodes -w` + Monitoring alarm `OCTO — Shop CPU saturation high` fires (expected behavior — alarm-path validation).
  5. Drill into APM: latency p95 saved query `octo-oke-latency-percentiles`; trace-to-new-pod query `octo-oke-trace-to-new-pods`.
  6. Drill into Log Analytics: dashboard "OKE Autoscaling Timeline" with the four saved searches; filter by `run_id = <your_run_id>`.
  7. Cool-down (5 min): watch scale-down to baseline; alarm clears.
- **Cross-links**: Lab 01 (`/site/workshop/lab-01-first-trace.md` — first trace pattern), Lab 05 (`/site/workshop/lab-05-metric-and-alarm.md` — alarm pattern), Lab 09 (`/site/workshop/lab-09-chaos-drill.md` — run_id pivot pattern).
- **External drilldown links section (D-20)**: 4 list items linking to `lm.<DNS_DOMAIN>`, `phoenix.<DNS_DOMAIN>`, `openlit.<DNS_DOMAIN>`, `grafana.<DNS_DOMAIN>` — each with a one-line "use when..." description.
- **Operator-gated sections**: A clearly marked "Live demo" callout box at the top stating that steps 2–7 require the operator window prerequisites to be applied; otherwise the lab is read-only walkthrough only.

Create `site/operations/stress-demo-lb-routing.md` (≥ 60 lines) following `site/operations/chaos.md` shape:

- **Purpose**: Document the OCI Flexible LB routing policy that pins requests with header `X-Octo-Stress-Target: oke` to the OKE backend set, leaving VM backends untouched (DEPLOY-03 preservation per D-09).
- **Prerequisites**: List the LB OCID env var (placeholder, never live), the OCI CLI profile, operator window approval.
- **Steps**:
  1. Inspect current routing policy: `oci lb routing-policy list --load-balancer-id "${OCTO_LB_OCID}"`.
  2. Compose the new rule via `oci lb routing-policy update` with the rule expression `http.request.headers["X-Octo-Stress-Target"] == 'oke'` mapped to the OKE backend-set.
  3. Apply (require operator confirm).
  4. Verify: `curl -H "X-Octo-Stress-Target: oke" https://shop.${DNS_DOMAIN}/api/products -v` and inspect response server hint to confirm OKE backend.
- **Rollback**: How to delete the rule or set `isEnabled: false`.
- **Cross-link** to `deploy/oke/configure-cluster-autoscaler.sh` — these two operator actions ship together (CONTEXT.md `<deferred>`).

NO live OCIDs, IPs, secrets, or tenancy labels. Use `${OCTO_LB_OCID}` style envsubst placeholders throughout.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "lab11 or lb_routing" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All lab11_* + lb_routing_* tests PASS
    - `wc -l site/workshop/lab-11-oke-autoscaling.md` ≥ 120
    - `wc -l site/operations/stress-demo-lb-routing.md` ≥ 60
    - `grep -E '([0-9]{1,3}\.){3}[0-9]{1,3}' site/workshop/lab-11-oke-autoscaling.md site/operations/stress-demo-lb-routing.md` returns no matches
    - `grep -E 'ocid1\.' site/workshop/lab-11-oke-autoscaling.md site/operations/stress-demo-lb-routing.md` returns no matches
  </acceptance_criteria>
  <done>Lab 11 + runbook ship; cross-links functional.</done>
</task>

<task type="auto">
  <name>Task 3: Update mkdocs.yml nav + extend unified deploy surface test</name>
  <read_first>
    - mkdocs.yml (find the workshop + operations nav sections)
    - tests/test_unified_deploy_surface.py (lines 1-80 — existing surface test pattern)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md §`tests/test_unified_deploy_surface.py (EDIT)`
  </read_first>
  <action>(a) Edit `mkdocs.yml`: locate the workshop nav section, add a new entry `- Lab 11 — OKE Autoscaling: workshop/lab-11-oke-autoscaling.md` immediately after the Lab 09/10 entry; locate the operations nav section, add `- Stress demo LB routing: operations/stress-demo-lb-routing.md`. Preserve existing YAML indentation style. (b) Edit `tests/test_unified_deploy_surface.py`: add two new test functions to the existing file (do NOT create a new file): `test_stress_runner_manifest_in_unified_surface()` — assert `(ROOT / "deploy/k8s/oke/stress-runner/deployment.yaml").exists()` and contains `octo-stress-runner`; `test_configure_cluster_autoscaler_in_unified_surface()` — assert `(ROOT / "deploy/oke/configure-cluster-autoscaler.sh").exists()` AND `os.access(p, os.X_OK)` AND contains `install-addon`. Mirror the existing `test_unified_deploy_wrapper_exists` shape from lines 27-34. Append, do not refactor.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py tests/test_unified_deploy_surface.py -x -k "mkdocs or unified_deploy_surface" 2>&1 | tail -15 && mkdocs build --strict 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test_mkdocs_nav_includes_lab11`, `test_mkdocs_nav_includes_runbook`, `test_unified_deploy_surface_stress_runner_check`, `test_unified_deploy_surface_ca_script_check` ALL PASS
    - `mkdocs build --strict` exits 0 (no broken links, no missing files)
    - `pytest tests/test_unified_deploy_surface.py -x` shows the two new tests PASS alongside all pre-existing tests
  </acceptance_criteria>
  <done>Workshop attendees see Lab 11 in the published site; surface test catches stress-runner + CA artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published docs site → public readers | Workshop content must not leak live OCIDs / IPs / tenancy labels |
| Repo operator runbook → live OCI LB | Runbook documents mutations that an operator may apply outside Claude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-44 | Information Disclosure | Workshop or runbook leaks live OCIDs / IPs / tenancy | mitigate | Test grep gates; envsubst placeholders only; SEC-04 enforced |
| T-07-45 | Tampering | Runbook misleads operator into applying rule too broadly | accept | Runbook explicitly scopes to the OCTO LB OCID env var; rollback section documents disable; reviewer-required PR before publication |
| T-07-46 | Repudiation | Live LB rule applied without record | mitigate | OCI LB CLI logs to audit log; runbook instructs operator to record run_id in PR comment when applied during a workshop |
</threat_model>

<verification>
- `pytest tests/test_stress_demo_surface.py tests/test_unified_deploy_surface.py -x -k "lab11 or lb_routing or mkdocs or unified_deploy_surface"` green
- `mkdocs build --strict` exits 0
- `pytest tests/ -q` full suite green (Phase 7 + all prior phases)
- `bash deploy/verify.sh` continues to pass
</verification>

<success_criteria>
- Lab 11 walks the full demo with cross-links + drilldowns
- LB routing runbook documents the operator-gated rule
- mkdocs build strict-green
- Unified deploy surface test catches new artifacts
- All plan 10 tests green
- Entire Phase 7 test surface green: `pytest tests/test_stress_demo_surface.py tests/test_unified_deploy_surface.py tests/test_admin_host_helper.py -q`
</success_criteria>

<output>
After completion, create `.planning/phases/07-oke-autoscaling-and-stress-demo/07-10-SUMMARY.md`
</output>
