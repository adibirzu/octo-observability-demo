---
phase: 07-oke-autoscaling-and-stress-demo
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/monitoring-alarms/README.md
  - tools/monitoring-alarms/octo-high-cpu-saturation.json
  - tools/monitoring-alarms/octo-hpa-at-max-replicas.json
  - tools/monitoring-alarms/apply.sh
autonomous: true
requirements: [SCALE-04]
tags: [monitoring, alarms, oci, observability]
must_haves:
  truths:
    - "Two OCI Monitoring alarm definitions ship as JSON specs (D-18): High CPU saturation, HPA at max replicas"
    - "Both alarms query the `octo_apm_demo` namespace exclusively (Phase 1 OBS-05)"
    - "`apply.sh` operator script wraps `oci monitoring alarm create-or-update` with dry-run default + confirm-on-apply (parallels other apply scripts)"
    - "Alarm queries use MQL syntax compatible with the existing `tools/la-saved-searches/apply.sh` pattern"
    - "No live OCIDs, compartment IDs, IPs, or notification topic OCIDs committed (operator supplies at apply time via env)"
  artifacts:
    - path: "tools/monitoring-alarms/octo-high-cpu-saturation.json"
      provides: "Alarm: shop_cpu_saturation_pct > 80 for 2 min (D-18 #1)"
      contains: "shop_cpu_saturation_pct"
    - path: "tools/monitoring-alarms/octo-hpa-at-max-replicas.json"
      provides: "Alarm: pod count == maxReplicas for > 5 min (D-18 #2)"
      contains: "maxReplicas"
    - path: "tools/monitoring-alarms/apply.sh"
      provides: "Operator-gated alarm create/update script"
      contains: "APPLY:=false"
  key_links:
    - from: "Alarm JSON queries"
      to: "octo_apm_demo Monitoring namespace"
      via: "query field references namespace"
      pattern: "octo_apm_demo"
    - from: "Alarms"
      to: "D-17 metrics (plan 05 publisher)"
      via: "shop_cpu_saturation_pct + shop_pod_count metrics"
      pattern: "shop_cpu_saturation_pct"
---

<objective>
Ship the two OCI Monitoring alarms from D-18 as committed JSON specs in a
new `tools/monitoring-alarms/` directory, plus an apply script. Alarms
query the `octo_apm_demo` namespace exclusively and reference the D-17
metrics published by `shop/server/observability/oci_monitoring.py` (plan
05). Live apply happens in operator window.

Purpose: Reproducible alarm definitions for the Phase 7 scale narrative.
The "High CPU saturation" alarm doubles as alarm-path validation (D-18
explicit: expected to fire on every stress run).

Output: New directory + 2 JSON files + README + apply.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-RESEARCH.md
@.planning/phases/07-oke-autoscaling-and-stress-demo/07-PATTERNS.md

<interfaces>
<!-- OCI Monitoring alarm create-or-update payload shape (MQL query) -->
{
  "displayName": "...",
  "metricCompartmentId": "${COMPARTMENT_ID}",  // envsubst placeholder
  "namespace": "octo_apm_demo",
  "query": "<MQL expression>",
  "severity": "WARNING|CRITICAL",
  "pendingDuration": "PT2M",
  "destinations": ["${NOTIFICATION_TOPIC_OCID}"],  // envsubst placeholder
  "isEnabled": true,
  "body": "..."
}

<!-- Pattern analog: tools/la-saved-searches/apply.sh shape -->
#!/usr/bin/env bash
set -euo pipefail
: "${OCI_PROFILE:=demo}"
: "${APPLY:=false}"
require_tool oci; require_tool jq
for json in *.json; do
  envsubst < "$json" > /tmp/...
  if [[ "$APPLY" == "true" ]]; then oci monitoring alarm create-or-update ...; else echo "[dry-run]"; fi
done
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pytest assertions for monitoring alarms</name>
  <read_first>
    - tests/test_stress_demo_surface.py (extend)
    - tools/la-saved-searches/apply.sh (apply script analog)
  </read_first>
  <behavior>
    - test_monitoring_alarms_directory_exists() — `tools/monitoring-alarms/` exists with README.md
    - test_high_cpu_alarm_valid() — `octo-high-cpu-saturation.json` parses, contains `shop_cpu_saturation_pct`, `> 80`, `PT2M` (2-min pending duration), `namespace: "octo_apm_demo"`
    - test_hpa_max_replicas_alarm_valid() — `octo-hpa-at-max-replicas.json` parses, contains `shop_pod_count` and references max-replicas threshold, `PT5M` (5-min pending duration)
    - test_monitoring_alarms_apply_script_dry_run_default() — `apply.sh` exists, executable, `: "${APPLY:=false}"`, contains `read -p` confirm
    - test_monitoring_alarms_use_envsubst_for_ocids() — JSON files contain `${COMPARTMENT_ID}` and `${NOTIFICATION_TOPIC_OCID}` (or similar envsubst placeholders)
    - test_monitoring_alarms_no_live_ocids() — `grep -rE "ocid1\\." tools/monitoring-alarms/` returns no matches
  </behavior>
  <action>Append 6 test functions to `tests/test_stress_demo_surface.py`. JSON tests use `json.load`. Commit `test(07-08): RED assertions for monitoring alarms`.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "monitoring_alarms or high_cpu_alarm or hpa_max_replicas_alarm" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 6 test functions defined; all fail
  </acceptance_criteria>
  <done>RED state committed.</done>
</task>

<task type="auto">
  <name>Task 2: Author monitoring alarms + apply.sh</name>
  <read_first>
    - tools/la-saved-searches/apply.sh (full file)
    - deploy/oke/install-oci-kubernetes-monitoring.sh (header + dry-run + confirm pattern)
    - .planning/phases/07-oke-autoscaling-and-stress-demo/07-CONTEXT.md §D-18, §D-17
    - site/workshop/lab-05-metric-and-alarm.md (lines 50-80 if present, for alarm CLI shape reference)
  </read_first>
  <action>Create `tools/monitoring-alarms/` directory with:

(1) `README.md` — documents the directory ships OCI Monitoring alarm JSON specs; `apply.sh` calls `oci monitoring alarm create-or-update`; env vars `COMPARTMENT_ID`, `NOTIFICATION_TOPIC_OCID` required; dry-run default; live apply during operator window.

(2) `octo-high-cpu-saturation.json` — JSON object with `displayName: "OCTO — Shop CPU saturation high"`, `metricCompartmentId: "${COMPARTMENT_ID}"`, `namespace: "octo_apm_demo"`, `query: "shop_cpu_saturation_pct[1m].mean() > 80"`, `severity: "WARNING"`, `pendingDuration: "PT2M"`, `destinations: ["${NOTIFICATION_TOPIC_OCID}"]`, `isEnabled: true`, `body: "Shop pod CPU saturation > 80% for 2 minutes. Expected during stress runs; investigate if firing outside a stress run window."`, optional `resolveBody`.

(3) `octo-hpa-at-max-replicas.json` — `displayName: "OCTO — Shop HPA at maxReplicas"`, `metricCompartmentId: "${COMPARTMENT_ID}"`, `namespace: "octo_apm_demo"`, `query: "shop_pod_count[1m].mean() >= 10"` (the maxReplicas threshold from plan 01 D-03), `severity: "CRITICAL"`, `pendingDuration: "PT5M"`, `destinations: ["${NOTIFICATION_TOPIC_OCID}"]`, `isEnabled: true`, `body: "Shop deployment has been at maxReplicas (10) for 5 minutes. Cluster may need capacity beyond Cluster Autoscaler max=4 nodes."`. The body should include a comment that the value `10` must match `shop.autoscaling.maxReplicas` in `deploy/helm/octo-apm-demo/values.yaml` — keep the two in sync. Include `metadata.maxReplicas` reference in the README so the test can also assert the cross-file invariant.

(4) `apply.sh` — clone the structure of `tools/la-saved-searches/apply.sh`. Body: iterate `*.json` in script directory, run each through `envsubst` to a temp file (resolves `${COMPARTMENT_ID}` and `${NOTIFICATION_TOPIC_OCID}` from operator env), if `APPLY=false` echo `[dry-run] would call oci monitoring alarm create-or-update --from-json file://$tmp ...`, else require `read -p "Type APPLY to confirm: " CONFIRM` and call `oci monitoring alarm create --from-json file://$tmp` (or update — list-alarms first to detect existence, mirroring the CA script idempotency pattern). Make executable. Hard-fail if required env vars unset.

Do NOT include any live OCIDs, IPs, or tenancy labels.</action>
  <verify>
    <automated>pytest tests/test_stress_demo_surface.py -x -k "monitoring_alarms or high_cpu_alarm or hpa_max" 2>&1 | tail -15 && for f in tools/monitoring-alarms/*.json; do python -c "import json; json.load(open('$f'))"; done && bash -n tools/monitoring-alarms/apply.sh</automated>
  </verify>
  <acceptance_criteria>
    - All 6 monitoring_alarms tests PASS
    - Both JSON files parse via `json.load`
    - `bash -n tools/monitoring-alarms/apply.sh` exits 0
    - `tools/monitoring-alarms/apply.sh --help` exits 0
    - `grep -rE 'ocid1\.' tools/monitoring-alarms/` returns no matches
    - `grep -rE '([0-9]{1,3}\.){3}[0-9]{1,3}' tools/monitoring-alarms/` returns no matches
    - Cross-file invariant: maxReplicas value in `octo-hpa-at-max-replicas.json` query matches `shop.autoscaling.maxReplicas` in `deploy/helm/octo-apm-demo/values.yaml` (manual check now; turn into a test in plan 08 task 2 if convenient — add `test_hpa_max_replicas_value_matches_helm_values()` as bonus assertion)
  </acceptance_criteria>
  <done>Two alarm JSONs committed; apply script ready for operator window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → OCI Monitoring | apply.sh issues `oci monitoring alarm create-or-update` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-39 | Information Disclosure | Live notification topic OCIDs committed | mitigate | envsubst placeholders only; test asserts no `ocid1.` substring |
| T-07-40 | Tampering | apply.sh accidentally mutates non-OCTO compartment | mitigate | `${COMPARTMENT_ID}` hard-required; confirm prompt before live call |
| T-07-41 | Denial of Service | Alarm spam from misconfigured pendingDuration | accept | PT2M/PT5M values reviewed at operator window; SEV-WARNING/CRITICAL routing handled in topic config |
</threat_model>

<verification>
- `pytest tests/test_stress_demo_surface.py -x -k "monitoring_alarms or alarm"` green
- Both JSONs parse
- apply.sh dry-runs cleanly without OCI credentials
- No leaked OCIDs / IPs / secrets
</verification>

<success_criteria>
- Two alarms target `octo_apm_demo` namespace
- envsubst-based OCIDs (no live values committed)
- Apply script idempotent + dry-run + confirm
- All plan 08 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-oke-autoscaling-and-stress-demo/07-08-SUMMARY.md`
</output>
