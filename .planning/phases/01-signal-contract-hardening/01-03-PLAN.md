---
phase: 01-signal-contract-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java
  - services/apm-java-demo/src/main/java/com/octo/apmdemo/OtelSupport.java
  - services/apm-java-demo/src/test/java/com/octo/apmdemo/AppTest.java
  - services/async-worker/src/octo_async_worker/telemetry.py
  - services/load-control/src/octo_load_control/telemetry.py
  - services/object-pipeline/src/octo_object_pipeline/telemetry.py
  - services/remediator/src/octo_remediator/telemetry.py
  - services/edge-fuzz/src/octo_edge_fuzz/telemetry.py
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
user_setup: []
must_haves:
  truths:
    - "Java structured payment events include trace, span, request, workflow, service, deployment, order, payment gateway, processor, network, and token-safe payment fields."
    - "Support-service telemetry helpers include service.instance.id and preserve run_id, workflow_id, and request_id attributes supplied to script_span."
    - "Java and support-service telemetry continue exporting to the existing OCI APM endpoint without hardcoded secrets."
  artifacts:
    - path: services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java
      provides: "Java structured stdout event contract"
      contains: "oracleApmTraceId"
    - path: services/apm-java-demo/src/test/java/com/octo/apmdemo/AppTest.java
      provides: "Java structured log contract tests"
      contains: "request_id"
    - path: services/async-worker/src/octo_async_worker/telemetry.py
      provides: "Async worker OTel resource identity"
      contains: "service.instance.id"
    - path: services/load-control/src/octo_load_control/telemetry.py
      provides: "Load-control OTel resource identity"
      contains: "service.instance.id"
  key_links:
    - from: services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java
      to: services/apm-java-demo/src/test/java/com/octo/apmdemo/AppTest.java
      via: "structured event assertions"
      pattern: "java_payment_authorize"
    - from: services/async-worker/src/octo_async_worker/telemetry.py
      to: deploy/k8s/oke
      via: "OTEL_RESOURCE_ATTRIBUTES and service identity envs"
      pattern: "OTEL_RESOURCE_ATTRIBUTES"
---

<objective>
Harden Java payment sidecar and support-service telemetry so non-FastAPI spans
and logs carry the same service and correlation contract as Shop and CRM.

Purpose: Complete APM topology and Log Analytics coverage requires Java and
background services to emit the same resource and correlation identifiers.
Output: Java structured log improvements, support-service telemetry resource
identity improvements, and targeted tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-signal-contract-hardening/01-CONTEXT.md
@.planning/phases/01-signal-contract-hardening/01-RESEARCH.md
@.planning/phases/01-signal-contract-hardening/01-PATTERNS.md
@site/architecture/correlation-contract.md
@services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java
@services/apm-java-demo/src/main/java/com/octo/apmdemo/OtelSupport.java
@services/apm-java-demo/src/main/java/com/octo/apmdemo/PaymentRailSimulator.java
@services/apm-java-demo/src/test/java/com/octo/apmdemo/AppTest.java
@services/async-worker/src/octo_async_worker/telemetry.py
@services/load-control/src/octo_load_control/telemetry.py
@services/object-pipeline/src/octo_object_pipeline/telemetry.py
@services/remediator/src/octo_remediator/telemetry.py
@services/edge-fuzz/src/octo_edge_fuzz/telemetry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add request/workflow/service aliases to Java structured events</name>
  <files>services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java, services/apm-java-demo/src/test/java/com/octo/apmdemo/AppTest.java</files>
  <read_first>services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java, services/apm-java-demo/src/main/java/com/octo/apmdemo/OtelSupport.java, services/apm-java-demo/src/main/java/com/octo/apmdemo/PaymentRailSimulator.java, services/apm-java-demo/src/test/java/com/octo/apmdemo/AppTest.java, site/architecture/correlation-contract.md</read_first>
  <action>Update Java `emitStructuredEvent` so stdout JSON includes dotted and alias fields for `service.name`, `service.namespace`, `deployment.environment`, `request_id`, `workflow_id`, `workflow_step`, `payment.gateway.request_id`, `payment.processor.name`, `payment.network`, `payment.processor.response_code`, `payment.network.transaction_id`, `orders.order_id`, and `payment.token.safe` when those values are available from request headers or payload fields. Keep existing `service_name`, `service_namespace`, `trace_id`, `span_id`, `oracleApmTraceId`, and `oracleApmSpanId`. Extend `traceMdcFilter` to put `X-Request-Id`, `X-Workflow-Id`, and `X-Run-Id` into MDC and remove them in `finally`. Add AppTest assertions that a payment authorize request with `X-Request-Id` and `X-Workflow-Id` emits those fields, `service.name`, `service.namespace`, and `payment.token.safe` in stdout JSON.</action>
  <verify>cd services/apm-java-demo && mvn test</verify>
  <acceptance_criteria>
    - `App.java` contains `request_id`.
    - `App.java` contains `workflow_id`.
    - `App.java` contains `service.name`.
    - `AppTest.java` asserts `payment.token.safe`.
    - `cd services/apm-java-demo && mvn test` exits 0.
  </acceptance_criteria>
  <done>Java stdout events can be promoted by Log Analytics and joined to APM traces and payment searches.</done>
</task>

<task type="auto">
  <name>Task 2: Add service.instance.id to support-service telemetry helpers</name>
  <files>services/async-worker/src/octo_async_worker/telemetry.py, services/load-control/src/octo_load_control/telemetry.py, services/object-pipeline/src/octo_object_pipeline/telemetry.py, services/remediator/src/octo_remediator/telemetry.py, services/edge-fuzz/src/octo_edge_fuzz/telemetry.py</files>
  <read_first>services/async-worker/src/octo_async_worker/telemetry.py, services/load-control/src/octo_load_control/telemetry.py, services/object-pipeline/src/octo_object_pipeline/telemetry.py, services/remediator/src/octo_remediator/telemetry.py, services/edge-fuzz/src/octo_edge_fuzz/telemetry.py, deploy/k8s/oke/common/namespaces.yaml, deploy/k8s/oke/shop/deployment.yaml, deploy/k8s/oke/crm/deployment.yaml, site/architecture/correlation-contract.md</read_first>
  <action>In each support-service `telemetry.py`, add `service.instance.id` to the resource attributes. Use `SERVICE_INSTANCE_ID`, then `POD_NAME`, then `HOSTNAME`, then `unknown` as the fallback chain. Add `service.namespace` default `octo`, `deployment.environment` from `APP_ENV` or `ENVIRONMENT`, and preserve existing `OTEL_RESOURCE_ATTRIBUTES` overrides. Ensure `script_span` keeps caller-supplied `run_id`, `workflow_id`, and `request_id` attributes without renaming them.</action>
  <verify>python3 -m pytest -q services/load-control services/remediator services/object-pipeline services/edge-fuzz services/async-worker</verify>
  <acceptance_criteria>
    - Each listed telemetry file contains `service.instance.id`.
    - Each listed telemetry file contains `SERVICE_INSTANCE_ID`.
    - Each listed telemetry file still contains `OTEL_RESOURCE_ATTRIBUTES`.
    - Targeted service pytest command exits 0, or missing optional local service test environments are documented in the summary with the import error.
  </acceptance_criteria>
  <done>Support services show stable instance identity in APM resources and can preserve run/workflow/request pivots.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run global signal inventory for Java and support services</name>
  <files>tests/test_signal_contract_inventory.py</files>
  <read_first>tests/test_signal_contract_inventory.py, services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java, services/async-worker/src/octo_async_worker/telemetry.py, services/load-control/src/octo_load_control/telemetry.py, services/object-pipeline/src/octo_object_pipeline/telemetry.py, services/remediator/src/octo_remediator/telemetry.py, services/edge-fuzz/src/octo_edge_fuzz/telemetry.py</read_first>
  <action>Extend the Plan 01 signal inventory test to assert Java structured event source contains `request_id`, `workflow_id`, `service.name`, `payment.token.safe`, and `payment.gateway.request_id`; assert every support-service telemetry helper contains `service.instance.id` and `SERVICE_INSTANCE_ID`. Do not weaken existing Plan 01 assertions.</action>
  <verify>python3 -m pytest -q tests/test_signal_contract_inventory.py</verify>
  <acceptance_criteria>
    - `tests/test_signal_contract_inventory.py` contains `payment.token.safe`.
    - `tests/test_signal_contract_inventory.py` contains `SERVICE_INSTANCE_ID`.
    - `python3 -m pytest -q tests/test_signal_contract_inventory.py` exits 0.
  </acceptance_criteria>
  <done>The inventory test guards Java and support-service signal drift.</done>
</task>

</tasks>

<verification>
- [ ] cd services/apm-java-demo && mvn test
- [ ] python3 -m pytest -q tests/test_signal_contract_inventory.py
- [ ] python3 -m pytest -q services/load-control services/remediator services/object-pipeline services/edge-fuzz services/async-worker
- [ ] git diff --check -- services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java services/apm-java-demo/src/main/java/com/octo/apmdemo/OtelSupport.java services/apm-java-demo/src/test/java/com/octo/apmdemo/AppTest.java services/async-worker/src/octo_async_worker/telemetry.py services/load-control/src/octo_load_control/telemetry.py services/object-pipeline/src/octo_object_pipeline/telemetry.py services/remediator/src/octo_remediator/telemetry.py services/edge-fuzz/src/octo_edge_fuzz/telemetry.py tests/test_signal_contract_inventory.py
</verification>

<success_criteria>
- Java payment sidecar stdout JSON is APM/Log Analytics joinable by request, workflow, payment, and service fields.
- Support services emit stable resource identity and keep script span correlation attributes.
- Java and support-service targeted tests pass or documented local environment constraints are captured in the summary.
</success_criteria>

<output>
After completion, create `.planning/phases/01-signal-contract-hardening/01-03-SUMMARY.md`.
</output>
