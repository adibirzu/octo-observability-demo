---
phase: 01-signal-contract-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_signal_contract_inventory.py
  - site/architecture/correlation-contract.md
  - site/architecture/service-inventory.md
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-05
user_setup: []
must_haves:
  truths:
    - "The repository has a local signal contract test that checks app, Java, support-service, APM, Log Analytics, and Monitoring assets."
    - "The correlation contract documents request_id, workflow_id, service identity, payment gateway, APM saved-query, Log Analytics, and Monitoring enforcement points."
    - "The service inventory points operators to the tests that guard APM-to-Log-Analytics and Monitoring drift."
  artifacts:
    - path: tests/test_signal_contract_inventory.py
      provides: "Source-level signal contract coverage checks"
      min_lines: 80
      contains: "REQUIRED_APP_FIELDS"
    - path: site/architecture/correlation-contract.md
      provides: "Authoritative field and enforcement contract"
      contains: "Signal contract enforcement"
    - path: site/architecture/service-inventory.md
      provides: "Operator-facing service and validation inventory"
      contains: "signal contract inventory"
  key_links:
    - from: tests/test_signal_contract_inventory.py
      to: site/architecture/correlation-contract.md
      via: "field constants and source assertions"
      pattern: "correlation-contract.md"
    - from: tests/test_signal_contract_inventory.py
      to: deploy/oci/log_analytics/fields/octo-apm-field-reuse-map.json
      via: "reuse-map assertion"
      pattern: "octo-apm-field-reuse-map.json"
---

<objective>
Create the Phase 1 source-level signal contract inventory and document where
the contract is enforced.

Purpose: Give later Phase 1 plans a failing/passing local gate for the exact
fields that must survive across app logs, Java logs, support-service spans, APM
queries, Log Analytics assets, and Monitoring metrics.
Output: A root pytest contract test plus documentation updates that describe
the enforcement points.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-signal-contract-hardening/01-CONTEXT.md
@.planning/phases/01-signal-contract-hardening/01-RESEARCH.md
@.planning/phases/01-signal-contract-hardening/01-PATTERNS.md
@site/architecture/correlation-contract.md
@site/architecture/service-inventory.md
@tests/test_unified_deploy_surface.py
@tests/test_log_analytics_attack_assets.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source-level signal contract inventory test</name>
  <files>tests/test_signal_contract_inventory.py</files>
  <read_first>site/architecture/correlation-contract.md, site/architecture/service-inventory.md, tests/test_unified_deploy_surface.py, tests/test_log_analytics_attack_assets.py, shop/server/observability/logging_sdk.py, crm/server/observability/logging_sdk.py, services/apm-java-demo/src/main/java/com/octo/apmdemo/App.java, deploy/oci/apm/saved-queries/README.md, deploy/oci/log_analytics/fields/octo-apm-field-reuse-map.json</read_first>
  <action>Create `tests/test_signal_contract_inventory.py`. Define constants named `REQUIRED_APP_FIELDS`, `REQUIRED_RESOURCE_FIELDS`, `REQUIRED_PAYMENT_FIELDS`, and `REQUIRED_MONITORING_VALUES`. Add pytest tests that assert Shop and CRM logging SDKs include `trace_id`, `span_id`, `oracleApmTraceId`, `oracleApmSpanId`, `request_id`, `workflow_id`, `workflow_step`, `service.name`, `service.namespace`, `service.instance.id`, and `deployment.environment`; Java structured events include `trace_id`, `span_id`, `oracleApmTraceId`, `oracleApmSpanId`, `service_name`, `service_namespace`, and `payment_gateway_request_id`; support service telemetry files include `service.name`, `service.namespace`, `deployment.environment`, `cloud.provider`, and `oci.demo.stack`; APM saved-query JSON files include checkout, payment Java sidecar, DB slow spans, login auth, GenAI LLMetry, service errors, platform workflows, and trace drilldown; Log Analytics field reuse map includes `Trace ID`, `Span ID`, `Service Name`, `Service Namespace`, `Order ID`, and `Payment Gateway Request ID`; Monitoring publisher source contains `octo_apm_demo`, `OCI_REGION`, `OCI_REGION_ID`, and `telemetry-ingestion`.</action>
  <verify>python3 -m pytest -q tests/test_signal_contract_inventory.py</verify>
  <acceptance_criteria>
    - `tests/test_signal_contract_inventory.py` contains `REQUIRED_APP_FIELDS`.
    - `tests/test_signal_contract_inventory.py` contains `REQUIRED_RESOURCE_FIELDS`.
    - `tests/test_signal_contract_inventory.py` contains `REQUIRED_PAYMENT_FIELDS`.
    - `python3 -m pytest -q tests/test_signal_contract_inventory.py` exits 0.
  </acceptance_criteria>
  <done>The new root test fails when key signal-contract fields or assets are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Document signal contract enforcement points</name>
  <files>site/architecture/correlation-contract.md, site/architecture/service-inventory.md</files>
  <read_first>site/architecture/correlation-contract.md, site/architecture/service-inventory.md, tests/test_signal_contract_inventory.py, deploy/oci/log_analytics/fields/octo-apm-field-reuse-map.json</read_first>
  <action>Update `site/architecture/correlation-contract.md` with a `## Signal contract enforcement` section. The section must name the source-level guard `python3 -m pytest -q tests/test_signal_contract_inventory.py`, explain that `push_log` stamps trace/log fields, explain that Java stdout JSON is parser input, explain that support-service telemetry helpers must emit resource identity, and state that Monitoring metrics use `octo_apm_demo`. Update `site/architecture/service-inventory.md` validation surfaces with a row for `python3 -m pytest -q tests/test_signal_contract_inventory.py` and describe it as the signal contract inventory for APM, Log Analytics, and Monitoring drift.</action>
  <verify>python3 -m mkdocs build --strict</verify>
  <acceptance_criteria>
    - `site/architecture/correlation-contract.md` contains `## Signal contract enforcement`.
    - `site/architecture/correlation-contract.md` contains `tests/test_signal_contract_inventory.py`.
    - `site/architecture/service-inventory.md` contains `signal contract inventory`.
    - `python3 -m mkdocs build --strict` exits 0.
  </acceptance_criteria>
  <done>Docs identify the local enforcement point for future operators and GSD phases.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m pytest -q tests/test_signal_contract_inventory.py
- [ ] python3 -m mkdocs build --strict
- [ ] git diff --check -- tests/test_signal_contract_inventory.py site/architecture/correlation-contract.md site/architecture/service-inventory.md
</verification>

<success_criteria>
- Source-level signal contract inventory exists and passes.
- Contract docs name the enforcement points.
- No public docs expose tenancy-specific values or secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-signal-contract-hardening/01-01-SUMMARY.md`.
</output>
