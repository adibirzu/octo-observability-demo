---
phase: 01-signal-contract-hardening
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tests/test_observability_asset_contract.py
  - deploy/oci/apm/saved-queries/README.md
  - deploy/oci/log_analytics/README.md
  - site/observability-v2/apm-drilldown.md
  - site/observability-v2/log-analytics-dashboards.md
autonomous: true
requirements:
  - OBS-02
  - OBS-03
  - OBS-05
user_setup: []
must_haves:
  truths:
    - "APM saved-query assets cover checkout, Java payment, DB slow spans, login/auth, GenAI LLMetry, service errors, platform workflows, and trace drilldown."
    - "Log Analytics fields/searches/dashboards are locally checked for required APM trace, service, workflow, order, payment, OKE, and Monitoring pivots."
    - "Observability docs explain how to validate assets locally before live OCI deployment."
  artifacts:
    - path: tests/test_observability_asset_contract.py
      provides: "APM, Log Analytics, and Monitoring asset contract tests"
      min_lines: 90
      contains: "REQUIRED_APM_QUERIES"
    - path: deploy/oci/apm/saved-queries/README.md
      provides: "APM saved-query asset runbook"
      contains: "checkout-end-to-end"
    - path: deploy/oci/log_analytics/README.md
      provides: "Log Analytics asset runbook"
      contains: "octo-apm-field-reuse-map.json"
    - path: site/observability-v2/log-analytics-dashboards.md
      provides: "Dashboard and saved-search validation docs"
      contains: "service-trace-log-coverage"
  key_links:
    - from: tests/test_observability_asset_contract.py
      to: deploy/oci/apm/saved-queries
      via: "saved-query inventory assertions"
      pattern: "REQUIRED_APM_QUERIES"
    - from: tests/test_observability_asset_contract.py
      to: deploy/oci/log_analytics/searches
      via: "required field/search assertions"
      pattern: "REQUIRED_LOG_ANALYTICS_SEARCHES"
---

<objective>
Guard APM saved queries, Log Analytics assets, and Monitoring contract values
with local tests and update docs with the validation path.

Purpose: Phase 1 must ensure the versioned OCI observability assets keep
matching the app signal contract before later phases deploy or validate them
live.
Output: Root asset-contract tests plus APM/Log Analytics documentation updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-signal-contract-hardening/01-CONTEXT.md
@.planning/phases/01-signal-contract-hardening/01-RESEARCH.md
@.planning/phases/01-signal-contract-hardening/01-PATTERNS.md
@site/architecture/correlation-contract.md
@deploy/oci/apm/saved-queries/README.md
@deploy/oci/log_analytics/README.md
@site/observability-v2/apm-drilldown.md
@site/observability-v2/log-analytics-dashboards.md
@tests/test_log_analytics_attack_assets.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add observability asset contract test</name>
  <files>tests/test_observability_asset_contract.py</files>
  <read_first>deploy/oci/apm/saved-queries/README.md, deploy/oci/apm/saved-queries/checkout-end-to-end.json, deploy/oci/apm/saved-queries/payment-java-sidecar.json, deploy/oci/log_analytics/fields/octo-apm-field-reuse-map.json, deploy/oci/log_analytics/searches/service-trace-log-coverage.sql, deploy/oci/log_analytics/searches/melts-collection-completeness.sql, deploy/oci/log_analytics/dashboards/workflow-command-center.json, tests/test_log_analytics_attack_assets.py</read_first>
  <action>Create `tests/test_observability_asset_contract.py`. Define `REQUIRED_APM_QUERIES` with `assistant-genai-llmetry`, `checkout-end-to-end`, `db-slow-spans`, `login-auth-flow`, `payment-java-sidecar`, `platform-workflows`, `service-errors`, and `trace-drilldown`. Define `REQUIRED_LOG_ANALYTICS_SEARCHES` with `service-trace-log-coverage`, `checkout-payment-correlation`, `auth-login-correlation`, `genai-assistant-llmetry`, `service-error-triage`, `db-slowness-hotspots`, `melts-collection-completeness`, `oke-checkout-payment-correlation`, and `oke-onm-ingestion-health`. Add assertions that saved-query JSON files exist and contain at least one OCTO service or field pivot, Log Analytics searches contain `Trace ID` or `oracleApmTraceId`, the field reuse map contains `Trace ID`, `Span ID`, `Service Name`, `Service Namespace`, `Order ID`, and `Payment Gateway Request ID`, dashboards reference saved searches, and Monitoring code contains `octo_apm_demo` and `telemetry-ingestion`.</action>
  <verify>python3 -m pytest -q tests/test_observability_asset_contract.py tests/test_log_analytics_attack_assets.py</verify>
  <acceptance_criteria>
    - `tests/test_observability_asset_contract.py` contains `REQUIRED_APM_QUERIES`.
    - `tests/test_observability_asset_contract.py` contains `REQUIRED_LOG_ANALYTICS_SEARCHES`.
    - `tests/test_observability_asset_contract.py` asserts `octo_apm_demo`.
    - The targeted pytest command exits 0.
  </acceptance_criteria>
  <done>APM, Log Analytics, and Monitoring source assets have local drift tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update APM and Log Analytics validation docs</name>
  <files>deploy/oci/apm/saved-queries/README.md, deploy/oci/log_analytics/README.md, site/observability-v2/apm-drilldown.md, site/observability-v2/log-analytics-dashboards.md</files>
  <read_first>deploy/oci/apm/saved-queries/README.md, deploy/oci/log_analytics/README.md, site/observability-v2/apm-drilldown.md, site/observability-v2/log-analytics-dashboards.md, tests/test_observability_asset_contract.py, site/architecture/correlation-contract.md</read_first>
  <action>Update the docs to include the local validation command `python3 -m pytest -q tests/test_observability_asset_contract.py tests/test_log_analytics_attack_assets.py`, identify the required APM saved queries by filename, identify the required Log Analytics saved searches by filename, and state that `octo-apm-field-reuse-map.json` is the reuse-first field map. Add a short note that live OCI import/deployment remains a separate operator step and this phase does not run Terraform apply or create OCI resources.</action>
  <verify>python3 -m mkdocs build --strict</verify>
  <acceptance_criteria>
    - `deploy/oci/apm/saved-queries/README.md` contains `tests/test_observability_asset_contract.py`.
    - `deploy/oci/log_analytics/README.md` contains `octo-apm-field-reuse-map.json`.
    - `site/observability-v2/apm-drilldown.md` contains `payment-java-sidecar`.
    - `site/observability-v2/log-analytics-dashboards.md` contains `melts-collection-completeness`.
    - `python3 -m mkdocs build --strict` exits 0.
  </acceptance_criteria>
  <done>Operators have a documented local gate before deploying observability assets to OCI.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 1 combined validation</name>
  <files>tests/test_signal_contract_inventory.py, tests/test_observability_asset_contract.py</files>
  <read_first>tests/test_signal_contract_inventory.py, tests/test_observability_asset_contract.py, site/architecture/correlation-contract.md, site/observability-v2/log-analytics-dashboards.md</read_first>
  <action>Run the combined local validation commands for Phase 1 and make small source/test/doc corrections if they reveal contract drift introduced by Plans 01 through 04. Do not remove required assertions to make tests pass; fix the source or asset references instead.</action>
  <verify>python3 -m pytest -q tests/test_signal_contract_inventory.py tests/test_observability_asset_contract.py tests/test_log_analytics_attack_assets.py && python3 -m mkdocs build --strict && git diff --check</verify>
  <acceptance_criteria>
    - Combined pytest command exits 0.
    - `python3 -m mkdocs build --strict` exits 0.
    - `git diff --check` exits 0.
  </acceptance_criteria>
  <done>Phase 1 local gates pass as a set.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m pytest -q tests/test_observability_asset_contract.py tests/test_log_analytics_attack_assets.py
- [ ] python3 -m pytest -q tests/test_signal_contract_inventory.py tests/test_observability_asset_contract.py tests/test_log_analytics_attack_assets.py
- [ ] python3 -m mkdocs build --strict
- [ ] git diff --check
</verification>

<success_criteria>
- APM saved query and Log Analytics source assets are locally guarded.
- Monitoring namespace and ingestion endpoint assumptions are locally guarded.
- Docs explain local validation and keep live OCI changes out of Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-signal-contract-hardening/01-04-SUMMARY.md`.
</output>
