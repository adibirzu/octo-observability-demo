---
phase: 01-signal-contract-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - shop/server/observability/logging_sdk.py
  - crm/server/observability/logging_sdk.py
  - crm/server/middleware/tracing.py
  - shop/tests/test_logging_sdk.py
  - crm/tests/test_logging_sdk.py
  - crm/tests/test_observability_capabilities.py
autonomous: true
requirements:
  - OBS-01
  - OBS-02
user_setup: []
must_haves:
  truths:
    - "Shop and CRM push_log output carries request_id, workflow_id, workflow_step, trace_id, span_id, oracleApmTraceId, oracleApmSpanId, service.name, service.namespace, service.instance.id, and deployment.environment when context exists."
    - "CRM request logs include the same workflow and request-id pivots as Admin actions need for Log Analytics and APM drilldown."
    - "Log enrichment remains token-safe and continues masking email, phone, card, CVV, and username fields."
  artifacts:
    - path: shop/server/observability/logging_sdk.py
      provides: "Shop structured log enrichment"
      contains: "request_id"
    - path: crm/server/observability/logging_sdk.py
      provides: "CRM structured log enrichment"
      contains: "request_id"
    - path: crm/server/middleware/tracing.py
      provides: "CRM request span and log enrichment"
      contains: "workflow_id"
    - path: shop/tests/test_logging_sdk.py
      provides: "Shop logging contract tests"
      contains: "request_id"
    - path: crm/tests/test_logging_sdk.py
      provides: "CRM logging contract tests"
      contains: "request_id"
  key_links:
    - from: shop/server/security/request_id.py
      to: shop/server/observability/logging_sdk.py
      via: "current_request_id imported or equivalent request-state context"
      pattern: "current_request_id"
    - from: crm/server/security/request_id.py
      to: crm/server/observability/logging_sdk.py
      via: "current_request_id imported or equivalent request-state context"
      pattern: "current_request_id"
---

<objective>
Harden Shop and CRM/Admin FastAPI structured logging and request spans so app
logs can always pivot to APM traces, workflows, users/orders, and request IDs.

Purpose: The main demo flows depend on FastAPI logs and spans being joinable in
Log Analytics and OCI APM.
Output: Updated Shop/CRM logging and middleware code plus targeted tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-signal-contract-hardening/01-CONTEXT.md
@.planning/phases/01-signal-contract-hardening/01-RESEARCH.md
@.planning/phases/01-signal-contract-hardening/01-PATTERNS.md
@site/architecture/correlation-contract.md
@shop/server/observability/logging_sdk.py
@crm/server/observability/logging_sdk.py
@shop/server/middleware/tracing.py
@crm/server/middleware/tracing.py
@shop/server/security/request_id.py
@crm/server/security/request_id.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add request-id context to Shop and CRM push_log</name>
  <files>shop/server/observability/logging_sdk.py, crm/server/observability/logging_sdk.py, shop/tests/test_logging_sdk.py, crm/tests/test_logging_sdk.py</files>
  <read_first>shop/server/observability/logging_sdk.py, crm/server/observability/logging_sdk.py, shop/server/security/request_id.py, crm/server/security/request_id.py, shop/tests/test_logging_sdk.py, crm/tests/test_logging_sdk.py, site/architecture/correlation-contract.md</read_first>
  <action>Update both `push_log` implementations so they import or safely call `current_request_id` from each app's `server.security.request_id`. If the caller did not provide `request_id` and `current_request_id()` returns a value, set `kwargs["request_id"]` to that value before PII masking and before `_add_current_span_log_event`. Preserve existing `correlation.id` behavior. Add tests in both logging SDK test files that set the request-id context through the existing middleware/context helper or monkeypatch `current_request_id` and assert the emitted JSON contains `request_id`, `trace_id`/`oracleApmTraceId` when a valid span exists, `service.name`, `service_namespace`, and does not expose raw PII for email/card/CVV cases already covered.</action>
  <verify>python3 -m pytest -q shop/tests/test_logging_sdk.py crm/tests/test_logging_sdk.py</verify>
  <acceptance_criteria>
    - `shop/server/observability/logging_sdk.py` contains `current_request_id`.
    - `crm/server/observability/logging_sdk.py` contains `current_request_id`.
    - `shop/tests/test_logging_sdk.py` asserts `request_id`.
    - `crm/tests/test_logging_sdk.py` asserts `request_id`.
    - `python3 -m pytest -q shop/tests/test_logging_sdk.py crm/tests/test_logging_sdk.py` exits 0.
  </acceptance_criteria>
  <done>Application logs get a durable request ID without each caller passing it manually.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich CRM request spans and logs with workflow/page context</name>
  <files>crm/server/middleware/tracing.py, crm/tests/test_observability_capabilities.py</files>
  <read_first>crm/server/middleware/tracing.py, shop/server/middleware/tracing.py, crm/server/observability/workflow_context.py, crm/server/observability/correlation.py, crm/tests/test_observability_capabilities.py, site/architecture/correlation-contract.md</read_first>
  <action>Update CRM `TracingMiddleware` to resolve workflow context using the existing CRM workflow helper and add `workflow.id`, `workflow.step`, `workflow_id`, `workflow_step`, `request_id`, `http.url.path`, `http.method`, `http.status_code`, `http.response_time_ms`, and `correlation.id` to request completion and slow/error `push_log` calls. Set the same workflow fields on `middleware.entry` and `response.finalize` spans. Use `request.state.request_id` when present and fall back to the incoming `X-Request-Id` header, then generated correlation ID. Do not remove existing `auth.check`, `request.validate`, WAF, security-span, or DB-session tagging behavior.</action>
  <verify>python3 -m pytest -q crm/tests/test_observability_capabilities.py crm/tests/test_logging_sdk.py</verify>
  <acceptance_criteria>
    - `crm/server/middleware/tracing.py` contains `workflow_id`.
    - `crm/server/middleware/tracing.py` contains `workflow.step`.
    - `crm/server/middleware/tracing.py` contains `request.state.request_id`.
    - Existing `auth.check`, `request.validate`, and `response.finalize` span names remain present.
    - `python3 -m pytest -q crm/tests/test_observability_capabilities.py crm/tests/test_logging_sdk.py` exits 0.
  </acceptance_criteria>
  <done>CRM/Admin request logs and spans include the fields needed for auth/order/Admin troubleshooting.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run signal inventory for FastAPI app fields</name>
  <files>tests/test_signal_contract_inventory.py</files>
  <read_first>tests/test_signal_contract_inventory.py, shop/server/observability/logging_sdk.py, crm/server/observability/logging_sdk.py, crm/server/middleware/tracing.py</read_first>
  <action>If Plan 01 created `tests/test_signal_contract_inventory.py`, extend or adjust its FastAPI assertions so it checks `current_request_id`, `workflow_id`, and `workflow_step` for both Shop and CRM structured logging paths. Do not weaken any constants created by Plan 01.</action>
  <verify>python3 -m pytest -q tests/test_signal_contract_inventory.py shop/tests/test_logging_sdk.py crm/tests/test_logging_sdk.py</verify>
  <acceptance_criteria>
    - `tests/test_signal_contract_inventory.py` contains `current_request_id`.
    - `tests/test_signal_contract_inventory.py` contains `workflow_step`.
    - Targeted pytest command exits 0.
  </acceptance_criteria>
  <done>The global inventory test covers the new FastAPI logging contract.</done>
</task>

</tasks>

<verification>
- [ ] python3 -m pytest -q shop/tests/test_logging_sdk.py crm/tests/test_logging_sdk.py
- [ ] python3 -m pytest -q crm/tests/test_observability_capabilities.py
- [ ] python3 -m pytest -q tests/test_signal_contract_inventory.py
- [ ] git diff --check -- shop/server/observability/logging_sdk.py crm/server/observability/logging_sdk.py crm/server/middleware/tracing.py shop/tests/test_logging_sdk.py crm/tests/test_logging_sdk.py crm/tests/test_observability_capabilities.py tests/test_signal_contract_inventory.py
</verification>

<success_criteria>
- FastAPI app logs can be joined by request, workflow, trace, span, service, and deployment fields.
- Existing PII masking and payment token-safety behavior is preserved.
- Targeted tests and the signal inventory pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-signal-contract-hardening/01-02-SUMMARY.md`.
</output>
