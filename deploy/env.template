# ============================================================================
# Enterprise CRM Portal — tenancy-portable environment template
# Copy to `.env` and fill. Never commit `.env`.
# Shares schema with Octo Drone Shop env.template (same OCI tenancy).
# ============================================================================

# ---- Public domains ----
SHOP_DOMAIN=shop.example.cloud
CRM_DOMAIN=crm.example.cloud
OPS_DOMAIN=ops.example.cloud
COORDINATOR_DOMAIN=coordinator.example.cloud
CONTROL_PLANE_URL=https://ops.example.cloud
PLATFORM_BACKEND_URL=https://ops-api.example.cloud

# ---- OCI tenancy ----
# Set OCI_REGION to your target region key, for example `us-phoenix-1`.
OCI_REGION=
OCI_TENANCY_OCID=
OCI_COMPARTMENT_OCID=

# ---- OCI Logging ----
OCI_LOG_GROUP_CRM=
OCI_LOG_GROUP_WAF=
OCI_LOG_GROUP_CHAOS_AUDIT=

# ---- OCI APM ----
OCI_APM_DOMAIN_OCID=
OCI_APM_PUBLIC_KEY=
OCI_APM_PRIVATE_KEY=
OCI_APM_UPLOAD_ENDPOINT=

# ---- OCI Log Analytics ----
OCI_LA_NAMESPACE=
OCI_LA_LOG_GROUP_OCID=

# ---- OCI WAF ----
OCI_WAF_POLICY_OCID_CRM=
WAF_MODE=DETECTION

# ---- Secrets / Vault ----
OCI_VAULT_OCID=
OCI_KMS_KEY_OCID=
BOOTSTRAP_ADMIN_PASSWORD_SECRET_OCID=

# ---- Database (Oracle ATP) ----
DB_TNS_NAME=
DB_WALLET_SECRET_OCID=
DB_USER_SECRET_OCID=
DB_PASSWORD_SECRET_OCID=

# ---- Chaos control (CRM IS CONTROLLER) ----
CHAOS_ENABLED=false
CHAOS_STATE_BACKEND=db
CHAOS_STATE_BUCKET=octo-chaos-state
CHAOS_STATE_POLL_SECONDS=10
CHAOS_MAX_TTL_SECONDS=3600
CHAOS_ADMIN_ROLE=chaos-operator

# ---- Feature flags ----
AUTOREMEDIATE_ENABLED=false
LA_ENRICHMENT_V2=true

# ---- Security ----
CORS_ALLOWED_ORIGINS=https://crm.example.cloud,https://ops.example.cloud,https://coordinator.example.cloud
SESSION_COOKIE_SECURE=true
RATE_LIMIT_LOGIN_PER_MIN=10
RATE_LIMIT_ADMIN_PER_MIN=30
CSP_REPORT_URI=

# ---- Cross-service identity ----
SERVICE_NAME_SHOP=octo-drone-shop
SERVICE_NAME_CRM=octo-enterprise-crm
SERVICE_NAME_COORDINATOR=oci-coordinator
